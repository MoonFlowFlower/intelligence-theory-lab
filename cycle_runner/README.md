# Cycle Runner Contract

No runner implementation is authorized in this scaffold.

Future runner work must first add:

```text
theory card validation
experiment contract validation
kill-test checklist validation
negative evidence append-only check
human review packet generation
```

It must not:

```text
implement agents
write selectors
create environments
auto-generate successor theories without human review
run indefinite cycles
```

This directory exists only to reserve the interface boundary for a future explicitly authorized task.

