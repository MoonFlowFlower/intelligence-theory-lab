"""TLGP-CAPABILITY-WITNESS-PREFLIGHT-001A — candidate-free preflight package.

Contains only the pure route-decision adjudicator. Trains nothing, imports no
candidate, touches no TLGP-R2 banked source. The training probe is implemented and
run separately on a GPU machine per the implementation card.
"""
