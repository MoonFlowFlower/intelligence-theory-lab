CSSP-001A (CRITERION-SHADOW-SEPARABILITY-PROBE) claim ceiling — analytic closure, paper-only, no run.

Scope of the closure: holds ONLY for "C treated as an equal-access predictive functional + a same-prior COMPUTED Bayes-optimal shadow + this tiny enumerable toy." It is a bounded analytic (near-theorem) argument, NOT an executed computation and NOT a global result.

What is closed: the c1 criterion (held-out interventional-prediction accuracy over threshold) has no equal-access SEPARATING instance in expectation. Identifiable query => SHADOW_COLLAPSE; non-identifiable query => INDUCTIVE_BIAS_ONLY (only a structure-handed prior passes; that is smuggled access, not a foothold).

Load-bearing assumption (do NOT drop): the closure is conditional on s* actually being the equal-access Bayes-optimal decision rule under C's own loss/threshold. A candidate that appears to beat threshold is a MANDATORY re-audit trigger (re-check s*-optimality / access symmetry / compute-and-H match / oracle label / leakage), NOT a subject foothold. Residual finite-sample inductive-bias flukes are classed NON_FOOTHOLD.

Does NOT prove: consciousness, subjective experience, real emotion, agency, autonomy, functional subject, electronic life; does NOT prove the goal globally impossible; does NOT close fork A. The fork-A regime (non-stationary / generated access, where "Bayes-optimal-given-access" is not a well-defined dominating object) is exactly where the necessary condition could still possibly hold, and it remains genuinely open — and even there the best outcome is un-shadowable mechanism evidence, which does not cross the functional->phenomenal gap.

Governance: NO-CLEAR-for-build (docs-only bank; no callable, no STEP-B, no agent). designer != self-CLEAR. Isomorphic to DRIFT-AXIS-CAPABILITY-ACBU (matched = equal-access Bayes-optimal => candidate <= baseline). Generalized by CDAP-001A-R1 (c1 = its Row 2). Banking this artifact resolves the in-repo provenance for CDAP-001A-R1 rows 2 & 8 [CONJECTURE] cites; the cited object is an analytic near-theorem argument, so downstream certificates should read it as analytic/reduction, not as an executed proof.
