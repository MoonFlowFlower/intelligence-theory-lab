# TLGP-001B full-frozen-budget run requirements (validated versions in parentheses).
# Any reasonably recent versions should work; the experiment logic is version-agnostic.
numpy            # validated: 2.2.6
scikit-learn     # validated: 1.7.2   (lower_reference baselines + 001A MI leakage detector)
torch            # validated: 2.12.1+cpu  (CUDA build strongly recommended for the full budget)
# CPU-only torch:   pip install torch --index-url https://download.pytorch.org/whl/cpu
# CUDA torch:       see https://pytorch.org/get-started/locally/
