# GATE1-REPLACEMENT-READBACK-OR-PREFLIGHT-SELECTION-001A Selection Analysis

## Layer And Objective

Current layer: engineering-governance / Gate1 replacement readback and preflight selection.

Mainline integration status: none.

Enabled status: readback plus local artifact generation only. No runtime path, scheduler path, admission path, Gate execution path, Gate1 execution path, Gate4/Gate5 path, or candidate path was enabled.

Real trigger evidence: current repo readback plus the named local inventory, provenance-hardening, verifier-hardening, hostile-audit, historical-calibration, and baseline-immunity contract artifacts.

Claim ceiling: Gate1 replacement selection only.

Real objective: return toward Gate mainline development without reusing old Gate1 positive inheritance, without jumping to Gate4/Gate5, and without treating the provenance verifier or baseline-immunity standard as an executor.

## Bounded Audit

Problem definition risk: the task could become a hidden Gate1 implementation or a Gate4/Gate5 route jump. This run stays at selection and task-card drafting only.

Strongest baseline explanation: the old Gate1 apparent success can be explained by graph/cache, lookup, transition-table, count-table, FSM/planner, episodic-traversal, and replay/cache controls rather than by a mechanism-specific effect.

Strongest reason this task may be invalid: if the current local artifacts conflict or if the dirty worktree makes canonical status ambiguous, then any replacement-preflight draft would be premature. The readback found dirty local evidence surfaces, but the task card explicitly named those local artifacts and no contradiction was found among the status-bearing files read in this scope.

Falsification of current framing: a future replacement preflight would be invalid if it requires candidate code, uses old Gate1 positive inheritance, omits any required graph-cache member, treats the verifier as admission, treats the baseline-immunity standard as an executor, or cannot define a candidate-free target variable and observable channel.

Evidence still insufficient after this task: any Gate1 pass, mechanism validity, candidate success, baseline-immunity enforcement, Gate4/Gate5 readiness, mainline effect, runtime/live effect, agency, autonomy, consciousness, emotion, stable user benefit, or EGO readiness.

Mechanism-vs-behavior classification: this task is engineering-governance and preflight selection. The selected next task would be a candidate-free mechanism-preflight gate over a proposed Gate1 measurement object, not a mechanism implementation.

Anti-hardcoding / leakage / weak-baseline checks: the draft blocks hard-coded verdicts, candidate-authored ground truth, leakage side channels, omitted graph-cache members, weak learned stubs, direct objective optimizer equivalence, replay hash-only checks, schema splits, second logic paths, post-hoc threshold tuning, and claim inflation.

Minimal validation for this task: JSON parse validation for generated JSON, baseline-immunity registry parse validation, artifact path allowlist check, forbidden positive-claim scan, and `git diff --check -- docs/decision_log.md`. No pytest, no Gate, no historical Gate module, and no Route C module is authorized.

Stop condition assessment: no stop condition requiring `blocked_pending_canonical_readback_conflict` was found in the bounded readback. Existing dirty local artifacts remain uncommitted and unanchored, but they are the exact local evidence inputs named by the task and are not treated as remote-anchored truth.

## Fact From Repo Readback

- Branch: `codex/meta-theory-scaffold`.
- HEAD: `b45598b1c56080f2f850ae088f42cf585950483e`.
- Upstream: `origin/codex/meta-theory-scaffold`, ahead 9, behind 0.
- Worktree before this artifact bundle: `docs/decision_log.md` modified, no staged files, many untracked governance/evidence artifacts.
- `artifacts/gate_canonical_inventory_and_return_point_readback_001a/` exists and selected `return_to_gate_provenance_hardening`.
- `artifacts/gate_provenance_hardening_worktree_reconciliation_001a/readback.json` exists and records `gate_provenance_hardening_reconciled`.
- `artifacts/gate_evidence_provenance_verifier_hardening_001a/readback.json` exists and records `gate_evidence_provenance_verifier_hardened`.
- `artifacts/CLAUDE-INDEPENDENT-GATE-EVIDENCE-PROVENANCE-VERIFIER-001A-HOSTILE-AUDIT-001A/audit_result.json` exists and records `accept_as_local_provenance_shape_checker`.
- `artifacts/gate_evidence_provenance_verifier_001a_historical_negative_calibration_001a/calibration_results.json` exists and records diagnostic-only historical calibration with no `provenance_wellformed_only` historical result.
- `docs/codex/contracts/BASELINE-IMMUNITY-ADMISSION-STANDARD-001A.md` and `.registry.json` exist.
- The registry parses and declares `executor: none`.
- `docs/research/GATE1-FAILED-GRAPH-CACHE-RECONCILIATION-001A.md` exists.
- `artifacts/post_freeze_gate0_3_sequential_repair_queue_001a_gate1_failed_graph_cache_reconciliation/baseline_comparison.json` reports full graph/cache family completion and candidate collapse.
- No `GATE1-REPLACEMENT` or `gate1_replacement` source/test path was found in the bounded search.

## Inference

- The provenance-first return route selected by the earlier inventory has been locally reconciled enough to draft a pre-candidate Gate1 replacement preflight card.
- The next task should be `mechanism_preflight`, not another lower-gate provenance-only task, because the remaining blocker is not file presence or verifier shape; it is whether a new Gate1 measurement object can survive baseline-immunity and graph-cache preflight before any candidate exists.
- The preflight must be candidate-free and must not design a new mechanism surface in this task. It should consume a frozen surface specification pack or stop if the pack is missing.
- Old Gate1 positive inheritance remains closed and cannot be reused.

## Assumption

- The local untracked evidence artifacts are intentionally present because the task explicitly named them as current local evidence. This task does not claim they are committed, pushed, tagged, or remotely anchored.

## Unknown

- No fresh Gate0, Gate1, Gate2, Gate3, Gate4, Gate5, Route C, bridge, admission, runtime, or EGO module was executed.
- No replacement Gate1 surface has been proven admissible.
- No baseline-immunity executor exists.
- No verifier historical schema shim exists.

## 1. What Exactly Failed In Old Gate1?

Old Gate1 failed as positive parent evidence through graph-cache collapse and lookup/replay equivalence. The decisive readback is `failed_graph_cache_collapse`: `graph_lookup`, `transition_table`, `successor_map`, `count_table`, `fsm_planner`, and `episodic_traversal` were present in callable ledger evidence and could match or beat the candidate lineage under the frozen rule. Additional cache/replay controls also matched. The apparent Gate1 001C pass is treated as a literal/report-shaped risk and is not upgraded.

Not established as the decisive old-Gate1 closure cause in this readback: direct objective optimizer equivalence, leakage, or source-pin conflict. Those remain required replacement checks because they are proven failure families elsewhere in the lab.

## 2. What A Replacement Gate1 Preflight Must Prove Before Candidate Implementation

A replacement preflight must prove, before any candidate implementation, that the proposed Gate1 measurement object has a clear target variable, legal observable channel, fixed action/query budget, generator provenance, and source/readback pins. It must show that candidate-free baselines and challengers are actually invoked by callable paths, with producer function, input artifacts, run id, seed/context/episode ids, aggregation rule, and code path hash.

Minimum required panels:

- Full six-member graph-cache panel: `graph_lookup`, `transition_table`, `successor_map`, `count_table`, `fsm_planner`, `episodic_traversal`.
- Lookup imitation panel: `trace_only_replay`, `ngram_trace_lookup`, `full_bundle_decoder`, `serialized_state_decoder`, `belief_table`, `pair_count_table`.
- Direct objective optimizer panel when the objective admits it: `discounted_wls`, `least_squares`, `convex_solver`.
- Trivial predictor panel where set/classification metrics exist: `predict_all`, `predict_none`, `constant_k_sweep`, `random`, `majority`.
- Passive baseline/value decoder panel: observation-only and value decoders over mean, variance, correlation, PCA, and nearest-neighbor passive features.
- Active/query panel when actions or queries are available: exhaustive legal query and greedy/uncertainty query under the same budget.
- Leakage scanner with at least one fail-able positive control on the same admission path.
- Replay recomputation from serialized state plus observation, not hash-only replay.
- Source-pin/readback provenance with at least two independent authoritative channels or a fail-closed conflict.
- Evidence-bundle shape compatible with the local verifier, used as prefilter only.
- Compliance with `BASELINE-IMMUNITY-ADMISSION-STANDARD-001A` as static normative contract, not as executor.

## 3. Is A Gate1 Replacement Preflight Ready To Draft?

Yes, as a pre-candidate task card only.

The readback supports drafting because the next task can be framed without a candidate: it tests whether a frozen proposed Gate1 surface specification is admissible for later candidate-card drafting. The draft explicitly stops if the target variable, observable channel, action/query budget, generator source pins, strongest fair baseline panel, or stop conditions cannot be specified without reusing old Gate1 positive inheritance.

This does not mean a replacement surface is admissible. It means drafting the preflight card is admissible.

## 4. Provenance-First Or Mechanism-Preflight?

Selected route type: `mechanism_preflight`.

Reason: the prior provenance-first route has now produced local reconciliation, the verifier has independent hostile-audit acceptance as a shape prefilter only, historical calibration has completed diagnostic-only, and the baseline-immunity standard is present as a static contract. The next closed-loop action is to draft a Gate1 replacement preflight card constrained by those boundaries, not to run another lower-gate provenance-only task.

## Required Verdict

`draft_gate1_replacement_preflight_card`
