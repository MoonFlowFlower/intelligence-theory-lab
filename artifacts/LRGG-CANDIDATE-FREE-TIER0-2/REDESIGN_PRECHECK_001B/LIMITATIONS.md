# LIMITATIONS

- Redesign PRECHECK only; not an official rerun, not candidate work, not Tier 3+.
- Rule structure is public to all agents; only latent T is hidden (twin bit g_lo is intervention-only).
- Required baseline panel is passive/lookup/regression/classical-class; an ACTIVE interventional query baseline is NOT in the panel; a future re-audit should add one before any headroom claim.
- Evidence bounded to this harness and this generated debug split.
