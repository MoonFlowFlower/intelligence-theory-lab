# CLAUDE-INDEPENDENT-DRAFT-ROUTE-C-CANDIDATE-CARD-001A-R1-REAUDIT-001A

R1 hostile re-audit preservation artifact for
`DRAFT-ROUTE-C-CANDIDATE-CARD-001A-R1`.

Date preserved: 2026-06-16

## Verdict Preserved

`accepted_for_candidate_implementation_card_drafting_only`

This authorizes drafting `ROUTE-C-CANDIDATE-IMPLEMENTATION-CARD-001A` only. It
does not authorize candidate implementation, source changes, tests, Gate
execution, mainline wiring, runtime/live integration, push, tag, or remote
anchor.

## Status Fields

- Current layer: engineering-governance / implementation-card drafting only.
- Mainline integration status: none.
- Enabled status: none.
- Real trigger evidence: local docs/artifacts only; no executable candidate, no
  CLI implementation, no pytest implementation, no Gate runner, no runtime/live
  path.
- Claim ceiling: R1 hostile re-audit preservation and bounded
  candidate-implementation-card drafting only.
- Auto-Remote-Anchor: forbidden.

## Required Bindings

### RF-1: Forged-Provenance Option-2 Closure

The future implementation card must extend forged-provenance positive controls
to option-2, include a co-forged-input positive control where value, basis, and
recorded inputs are self-consistent, and fail closed unless recorded inputs are
pinned to independent source artifact hashes. Re-derived basis is admissible
only with independently hashed source-pinned inputs. Self-declared input/basis
consistency is insufficient. Candidate-run material rows are in scope.

### RF-2: Pre-Run Mechanism-Component Lock

The future implementation card must require the mechanism component under test
to be minimal, independently specified outside candidate source, and hashed
before run. Candidate advantage cannot define the component after the fact. The
feature-impoverishment control must use this pre-hashed component, and if
granting it to the fair baseline does not let that baseline approach candidate
performance, the run is invalid.

### RF-3: Independent Anchoring Of B1-B5 / N1-N4

The future implementation card must independently restate and bind B1 F-forge
closure, B2 Phase 0 passive-family superset, B3 fair-interventional baseline
hardening, B4 margin/saturation failability, B5 six graph-cache challengers, N1
prior negatives, N2 frozen margin predeclaration/hash, N3 replay tautology
exclusion, and N4 truth-seed isolation.

### RF-4: Future Implementation Diff Hygiene

The future implementation card must require git-verifiable diff evidence,
including exact changed-file proof and separate proof of whether any
`src/`, `tests/`, `scripts/`, Gate, mainline, runtime, scheduler, bridge,
product/admission, credential, or push-script path was touched. Push, tag, and
remote anchor remain forbidden while PAT hygiene is unresolved.

## Required Baseline, Ablation, Replay, Leakage, And Evidence Constraints

The strongest passive family must be a superset of accepted Phase 0:
`positional_first_k`, `mean`, `variance`, `correlation`, `pca_subspace`,
`cross_episode`, `supervised`, and `legal_field_membership`.
`obs_only_family_max` must be max over the union.

The fair interventional family must include random legal intervention, greedy
information gain, exhaustive legal query within same budget, Bayesian /
likelihood updater if applicable, lookup-capable interventional imitation,
direct-objective optimizer using legal non-candidate mechanisms, and all six
graph-cache challengers: `graph_lookup`, `transition_table`, `successor_map`,
`count_table`, `fsm_planner`, and `episodic_traversal`.

Future candidate must beat `obs_only_family_max` and strongest fair
interventional baseline under predeclared hashed margins. If the strongest fair
interventional baseline equals, exceeds, or approaches candidate within frozen
margin, Route C closes or downgrades as bounded negative evidence. Both-scored
high is not success.

Future ablations must rerun episodes under real interventions: remove
mechanism-specific state, interventional access, update path, replay state,
candidate memory/cache, self-boundary-specific feature, candidate-only fields,
replace candidate policy with strongest fair baseline policy, shuffle target
labels, and swap twin pairs with passive distribution held constant.

Replay must recompute candidate behavior from serialized state plus legal
observation/action history. Stored-hash comparison alone is insufficient.
Leakage controls must include positive controls for value-level passive leakage,
schema/name/key leakage, order leakage, action/API leakage, serialized-state
leakage, result-json leakage, candidate-only-field leakage, train/heldout
contamination, truth-seed contamination, and unused frozen seed.

Every material score must be produced by callable computation and record
producer_function, source path, recorded inputs, source-pinned input artifact
hash, run_id, seed, context_id, episode_id, aggregation, code_path_hash,
recompute command, and output artifact.

## What This Does Not Prove

No candidate implementation, no mechanism evidence, no Gate pass, no mainline
effect, no runtime/live effect, no agency, autonomy, consciousness, emotion,
stable user benefit, or EGO readiness.

