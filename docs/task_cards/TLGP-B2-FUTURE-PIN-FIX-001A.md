# TLGP-B2-FUTURE-PIN-FIX-001A

> Status: DRAFT, DESIGN-ONLY. Created per external red-team B2 to DECOUPLE the
> verifier fix from `TLGP-001B-R2-PROVENANCE-BANK-001A`. Must NOT touch the banked
> TLGP-001B-R2 run, its source bytes, or its artifacts. Blocked-by: the bank card
> being banked first (so the banked run is immutable before any verifier change).

Task id: TLGP-B2-FUTURE-PIN-FIX-001A

Problem definition: the official-run self-check `delivered_equals_executed` is
tautological — `official_full.py:584-585` sets `delivered = source_hashes()` and
`executed = source_hashes()` (identical live reads), so it can never detect a
delivered≠executed divergence. Fix it for FUTURE runs so `delivered` is read from an
authorization-time pin fixed BEFORE the run (a manifest written at delivery), not
from a second live read.

Current stage/layer: engineering correctness of a future provenance gate. No
mechanism, no learning, no subjectivity.

Hypothesis (falsifiable): reading `delivered` from a pre-run pinned manifest makes
`delivered_equals_executed` able to FAIL. Falsified if a planted post-pin source
edit still reports True.

Required negative control: plant a byte change in a source file AFTER the pin is
written; the gate MUST flip to False. Record as a test (the gate must be fail-able,
not just pass-able).

Baseline / triviality check: the fix must not be a second tautology (e.g. pin
generated by the same call at run time). The pin must be an artifact produced before
the run and independently readable.

Acceptance gate: a unit test demonstrates the gate is True on a genuine match AND
False on a planted post-pin divergence; the pin source is a pre-run artifact, not a
live `source_hashes()` call; no banked TLGP-001B-R2 file is touched.

Claim ceiling: future-run provenance-gate correctness only. Changes NOTHING about
the banked TLGP-001B-R2 run (which remains banked with B2 recorded-OPEN). Asserts
nothing about mechanism, learning, transfer, agency, self, or subjectivity.

Stop condition: STOP if the fix would require editing or re-banking the
TLGP-001B-R2 run source/artifacts, or re-judging the banked run as self-validated —
all out of scope.

Rollback plan: isolated change to a future-run code path + its test; revert =
remove the change and test. No banked artifact involved.

Forbidden changes: NO edit to banked TLGP-001B-R2 source/artifacts; NO retroactive
claim that the original gate worked or that the banked run's delivered==executed was
verified; NO `git add -A`; NO `scripts/push.*`; NO remote.

Governance note: this changes the judge for FUTURE runs ONLY; it must never be used
to upgrade the banked TLGP-001B-R2 run's status.

## Collision Record
Approach A — fix inside the bank commit: REJECTED (would make banked-source ≠
executed-source; this is exactly why the fix was split out).
Approach B — isolated future-run fix + fail-able negative-control test, after the
bank is immutable: selected.
Selected approach: Approach B.
