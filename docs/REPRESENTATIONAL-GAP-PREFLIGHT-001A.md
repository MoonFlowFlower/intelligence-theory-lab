# REPRESENTATIONAL-GAP-PREFLIGHT-001A

## Title

Bounded representational-gap preflight after Gate 1 replay/consolidation
lineage closeout.

## Task Identity

```text
task_id = REPRESENTATIONAL-GAP-PREFLIGHT-001A
status = blocked_until_stage0_freeze_and_anchor_verified
research_layer = mechanism hypothesis layer / representational-gap preflight
parent_closeout = GATE1-REPLAY-CONSOLIDATION-LINEAGE-CLOSEOUT-001
execution_type = design + closed-form / constructive proof + verifier only
mechanism_training_authorized = false
agent_training_authorized = false
same_agent_bridge_authorized = false
gate1_reopen_authorized = false
ego_integration_authorized = false
```

## Purpose

Test whether a bounded environment family contains a dependency not
representable by cheap bounded-order window/count/table/kNN/graph-cache
predictors under a fair access contract, while at least one non-oracle witness
model class can represent it.

## Parent Closeout Inheritance

```text
parent_gate1_package_verdict = failed_graph_cache_collapse
candidate_A_status = closed_current_operationalization
candidate_B_residue_status = closed_shuffled_same_loss_and_function_approximation_collapse
same_agent_bridge_status = blocked_no_clean_local_mechanism
next_allowed_direction = model_class_reset_or_representational_gap_preflight
claim_ceiling = bounded negative mechanism evidence only
```

## Prior Negative Evidence Inheritance

```text
GATE1-REPLAY-CONSOLIDATION-EXEC-TASKCARD-001
verdict = gate1_preflight_failed_graph_cache_collapse
collapse_family = graph_lookup / transition_table / successor_map / count_table / fsm_planner / episodic_traversal

LATENT-MULTISTEP-CONSISTENCY-RESIDUE-001A
relevant_negative_pattern = shuffled_same_loss_and_function_approximation_collapse
```

This task must not imply that Gate 1 is untouched or unexplored.

## Authorization Boundary

Authorized outputs only:

```text
docs/REPRESENTATIONAL-GAP-PREFLIGHT-001A.md
src/representational_gap_preflight/
tests/test_representational_gap_preflight.py
artifacts/representational_gap_001a/
```

Forbidden:

```text
mechanism training
agent training
Gate1 replay/consolidation rerun
Candidate A rerun
Candidate B residue rerun
same-agent bridge drafting or implementation
EGO mainline changes
LLM/RAG/companion/emotion/relationship/user-model modules
consciousness / agency / functional-subject / AGI claims
```

## Stage 0 Freeze and Anchor

Before any verifier run:

```text
task_card_hash = required
K_window_bound = 4
environment_family_definition = frozen
control_family_definitions = frozen
graph_cache_family_definitions = frozen
allowed_access_contract = frozen
witness_model_class_definition = frozen
external_time_anchor = required
first_run_start_time_after_anchor = required
```

If Stage 0 cannot be verified:

```text
verdict = representational_gap_failed_stage0_anchor
claim_ceiling = no representational-gap evidence
```

## Environment Family

```text
environment_family = ParityAliasGrid-v0
ladder_level = L1_partial_observable_causal_environment
K_window_bound = 4
sequence_length = 6
target_rule = XOR over action_bit XOR observation_bit for all tokens
```

The positive witness may update internal state from allowed action/observation
tokens. Hidden parity labels and heldout variant IDs are verifier-only.

## Acceptance Gate

A pass requires Stage 0 anchor pass, K=4 freeze, bounded-window gap for
K=1..4, graph-cache family gap, count/table/frequency/kNN/summary gap,
positive non-oracle witness, control competence, fair access, no lookup
triviality, no control disabling, and claim ceiling compliance.

## Claim Ceiling

At most:

```text
bounded representational-gap preflight evidence
```

This does not prove a new model class works, replay/consolidation works,
Gate1 should reopen, same-agent bridge is authorized, EGO integration is
justified, agency, consciousness, functional-subject evidence, companion
readiness, or AGI.
