# N2-SBMC-FRONTIER-REUSE-SCAN-001A

Status: REUSE-SCAN (route-governance / evidence-planning). Defines the N2/SBMC contamination source +
capable-baseline family from cited external literature. NOT mechanism evidence, NOT N2 pass, NOT an
endorsement that any external project works. Descends from ledger L-011 (N2 pivot).

## layer / claim ceiling
Reuse decision + N2 baseline-family/contamination-source definition only. External projects cited as
threat / baseline / schema DONORS, never as success evidence. No mechanism, N2, agency, or consciousness claim.

## problem
Before (re)building the SBMC env, decide via a bounded, N2-scoped literature scan: (1) does an open,
equal-access, mechanism-vs-baseline consistency probe already exist (drop-in)? (2) which published
poisoning taxonomy to ADOPT AS A PRINCIPLE for the contamination generator? (3) which published defenses
to REIMPLEMENT AS PRINCIPLES for the capable-baseline family N2 must beat?

## method / scope
Bounded web scan (Claude-run, cited), N2/SBMC ONLY. EXPLICITLY EXCLUDES: general memory systems
(MemGPT/Mem0/A-MEM), recall benchmarks (LongMemEval/LoCoMo/STATE-Bench/AMA-Bench/MemoryAgentBench),
proactivity axis, world-model/active axis (AutumnBench — that is N1.5, paused). No external code is run;
adoption is at PRINCIPLE level.

## verification status (honest)
Field existence + the provenance/consistency-evaluation GAP = VERIFIED via web search 2026-07. Individual
project open-source/runnability/specific numbers = TO-VERIFY. Because adoption is principle-level
(belief-corruption contamination; anomaly+provenance defenses), the decision does not depend on any single
unverified paper.

## outputs
Machine-readable matrix at artifacts/N2-SBMC-FRONTIER-REUSE-SCAN-001A/reuse_matrix.json.

## decisions (pre-registered for the N2 env redesign)
- DROP-IN: NONE found in bounded search -> BUILD/ADAPT our own probe.
- CONTAMINATION SOURCE: adopt MINJA-family memory-injection / belief-corruption PRINCIPLE (provenance-
  plausible, metadata-stripped items that corrupt beliefs, not action anomalies).
- BASELINE FAMILY (N2 mechanism must BEAT max of, under equal access + metadata-stripped): per_user_lookup_nn,
  value_aware_embedding_outlier, frequency_marginal, structural_anomaly (MemAudit principle),
  semantic_anomaly+provenance (Agent Memory Guard principle), provenance/hash_chain (Engram principle),
  predict_all, predict_none, majority.
- STANDING IDENTIFIABILITY REQUIREMENT (carried from the scout Red-audit, NOT dissolved by reuse): the
  mechanism must WIN (not tie) the above family in a regime where they do not already win; a tie = baseline
  equivalence = N2 close as engineering-sufficient.

## baseline / ablation / trace-replay
N/A (planning task, no experiment). The matrix + citations are the artifact.

## acceptance gate (for this scan)
Matrix banked with honest verification-status per row; drop-in decision recorded; >=1 contamination-source
principle + >=3 capable-baseline principles selected with citations.

## stop / rollback / forbidden
Stop = scan banked; contamination source + baseline family FROZEN for the N2 env redesign (redesign cites
this id and may not weaken the family without a superseding scan). Rollback = revert these files.
Forbidden: running external code as our evidence; treating any external result as N2 success; expanding
scope to non-N2 axes; adopting a baseline weaker than the cited defenses.

## feeds
N2 SBMC env redesign that supersedes the lookup-solvable scout (5a846d5): mechanism must beat max(baseline
family) under metadata-stripped belief-corruption, equal-access, trace/replay.
