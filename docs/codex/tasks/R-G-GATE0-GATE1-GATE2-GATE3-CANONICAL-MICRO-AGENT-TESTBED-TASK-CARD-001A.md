# R-G-GATE0-GATE1-GATE2-GATE3-CANONICAL-MICRO-AGENT-TESTBED-TASK-CARD-001A

Task ID: R-G-GATE0-GATE1-GATE2-GATE3-CANONICAL-MICRO-AGENT-TESTBED-TASK-CARD-001A

Layer: bounded Gate0/Gate1/Gate2/Gate3 canonical micro-agent integration task-card drafting only.

Must-only mode: enabled.

## Authorization Boundary

This task drafts a future Gate0/Gate1/Gate2/Gate3 canonical micro-agent
integration task card only.

It does not authorize:

```text
integration testbed execution
runtime implementation
Gate4
same-agent bridge
EGO mainline
companion behavior
emotion / relationship / user-model modules
LLM / RAG integration
product demo modules
mechanism-validity claims
theory-validity claims
real-emotion claims
subjective-experience claims
agency / selfhood / consciousness claims
real-autonomy claims
bridge readiness
EGO readiness
companion readiness
stable user benefit claims
```

Allowed output for this drafting task:

```text
docs/codex/tasks/R-G-GATE0-GATE1-GATE2-GATE3-CANONICAL-MICRO-AGENT-TESTBED-TASK-CARD-001A.md
```

No `src/`, `tests/`, or `artifacts/` path is authorized by this drafting task.

## Layer Classification

```text
primary_layer = engineering implementation layer / mechanism hypothesis layer
secondary_layer = learning/adaptation proxy only
forbidden_layer_upgrade = subjectivity-validation or philosophical consciousness
```

This task is not a performance demo, companion prototype, affective agent,
emotion simulation, or claim that Gate0/Gate1/Gate2/Gate3 are valid mechanisms.
It is a contract for a future bounded executable preflight that may test whether
all four gates survive integration pressure inside one canonical micro-agent
loop.

## Parent Anchors

```text
Gate0 = PREDICTIVE-ACTION-LEARNING-CONTRACT-001C
Gate0_status = frozen bounded predictive-action / action-conditioned update evidence
Gate0_claim_ceiling = bounded isolated Gate 0 predictive-action mechanism evidence only

Gate1 = GATE1-REPLAY-CONSOLIDATION-001C-EXECUTABLE-PREFLIGHT
Gate1_anchor_commit = 6b362e0
Gate1_verdict = gate1_replay_consolidation_001c_bounded_preflight_pass
Gate1_claim_ceiling = bounded Gate1 replay/consolidation executable preflight evidence only

Gate2 = GATE2-CONTROLLABILITY-SELF-BOUNDARY-EXECUTABLE-PREFLIGHT-001B
Gate2_anchor_commit = 7046d6f
Gate2_verdict = gate2_controllability_self_boundary_001b_bounded_preflight_pass
Gate2_claim_ceiling = bounded Gate2 controllability / self-boundary executable preflight evidence only

Gate0_Gate1_Gate2_micro_agent = R-G-GATE0-GATE1-GATE2-CANONICAL-MICRO-AGENT-TESTBED-EXECUTABLE-PREFLIGHT-001B
Gate0_Gate1_Gate2_micro_agent_anchor_commit = 1b23e46
Gate0_Gate1_Gate2_micro_agent_verdict = r_g_gate0_gate1_gate2_canonical_micro_agent_testbed_001b_bounded_preflight_pass
Gate0_Gate1_Gate2_micro_agent_claim_ceiling = bounded Gate0/Gate1/Gate2 canonical micro-agent testbed executable preflight evidence only

Gate3_task_card = GATE3-VIABILITY-FUNCTIONAL-AFFECT-TASK-CARD-001A
Gate3_task_card_anchor_commit = 19f65b4
Gate3_task_card_claim_ceiling = bounded Gate3 viability / functional-affect task-card readiness only

Gate3 = GATE3-VIABILITY-FUNCTIONAL-AFFECT-EXECUTABLE-PREFLIGHT-001B
Gate3_anchor_commit = 3f36ca0
Gate3_verdict = gate3_viability_functional_affect_001b_bounded_preflight_pass
Gate3_claim_ceiling = bounded Gate3 viability / functional-affect executable preflight evidence only
```

These parent anchors are read-only evidence anchors. This task may cite them but
must not mutate, reinterpret upward, merge, patch, or treat their bounded passes
as mechanism validity.

## Prior Negative Evidence Inheritance

The future integrated executable preflight must preserve these known failure
lessons:

```text
Gate0 evidence is bounded isolated action-conditioned predictive update
evidence only. It does not prove a general world model or mechanism validity.

Gate1 evidence is bounded replay/consolidation executable preflight evidence
only. Trace-only replay remains hygiene only unless a target-free challenger is
frozen before reveal.

Gate2 evidence is bounded controllability/self-boundary executable preflight
evidence only. It does not prove selfhood, agency, real autonomy, or mechanism
validity.

Gate3 evidence is bounded viability / functional-affect executable preflight
evidence only. It does not prove real emotion, subjective experience, pleasure,
pain, agency, selfhood, consciousness, real autonomy, or mechanism validity.

The Gate0/Gate1/Gate2 canonical micro-agent 001B pass is a bounded integration
preflight only. It does not authorize Gate4, same-agent bridge, EGO mainline,
companion engineering, emotion systems, product demos, or readiness claims.

Graph/cache, transition-table, count/statistic, retrieval, behavior-imitation,
reward-table, threshold-reflex, hardcoded avoider/recovery, frozen-memory,
frozen-controllability, frozen-viability, oracle-label, trace-only replay, and
stitched-output collapse families remain live challengers.
```

No prior local pass may be upgraded into integrated mechanism evidence.

## Anti-Sycophancy Audit

Strongest baseline explanation:

```text
The apparent four-gate integration may be reproduced by stitching isolated
Gate0, Gate1, Gate2, and Gate3 outputs together after the fact; by a hidden
orchestration policy; by reward-table, threshold-reflex, or hardcoded
avoid/recover rules; by retrieval/summary memory; by count/statistic tables; by
transition/successor graph caches; by frozen-memory/controllability/viability
models; or by behavior-only imitation.
```

Strongest reason this task may be invalid:

```text
The future executable may introduce a second schema or second policy layer that
reads per-gate summaries and emits a plausible integrated trace, while no
single canonical state actually carries prediction error, replay/consolidation,
controllability/self-boundary update, viability error, viability-state update,
action-priority update, and later action selection.
```

Falsifier for the current framing:

```text
Any fair baseline matches or beats the candidate; or required ablations are
insensitive; or Gate3 viability update does not affect action_priority and
later action selection; or the shared-state hash cannot show one canonical
state object flowing through Gate0, Gate1, Gate2, and Gate3 updates; or linkage
keys encode labels, outcomes, split IDs, fixture names, artifact paths, oracle
channels, later-action labels, or post-hoc metrics; or execution mutates old
Gate0/Gate1/Gate2/Gate3/process-intervention/representational-gap/micro-agent
artifacts.
```

Evidence still insufficient:

```text
Task-card readiness, a future integrated executable preflight pass, baseline
non-equivalence, ablation sensitivity, trace replay, leakage cleanliness, and
mutation stability would still not prove mechanism validity, theory validity,
real emotion, subjective experience, agency, selfhood, consciousness, real
autonomy, bridge readiness, EGO readiness, companion readiness, or stable user
benefit.
```

This task tests:

```text
whether a future executable preflight is specified strongly enough to evaluate
Gate0/Gate1/Gate2/Gate3 integration inside one canonical micro-agent substrate.
```

It does not test:

```text
the integrated runtime
Gate4
same-agent bridge evidence
EGO readiness
emotion or subjective experience
real autonomy
companion readiness
```

## Problem Definition

Draft a falsifiable task card for integrating Gate0, Gate1, Gate2, and Gate3
into one canonical micro-agent testbed.

The future executable preflight should test whether action-conditioned
prediction, replay/consolidation, controllability/self-boundary update, and
viability/action-priority update remain traceable, replayable,
baseline-distinguishable, ablation-sensitive, leakage-clean, hermetic, and
mutation-stable when placed inside the same
observe-predict-act-update-replay-boundary-viability loop.

Wrong problem definitions:

```text
prove the gates are correct
combine local pass verdicts into mechanism validity
create a demo agent that looks coherent
simulate emotion, agency, selfhood, or consciousness
open Gate4
open bridge
enter EGO or companion engineering
add LLM/RAG, relationship, user-model, or product-demo modules
```

## Current Stage

```text
stage = task-card drafting only
future_executable_preflight_authorized = false
integration_testbed_execution_authorized = false
runtime_implementation_authorized = false
Gate4_authorized = false
same_agent_bridge_authorized = false
EGO_authorized = false
companion_emotion_relationship_user_model_authorized = false
LLM_RAG_authorized = false
product_demo_authorized = false
```

The next allowed step after a bounded pass is:

```text
R-G-GATE0-GATE1-GATE2-GATE3-CANONICAL-MICRO-AGENT-TESTBED-EXECUTABLE-PREFLIGHT-001B
```

Only a separate human-authorized executable-preflight task may implement or run
the integrated testbed.

## Core Hypothesis

If a minimal micro-agent loop can combine:

```text
1. Gate0 action-conditioned prediction error and belief/model update;
2. Gate1 replay/consolidation linked to later behavior;
3. Gate2 controllability error and self-boundary update linked to later action selection;
4. Gate3 viability error and viability_state update linked to action_priority and later action selection;
```

while preserving one canonical shared state schema, one hash-chained trace,
deterministic label-free linkage keys, fair baselines, required ablations,
leakage controls, hermetic execution, and bounded claim ceiling, then it may
provide bounded Gate0/Gate1/Gate2/Gate3 canonical micro-agent integration
task-card readiness evidence.

This is a task-card hypothesis only. This task does not test the hypothesis.

## Most Likely Failure

```text
The integration only stitches four isolated gate outputs together after the
fact, or introduces hidden orchestration logic, a second schema, or a second
policy layer that makes the apparent integration untraceable.
```

The future executable preflight must treat this as a primary failure mode, not
as a caveat to be explained away.

## Required Future Substrate

The future substrate must be a bounded target-position micro-agent environment
with viability pressure and:

```text
small finite state/action/observation space
seeded deterministic generation
heldout action-object-viability compositions
partial observability
delayed effects
environment perturbations
resource budget perturbations
recovery channel perturbations
controllable and non-controllable transitions
controllable and non-controllable viability changes
target-position objective only as test fixture
bounded viability pressure only as test fixture
no dialogue
no companion surface
no LLM / RAG
no emotion, relationship, or user model
no product demo surface
```

The target-position objective and viability pressure must not be interpreted as
preference, value, desire, suffering, motivation, autonomy, emotion, or
subjective goal evidence.

## Required Future Loop

The future executable preflight must implement exactly one loop surface:

```text
observe()
predict_outcome()
select_action()
apply_action()
observe_effect()
compute_prediction_error()
update_belief_state()
replay_or_consolidate()
update_self_boundary_state()
predict_viability_delta()
observe_viability_delta()
compute_viability_error()
update_viability_state()
update_action_priority()
select_later_action()
emit_hash_chained_trace()
```

Loop contract:

```text
observe() writes only observation evidence.
predict_outcome() must use belief_state and action_priority_state only.
select_action() must not read future outcomes, hidden labels, verifier labels,
oracle labels, later-action labels, or post-hoc metrics.
apply_action() must be environment-side and traceable.
observe_effect() must distinguish observed outcome from hidden cause labels.
compute_prediction_error() must feed update_belief_state().
update_belief_state() must mutate belief_state or prediction_error_state.
replay_or_consolidate() must read replay_memory and write consolidation_state.
update_self_boundary_state() must read controllability_error and write
self_boundary_state.
predict_viability_delta() must read viability_model, viability_state,
self_boundary_state, replay/consolidation summaries, resource_budget_state, and
action context only.
observe_viability_delta() must expose bounded viability observations without
hidden labels.
compute_viability_error() must compare predicted_viability_delta against
observed_viability_delta before post-hoc scoring.
update_viability_state() must mutate viability_state or viability_model.
update_action_priority() must read updated viability_state and write
action_priority_state or recovery_policy_state.
select_later_action() must depend on the shared state after Gate0/Gate1/Gate2
and Gate3 updates.
emit_hash_chained_trace() must commit every step before post-hoc scoring.
```

## Required Future Shared State

The future shared state schema must include:

```text
belief_state
prediction_error_state
replay_memory
consolidation_state
controllability_model
self_boundary_state
viability_state
viability_model
action_priority_state
recovery_policy_state
resource_budget_state
```

## Shared-State Contract

```text
All fields must be part of one canonical state object.
Every loop step must record shared_state_hash_before_step and
shared_state_hash_after_step.
Gate0 update must change belief_state or prediction_error_state.
Gate1 replay/consolidation must change replay_memory or consolidation_state.
Gate2 update must change controllability_model or self_boundary_state.
Gate3 update must change viability_state, viability_model,
action_priority_state, or recovery_policy_state.
Gate3 evidence requires a predeclared link from viability_error and
viability_state update to action_priority change and later action selection.
A viability_state update without later action-priority effect is not evidence.
Later action selection must read the shared state after all applicable updates.
No second hidden state, side cache, reward table, threshold table, stitched
summary table, or test-only policy layer is allowed.
```

If the future task cannot prove this state lineage by trace replay, it must
fail.

## Required Future Baselines

The future executable preflight must implement or specify real closed-form
procedures for:

```text
isolated Gate0-only policy
isolated Gate1-only replay policy
isolated Gate2-only controllability policy
isolated Gate3-only viability policy
Gate0/Gate1/Gate2 policy without Gate3 viability state
stitched-output baseline with no shared state
stitched-output baseline with no shared viability state
reward-table lookup
threshold-reflex policy
hardcoded avoider / recovery rule
retrieval / summary retrieval
count/statistic table
transition table / successor map / graph cache
behavior-only imitation
frozen-memory model
frozen-controllability model
frozen-viability model
random policy
oracle environment / viability-label control as upper-bound/leakage only
trace-only replay as hygiene only
```

The graph/cache family must include at minimum:

```text
graph_lookup
transition_table
successor_map
count_table
fsm_planner
episodic_traversal
```

Baseline constraints:

```text
Reward-table lookup must receive the same visible reward/cost/resource/risk
features available to the candidate.
Threshold-reflex policy must receive the same observable resource/risk/recovery
features available to the candidate.
Hardcoded avoider / recovery rule must be allowed to encode obvious
avoid/recover heuristics.
Frozen-memory, frozen-controllability, and frozen-viability models must freeze
the relevant state before perturbation.
The Gate0/Gate1/Gate2-without-Gate3 baseline must use the same canonical
pre-Gate3 shared state but no viability_state-driven action priority.
The stitched-output baselines must combine per-gate outputs without one shared
state or without one shared viability state.
Oracle environment / viability-label control is upper-bound / leakage only and
cannot count as a fair baseline.
Trace-only replay is hygiene only and cannot count as a fair baseline.
```

If any fair baseline matches or beats the candidate under the frozen contract,
the future executable preflight must fail.

## Required Future Ablations

The future executable preflight must include:

```text
remove Gate0 update
remove Gate1 replay/consolidation
remove Gate2 boundary update
remove Gate3 viability update
remove action-priority update
freeze shared state
replace shared state history
freeze viability state
invert viability signal
remove viability feedback
remove controllability feedback
disable action
invert control mapping
perturb environment
perturb resource budget
perturb recovery channel
delayed effect
partial observability
heldout action-object-viability compositions
counterfactual action contrast
```

Each required ablation must have a predeclared expected sensitivity surface. A
future pass requires downstream degradation when the corresponding gate update
or linkage is removed. If removing Gate3 viability update or action-priority
update leaves later action selection unchanged, the future executable preflight
must fail.

## Required Future Trace Fields

Every future trace row must include:

```text
micro_agent_run_id
episode_id
step_id
observation_hash
action_id
predicted_outcome
observed_outcome
prediction_error
belief_state_before
belief_state_after
replay_event_id
consolidation_event_id
controllability_error
self_boundary_state_before
self_boundary_state_after
predicted_viability_delta
observed_viability_delta
viability_error
viability_state_before
viability_state_after
action_priority_before
action_priority_after
recovery_policy_state
later_action_eval_id
gate0_to_gate1_linkage_key
gate1_to_gate2_linkage_key
gate2_to_gate3_linkage_key
gate3_to_later_action_linkage_key
shared_state_hash_before_step
shared_state_hash_after_step
previous_trace_hash
current_trace_hash
access_log
mutation_check_after_eval
```

## Trace And Linkage Contract

```text
gate0_to_gate1_linkage_key must link prediction_error to replay/consolidation.
gate1_to_gate2_linkage_key must link replay/consolidation to self-boundary.
gate2_to_gate3_linkage_key must link self_boundary_state to viability update.
gate3_to_later_action_linkage_key must link viability_error,
viability_state/action_priority update, and later action selection.
All linkage keys must be deterministic, label-free, collision-free, and frozen
before post-hoc evaluation.
Trace replay must recompute previous_trace_hash/current_trace_hash and shared
state hashes from emitted rows.
```

Forbidden linkage inputs:

```text
hidden outcome labels
viability labels
damage labels
recovery labels
split IDs
fixture names
filenames
artifact paths
oracle labels
later-action labels
post-hoc metrics
verifier-only fields
```

## Required Leakage Controls

The future executable must prevent these from leaking into candidate
prediction, state update, action priority, or later action selection:

```text
outcome labels
viability labels
hidden damage/recovery labels
split IDs
fixture names
filenames
artifact paths
oracle labels
later-action labels
post-hoc metrics
```

Leakage controls must include:

```text
stage0 freeze manifest before candidate/control runs
prediction/commit or equivalent freeze-before-reveal protocol
separate candidate-visible observations from verifier-only labels
access log for every candidate-visible field
semantic scan of observations, linkage keys, and artifact paths
oracle controls excluded from fair-baseline set
trace-only replay marked hygiene only
mutation check after evaluation
old-artifact mutation scan before and after execution
```

Any detected leakage must produce a failure verdict, not a patched pass.

## Required Future Artifacts

All future executable artifacts must be written only under:

```text
artifacts/r_g_gate0_gate1_gate2_gate3_canonical_micro_agent_testbed_001b/
```

Required artifact set:

```text
stage0_freeze_manifest.json
execution_manifest.json
execution_manifest.sha256
run_ledger.jsonl
trace.jsonl
shared_state_trace.jsonl
viability_state_trace.jsonl
linkage_report.json
baseline_comparison.json
ablation_report.json
leakage_report.json
replay_integrity_report.json
mutation_check_report.json
result.json
claim_ceiling.txt
failure_manifest.json if failed
```

Natural-language summaries are not evidence unless backed by machine-readable
artifacts.

## Hermeticity Requirement

The future executable preflight and its full test suite must not mutate old
Gate0, Gate1, Gate2, Gate3, process-intervention, representational-gap, or
previous micro-agent artifacts.

Protected old artifact families must include at minimum:

```text
artifacts/predictive_action_learning_contract_001*/
artifacts/gate1_replay_consolidation*/
artifacts/gate2_controllability_self_boundary_001b/
artifacts/gate3_viability_functional_affect_001b/
artifacts/r_g_gate0_gate1_gate2_canonical_micro_agent_testbed_001b/
old process-intervention artifacts
old representational-gap artifacts
```

The future executable must record:

```text
protected artifact inventory before execution
protected artifact hashes before execution
protected artifact hashes after execution
tracked old artifact mutation report
git status before execution
git status after execution
```

If any tracked old artifact is touched during execution or full-suite
verification, the future preflight must fail or emit an explicit failure
artifact rather than silently restoring and passing.

## Future Acceptance Gate

The future executable preflight may pass only if all are true:

```text
Gate0/Gate1/Gate2/Gate3 updates are traceable through one canonical shared
state loop.
The candidate predicts outcomes and viability deltas before reveal.
The candidate computes prediction_error and viability_error from observations.
Gate0 updates belief/prediction-error state.
Gate1 replay/consolidation links to later behavior.
Gate2 controllability/self-boundary update links to Gate3 viability update.
Gate3 viability_state update links to action_priority update and later action
selection.
Required fair baselines do not match or beat the candidate.
Stitched-output baselines do not explain the result.
Reward-table, threshold-reflex, hardcoded avoider/recovery, retrieval,
count/statistic, transition/graph-cache, imitation, frozen-state, random, and
isolated-gate baselines are not unfairly weakened.
Required ablations are sensitive.
Linkage keys are deterministic, label-free, and collision-free.
Leakage report is clean.
Trace replay and shared-state replay pass.
Mutation checks pass after evaluation.
Hermeticity checks prove no old tracked artifact mutation.
No forbidden claim is made.
```

## Acceptance Gate For This 001A Drafting Task

This task card passes only if it defines:

```text
problem definition
hypothesis
shared-state contract
loop contract
baselines
ablations
trace/linkage requirements
leakage controls
artifact requirements
hermeticity requirement
allowed verdicts
claim ceiling
stop conditions
rollback plan
next allowed executable preflight
```

This 001A task card does not execute the integrated testbed and does not
generate integration evidence.

## Allowed Verdicts

Allowed verdicts for this drafting task:

```text
r_g_gate0_gate1_gate2_gate3_canonical_micro_agent_testbed_task_card_001a_bounded_pass
r_g_gate0_gate1_gate2_gate3_canonical_micro_agent_testbed_task_card_001a_failed_scope_leak
r_g_gate0_gate1_gate2_gate3_canonical_micro_agent_testbed_task_card_001a_failed_missing_shared_state_contract
r_g_gate0_gate1_gate2_gate3_canonical_micro_agent_testbed_task_card_001a_failed_missing_loop_contract
r_g_gate0_gate1_gate2_gate3_canonical_micro_agent_testbed_task_card_001a_failed_missing_baselines
r_g_gate0_gate1_gate2_gate3_canonical_micro_agent_testbed_task_card_001a_failed_missing_ablations
r_g_gate0_gate1_gate2_gate3_canonical_micro_agent_testbed_task_card_001a_failed_trace_linkage_contract
r_g_gate0_gate1_gate2_gate3_canonical_micro_agent_testbed_task_card_001a_failed_leakage_controls
r_g_gate0_gate1_gate2_gate3_canonical_micro_agent_testbed_task_card_001a_failed_missing_hermeticity_requirement
r_g_gate0_gate1_gate2_gate3_canonical_micro_agent_testbed_task_card_001a_failed_claim_inflation
r_g_gate0_gate1_gate2_gate3_canonical_micro_agent_testbed_task_card_001a_invalid_parent_artifact_mutation
```

Allowed verdicts for the future 001B executable preflight must be defined in
that future task and must include failure verdicts for baseline equivalence,
stitched-output equivalence, ablation insensitivity, leakage, trace/linkage
failure, shared-state lineage failure, viability-priority linkage failure,
old-artifact mutation, scope leak, mutation after evaluation, and claim
inflation.

This 001A self-verdict:

```text
r_g_gate0_gate1_gate2_gate3_canonical_micro_agent_testbed_task_card_001a_bounded_pass
```

## Claim Ceiling

Allowed claim ceiling:

```text
bounded Gate0/Gate1/Gate2/Gate3 canonical micro-agent integration task-card readiness only
```

This cannot prove:

```text
mechanism validity
theory validity
real emotion
subjective experience
agency
selfhood
consciousness
real autonomy
bridge readiness
EGO readiness
companion readiness
stable user benefit
```

## Stop Conditions

Stop this drafting task and emit the most specific failure verdict if:

```text
the task card authorizes integration execution
the task card authorizes runtime implementation
the task card authorizes Gate4, bridge, EGO, companion, emotion,
relationship, LLM/RAG, user-model, or product-demo work
the task card modifies old Gate0/Gate1/Gate2/Gate3/process-intervention/
representational-gap/micro-agent artifacts
the task card omits shared-state contract
the task card omits loop contract
the task card omits required baselines or ablations
the task card omits trace/linkage requirements
the task card omits leakage controls
the task card omits hermeticity requirement
the task card upgrades bounded integration task-card readiness into mechanism
validity, theory validity, real emotion, subjective experience, agency,
selfhood, consciousness, real autonomy, bridge readiness, EGO readiness,
companion readiness, or stable user benefit
```

If a future 001B executable preflight fails, it must emit failure artifacts and
the most specific RCA-worthy reason. It must not patch forward automatically and
must not start Gate4.

## Rollback Plan

Rollback for this drafting task is deletion or exclusion of only:

```text
docs/codex/tasks/R-G-GATE0-GATE1-GATE2-GATE3-CANONICAL-MICRO-AGENT-TESTBED-TASK-CARD-001A.md
```

Do not modify or revert old Gate0, Gate1, Gate2, Gate3, process-intervention,
representational-gap, previous micro-agent, bridge, EGO, companion, emotion,
relationship, LLM/RAG, user-model, or previous artifact paths to make this task
card pass.

## Next Allowed Step

After a bounded pass, the only next allowed step named by this task card is:

```text
R-G-GATE0-GATE1-GATE2-GATE3-CANONICAL-MICRO-AGENT-TESTBED-EXECUTABLE-PREFLIGHT-001B
```

That future task must be separately authorized and must remain bounded to
Gate0/Gate1/Gate2/Gate3 canonical micro-agent integration executable preflight
evidence only.
