# K0-FOUNDATION-ACCEPTANCE-TRANSITION-001A

## Task boundary

- **Repo/CWD:** `D:\Project\AIProject\MyProject\intelligence-theory-lab`
- **Auto-Remote-Anchor:** forbidden
- **Current layer:** Layer 2 engineering / route-governance evidence hygiene
- **Mainline target:** ITL canonical K0 route packet only; EGO mainline integration is none
- **Enabled-state requirement:** runtime stays disabled; all route and six child authorizations end false
- **Real-trigger evidence requirement:** callable cross-repo Git-object/artifact readback plus focused tests; no runtime, mechanism, learning, scoring, or experiment trigger
- **Claim ceiling:** local cross-repo provenance-backed Foundation engineering acceptance and route authorization consumption only

## Problem definition and real objective

Write the independently reviewed Foundation official bounded engineering acceptance into the ITL canonical route, consume the Foundation implementation authorization, keep H0, K0-R, H1, Freeze, and Formal false, and enter a non-terminal operator-decision boundary. Do not synchronize Ego `PROGRAM_STATE`, make an operator replace/close decision, or authorize runtime/mainline work.

## Current stage

The K0 parent route is `READY_TO_IMPLEMENT` at phase `CODE_FIRST_H0_PREBANK_PRECONDITION_FAILED_SCIENCE_BRANCH_CLOSED`. Foundation alone is implementation-authorized. H0 is closed at its precondition boundary and remains untested. This task records the Foundation engineering acceptance and consumes its authorization without selecting a successor route.

## Bounded audit and collision record

### Framing audit

- **Hypothesis:** the reviewed, immutable Ego Foundation artifact can be admitted as bounded engineering evidence by independently recomputing its Git-object provenance and exact contents, then consuming the corresponding ITL authorization.
- **Strongest baseline:** a hand-edited, internally consistent route packet or static copy of the executor receipt can look equivalent without independently verifying the sibling repository objects.
- **Strongest invalidity reason:** frozen literals could masquerade as cross-repo verification, or the route could silently convert bounded engineering acceptance into mechanism/theory or successor-route authority.
- **Falsifier:** any commit lineage, tree, blob, SHA-256, manifest member, official result field, focused test, H0 historical recomputation, authorization, event, or ledger invariant differs from the frozen contract.
- **Still insufficient:** a clean JSON packet, passing unit tests alone, the official producer's self-report alone, or behavioral resemblance does not establish mechanism validity or mainline effect.
- **Task type:** engineering provenance and route-governance enforcement, not a mechanism test.
- **Anti-hardcoding / leakage checks:** independent raw Git-object reads; exact manifest recomputation; exact result-field checks; mutation tests; no official evidence rerun; no second authority copy in `closure.json`; historical H0 recomputation remains live.
- **Stop condition:** any Step-0 mismatch, additional dirty path, unavailable Ego object store, provenance mismatch, failed test/check, unauthorized changed/staged path, or lost H0 recomputation stops execution fail-closed.
- **Rollback plan:** before commit, restore only task-owned Phase-B edits to the last committed bytes without touching user files; if Phase B fails after Phase A is committed, preserve Phase A, create no partial Phase-B commit, and stop. No reset, rebase, amend, stash, checkout-discard, or history rewrite.
- **Acceptance signal:** exact post-state plus callable provenance pass, full mutation coverage, route validation/status/dashboard pass, exact staged set, and committed Git-object readback.

### Candidate A — keep `READY_TO_IMPLEMENT` but set every authorization false

- Evidence: state text changed.
- Strongest cheap baseline: hand-edited internally consistent JSON.
- Leakage / hard-coding risk: semantic lie; READY without an executable target.
- Smallest falsifying test: validator accepts a stale implement action or true child.
- Expected failure mode: Foundation can appear re-runnable.
- Decision: rejected.

### Candidate B — directly `ADJUDICATED`, `REDESIGN_REQUIRED`, or select replace/close

- Evidence: decisive-looking route.
- Strongest cheap baseline: governance decision without operator choice.
- Leakage / hard-coding risk: premature closure or implicit replacement authorization.
- Smallest falsifying test: route chooses a successor before operator decision.
- Expected failure mode: claim/authority inflation.
- Decision: rejected.

### Candidate C — `CLOSURE_REVIEW_REQUIRED` + `GOVERNANCE_STOP`

- Evidence: callable external-object pins, consumed authorizations, explicit component statuses, append-only event and ledger.
- Strongest cheap baseline: static copy of the executor receipt.
- Leakage / hard-coding risk: frozen literals masquerading as verification.
- Smallest falsifying test: any commit/blob/SHA/result/test mismatch or any child survives true.
- Expected failure mode: missing sibling Git objects or provenance drift fails closed.
- Decision: selected. This is not a terminal science closure.

## Frozen preflight pins

### ITL

- Branch: `codex/meta-theory-scaffold`
- HEAD: `d519a84fb0891bca17edd4e605a0e3cb066ecc7d`
- Ahead/behind: `23/0`
- Tracked worktree/index: clean
- Only untracked paths, frozen and non-authoritative:
  - `docs/codex/tasks/FORK-A-IIO-MCI-INSTRUMENT-VALIDITY-AUDIT-001A.md`
  - `docs/research/FORK-A-IIO-MCI-INSTRUMENT-VALIDITY-AUDIT-001A.md`
- Authority commit: `07c0f1f85a3c855511ff1610ec9629f8e94e89b1`
- Route blob at authority and current HEAD: `5afe5060fa67caf7aaebfc92ba677d6fa9ee16e3`
- Route SHA-256: `ba97b56a971c3325b2862b00ff564f149df46dba178af61a780632f456c69c37`
- Required route state: `READY_TO_IMPLEMENT`
- Required phase: `CODE_FIRST_H0_PREBANK_PRECONDITION_FAILED_SCIENCE_BRANCH_CLOSED`
- `foundation_implementation=true`; Foundation child true; H0/K0-R/H1/Freeze/Formal children false
- `instrument_validity=NOT_TESTED`; `mechanism_evidence=NOT_TESTED`

The two frozen untracked user files must not be read as authority, modified, staged, deleted, stashed, or added to a validator authorization list.

### Ego

- Repo: sibling `Ego` repository discovered from the ITL repository parent, not an absolute validator path
- Branch: `main`
- HEAD / test-fix commit: `a95aa3f2d94bc4cb36f83e29e34a2c69da28497a`
- Parent / artifact commit: `8318bb33212f79e1e0a9cea15cad7b9cd176a0f5`
- Producer commit: `7dd6619c0c643344984d49383c61190bbab04f21`
- Worktree/index: clean
- Ahead/behind: `11/0`
- Artifact tree: `907457e7d3028ba5437cf0e7730ec068a21cbf6b`
- Result blob: `a8b3237afc40a1f56df3906870ff94c5db9c10ff`
- Result SHA-256: `834b4764514062f8937488fd4b89684b5ae684e9522d67f440ed3c077f077067`
- Test-fix blob: `922cea09a2c2b3c64be70e387a79f1027985fcc3`
- Test-fix SHA-256: `062c0f6de20dde5566c56938c516f8900b936a689c82b1fdc657816b8b65393a`

### Required baselines before editing

- ITL route tests: 158 passed.
- Read-only `build_validation_report()`: pass, 0 errors, 0 warnings.
- Ego focused suite: 32 passed.
- Canonical artifact snapshot unchanged across the focused suite.
- No official evidence rerun.

Do not run `routectl validate` during preflight because it rewrites `validation_report.json`; use the callable in-memory builder. Any mismatch or additional dirty path stops execution.

## Exact post-state

- `current_state=CLOSURE_REVIEW_REQUIRED`
- `phase=FOUNDATION_ENGINEERING_ACCEPTED_H0_NOT_TESTED_OPERATOR_DECISION_REQUIRED`
- `closure_type=GOVERNANCE_STOP`
- `implementation_authorized=false`
- `authorized_implementation_targets=[]`
- Every route authorization false
- All six child authorizations false
- Allowed actions exactly, in order:
  1. `sync_EGO_foundation_acceptance_route_views_under_separate_task`
  2. `operator_route_decision_after_EGO_sync`
  3. `run_route_state_machine_validation`
- H0 remains `PRECONDITION_FAILED_SCIENCE_BRANCH_CLOSED`
- `instrument_validity=NOT_TESTED`
- `mechanism_evidence=NOT_TESTED`

The closure packet must state Foundation engineering evidence `BANKED_ACCEPTED_BOUNDED`; H0 `NOT_TESTED`; K0-R/H1/Freeze/Formal `BLOCKED_NOT_TESTED`; `mechanism_evidence_authorized=false`; `theory_pressure_authorized=false`; no runtime/mainline authorization; and replace versus close remains an operator decision.

Store the complete Foundation acceptance pin only in the K0 state packet. `closure.json` references that canonical pin rather than becoming a second independent authority. `program_state.json` mirrors posture and authorization only.

## Computed-evidence provenance gate

The callable cross-repo validator must:

1. Discover the sibling Ego repo without a hard-coded absolute validator path.
2. Read raw Git-object bytes, not PowerShell text-pipeline output.
3. Verify direct lineage `7dd6619c -> 8318bb33 -> a95aa3f2`.
4. Verify the artifact commit changes only the 14 artifact files.
5. Recompute the 14-entry path/mode/blob/SHA-256 manifest.
6. Verify the exact artifact tree and result blob/SHA-256.
7. Parse `result.json` and require 21/21 computed gates, `official_evidence_bank=true`, `verdict=foundation_engineering_pass`, `enabled=false`, `mainline_connected=false`, and `runtime_authority=none`.
8. Verify the test-fix commit changes only `tests/test_ego_k0_foundation.py` and preserves the artifact tree.
9. Record `producer_function`, input artifacts, `run_id`, aggregation rule, and `code_path_hash`.
10. Fail closed if the Ego repo or object store is unavailable.
11. Never invoke the official evidence producer.

Moving out of `READY_TO_IMPLEMENT` must not make the existing H0 historical-pin and precondition-closure recomputation not applicable. Preserve those live governance checks.

## Mutation / ablation requirements

Mutation tests must reject at least:

- wrong producer/artifact/test-fix commit or lineage;
- wrong tree/result/test blob or SHA-256;
- wrong artifact count/path/blob/SHA-256;
- any official result field or computed gate changed;
- Foundation authorization remaining true;
- any H0/K0-R/H1/Freeze/Formal child true;
- nonempty implementation target list;
- wrong state/phase/closure type;
- mechanism/theory authorization true;
- missing or duplicate `L-027` or route event;
- loss of existing H0 precondition recomputation;
- an action that starts K0-R or reopens/reruns old H0.

These are governance negative controls, not mechanism ablations. Trace/replay requirement is exact Git-object recomputation and append-only event/ledger readback; no experiment replay is authorized.

## Append-only requirements

- Append exactly one `L-027` transition decision recording bounded Foundation acceptance, consumed Foundation authorization, H0 `NOT_TESTED`, and operator decision pending.
- Append exactly one route event named `foundation_engineering_accepted_operator_decision_required`.
- Preserve every prior ledger and event byte.

## Phase A — bank card only

Add only this file and commit exactly:

`docs: bank K0 Foundation acceptance transition`

Stage only this literal path. `git add -A` is forbidden.

## Phase B — route enforcement allowlist

Only these ten paths may change:

1. `docs/research/FSP-STAGE-LEDGER.md`
2. `src/route_state_machine_001a/state_machine.py`
3. `src/route_state_machine_001a/validator.py`
4. `tests/route_state_machine_001a/test_validator.py`
5. `artifacts/ROUTE-STATE-MACHINE-001A/routes/K0-DUAL-TRACK-SUPERSESSION-001A/state.json`
6. `artifacts/ROUTE-STATE-MACHINE-001A/routes/K0-DUAL-TRACK-SUPERSESSION-001A/events.jsonl`
7. `artifacts/ROUTE-STATE-MACHINE-001A/routes/K0-DUAL-TRACK-SUPERSESSION-001A/closure.json`
8. `artifacts/ROUTE-STATE-MACHINE-001A/program_state.json`
9. `artifacts/ROUTE-STATE-MACHINE-001A/STATUS.md`
10. `artifacts/ROUTE-STATE-MACHINE-001A/validation_report.json`

Existing schemas already support `CLOSURE_REVIEW_REQUIRED` and `GOVERNANCE_STOP`; schema edits are forbidden. Commit exactly:

`governance: record K0 Foundation acceptance boundary`

## Acceptance gate

Before the Phase-B commit:

- all route tests pass;
- `routectl validate` writes a computed pass report with 0 errors and 0 warnings;
- `routectl status` and `routectl dashboard` pass;
- working and cached `git diff --check` pass;
- staged set is exactly the ten Phase-B paths;
- the two unrelated untracked user files remain untouched and unstaged;
- Ego artifact 14-path/blob/SHA-256 snapshot is unchanged.

After the commit:

- use read-only `build_validation_report()` plus status/dashboard;
- do not rerun `routectl validate` after commit if it would dirty the generated report;
- tracked worktree/index are clean;
- only the two frozen untracked user paths remain;
- exact Git-object readback confirms both commits and every route file.

## Forbidden changes and actions

No push, tag, remote anchor, Ego edit, `PROGRAM_STATE_UNIFIED` synchronization, H0 replacement, K0-R, H1, Freeze, Formal, experiment, scoring, schema edit, official evidence rerun, or use of either frozen untracked user file as authority. If Phase B fails after Phase A was committed, preserve the Phase-A card commit, leave no partial Phase-B commit, report the blocker, and stop.

## Final claim ceiling

At most: local cross-repo provenance-backed Foundation engineering acceptance and route authorization consumption. This does not prove mechanism validity, instrument validity, theory pressure, learning, replay/consolidation, agency, autonomy, subjectivity, consciousness, EGO readiness, runtime/mainline effect, companion readiness, production readiness, or user benefit.
