# GATE1-REPLACEMENT-PREFLIGHT-00XA

Status: draft implementation card only. This document is not self-authorizing.

Auto-Remote-Anchor: forbidden.

## 1. Problem Definition

Draft a bounded future implementation task for a candidate-free Gate1 replacement
preflight. The future preflight may only evaluate whether an independently
frozen surface specification has enough legal, non-trivial, baseline-resistant,
fail-able signal to justify a later candidate-card draft.

This card exists because the R1 re-audit accepted only implementation-card
drafting:

- `r1_accepted_for_implementation_card_drafting`;
- implementation may proceed now: false;
- only implementation-card drafting authorized: true;
- remaining blocking issue: none.

The accepted R1 boundary that this card must preserve is:

- `R1.a`: metric-degeneracy controls are accepted as repaired;
- `R1.b`: positive partial-inferability requirements are accepted as repaired;
- `R1.c`: surface-spec authorship and immutability are accepted as repaired.

This card does not authorize implementation by itself. Before any future
implementation begins, this card must receive hostile audit acceptance and the
future implementer must confirm that an independently frozen surface
specification pack already exists.

## 2. Stage / Layer

Engineering-governance / Gate1 replacement preflight implementation-card draft.

No mechanism, learning, agency, subjectivity, consciousness, runtime, or
mainline claim is made by this card.

## 3. Mainline Target: None

The future preflight has no EGO mainline, runtime, bridge, admission, scheduler,
UI, LLM, AIRI, deployment, companion, Gate4/Gate5, or Route C target.

Any future implementation must remain isolated to the explicit paths named in
this card. It must not enable any live path.

## 4. Enabled-State Requirement

This card enables no executable path and does not authorize a Gate1 run. The
only next action authorized by this draft is independent hostile audit of this
implementation card.

If this card is later accepted and implementation is separately authorized, the
future preflight must still fail closed unless all required inputs, including an
independently frozen surface specification pack, are present before execution.

## 5. Real-Trigger Evidence Requirement

The future implementation, if separately authorized, must read and record
authoritative hashes for the card-drafting and R1 re-audit boundary:

- `artifacts/gate1_replacement_preflight_00xa_task_card_r1/draft_next_task_card_r1.md`
- `artifacts/gate1_replacement_preflight_00xa_task_card_r1/final_report.md`
- `artifacts/CLAUDE-INDEPENDENT-GATE1-REPLACEMENT-PREFLIGHT-00XA-TASK-CARD-R1-REAUDIT-001A/audit_report.md`
- `artifacts/CLAUDE-INDEPENDENT-GATE1-REPLACEMENT-PREFLIGHT-00XA-TASK-CARD-R1-REAUDIT-001A/audit_result.json`

It must also preserve the prior negative-evidence context from these read-only
inputs:

- `artifacts/gate1_replacement_readback_or_preflight_selection_001a/selected_verdict.json`
- `artifacts/gate1_replacement_readback_or_preflight_selection_001a/gate1_failure_readback.json`
- `artifacts/gate1_replacement_readback_or_preflight_selection_001a/gate_dependency_readback.json`
- `docs/research/GATE1-FAILED-GRAPH-CACHE-RECONCILIATION-001A.md`
- `artifacts/post_freeze_gate0_3_sequential_repair_queue_001a_gate1_failed_graph_cache_reconciliation/baseline_comparison.json`
- `docs/codex/contracts/BASELINE-IMMUNITY-ADMISSION-STANDARD-001A.md`
- `docs/codex/contracts/BASELINE-IMMUNITY-ADMISSION-STANDARD-001A.registry.json`
- `artifacts/CLAUDE-INDEPENDENT-GATE-EVIDENCE-PROVENANCE-VERIFIER-001A-HOSTILE-AUDIT-001A/audit_result.json`

The future implementation must record that old Gate1 positive inheritance is
closed by graph-cache reconciliation failure, and that candidate-free Route C
false-positive evidence showed single-sided or size-saturating metrics can admit
empty mechanism evidence.

## 6. Frozen Surface Specification Input Requirement

The frozen surface specification pack is a required input, not a deliverable.
It must exist before any future implementation begins.

The future implementation must record:

- spec path;
- SHA256;
- author/source;
- freeze timestamp or decision-log pointer;
- canonical readback channel;
- metric definition;
- equivalence band;
- target definition;
- state schema;
- observation schema;
- legal action/query schema;
- fixed action/query budget;
- generator provenance;
- train/validation/heldout/test seed policy, if applicable;
- source pins and readback paths;
- legal channels;
- illegal channels;
- negative-evidence pointers;
- baseline panel registry mapping each baseline to callable producer
  requirements.

If no independently frozen surface spec can be found, the future implementation
must stop with `blocked_missing_candidate_free_surface_spec`.

## 7. Surface-Spec Authorship / Immutability Rule

The future preflight implementer must not create, author, mutate, normalize,
amend, or freeze the surface specification pack.

The spec must be authored and frozen by a separate authority, or exist as a
pre-existing artifact explicitly accepted before implementation begins.

If the future preflight implementer authors, mutates, amends, normalizes, or
freezes the spec during implementation, the final verdict must be
`blocked_candidate_authored_or_mutated_surface_spec`.

## 8. Candidate-Forbidden Condition

No candidate may exist on the future preflight path.

Forbidden:

- candidate code;
- candidate outputs;
- candidate-authored target definitions;
- candidate-authored truth;
- candidate implementation paths;
- mechanism-specific oracle functions;
- any candidate call used to define the metric, surface, target, labels,
  baseline inputs, or oracle.

Stop if a candidate is required to define the target, generate data, author
truth, compute the metric, or supply the oracle.

## 9. Target Variable

`gate1_replacement_surface_pre_candidate_admissibility`

This is a pre-candidate admissibility variable. It is not a mechanism-success
variable and not Gate1 pass evidence. It asks only whether an independently
frozen proposed Gate1 surface has enough legal, non-trivial,
baseline-resistant signal to justify a later candidate-card draft.

## 10. Allowed Verdicts

The future implementation may emit only one of:

- `admissible_for_candidate_card_drafting_only`
- `rejected_metric_degenerate`
- `rejected_no_fair_signal`
- `rejected_answer_key_oracle_gap`
- `rejected_trivially_decodable`
- `rejected_baseline_saturated`
- `blocked_missing_partial_inferability_demonstration`
- `blocked_pending_canonical_readback`
- `blocked_missing_candidate_free_surface_spec`
- `blocked_candidate_authored_or_mutated_surface_spec`

No other pass-shaped verdict is allowed.

## 11. Observable State

The only allowed observed input is the independently frozen surface
specification pack and the read-only negative-evidence/source-pin inputs named
in this card.

The surface spec must define:

- target definition;
- state schema;
- observation schema;
- legal action/query schema;
- fixed action/query budget;
- balanced metric definition and equivalence band;
- generator provenance;
- train/validation/heldout/test seed policy, if applicable;
- source pins and readback paths;
- declared legal channels;
- declared illegal channels;
- negative-evidence pointers;
- baseline panel registry mapping each baseline to callable producer
  requirements.

The preflight must not observe candidate outputs.

## 12. Fixed Action / Query Budget

The future implementation must record a fixed action/query budget before running
candidate-free probes.

Budget rules:

- no candidate calls;
- no hidden answer-key access for fair baselines;
- no post-hoc threshold or metric tuning;
- identical query/action budget for all fair active/query baselines;
- exhaustive legal query allowed only as a challenger and decisive blocker if it
  saturates;
- if budget cannot be fixed before execution, verdict is
  `blocked_pending_canonical_readback`.

## 13. Hypothesis

A Gate1 replacement surface is admissible for later candidate-card drafting only
if candidate-free computed controls show that the target is neither trivially
decodable, metric-degenerate, fair-baseline saturated, graph-cache equivalent,
lookup equivalent, nor blocked by canonical readback.

This card does not test that hypothesis.

## 14. Strongest Baseline

The strongest baseline explanation is that any apparent Gate1 replacement signal
is reproduced by a graph/cache/lookup/replay/control family, a direct objective
optimizer, or a degenerate metric, not by a mechanism-specific effect.

The future implementation must treat baseline equivalence or saturation as
negative evidence, not as a wording problem to repair.

## 15. Full Baseline Panel

The future implementation must invoke every applicable callable baseline and
consume the result in final verdict derivation.

Required panel:

- trivial predictors: `predict_all`, `predict_none`, `constant_k_sweep`,
  `random`, `majority`;
- passive baselines: `observation_only`, `value_decoder_mean`,
  `value_decoder_variance`, `value_decoder_correlation`, `value_decoder_pca`,
  `nearest_neighbor_passive`;
- active/query baselines where actions or queries exist:
  `exhaustive_legal_query`, `greedy_uncertainty_query_under_budget`;
- graph-cache challengers: `graph_lookup`, `transition_table`,
  `successor_map`, `count_table`, `fsm_planner`, `episodic_traversal`;
- lookup imitation: `trace_only_replay`, `ngram_trace_lookup`,
  `full_bundle_decoder`, `serialized_state_decoder`, `belief_table`,
  `pair_count_table`;
- direct objective optimizers where applicable: `discounted_wls`,
  `least_squares`, `convex_solver`;
- real fitted amortized learner where adaptation or learning is claimed:
  `trained_legal_channel_learner`;
- strongest task-specific classical baseline.

If any applicable panel member is missing, uninvoked, not independent, weaker
than a known fair challenger, or not consumed by final verdict derivation, the
future implementation must not emit `admissible_for_candidate_card_drafting_only`.

## 16. Balanced Metric And Degeneracy Controls

Any set/classification/recovery metric must report one of:

- precision and recall jointly;
- F-beta with stated beta;
- explicit cost-weighted utility with false-positive and false-negative costs;
- another explicitly balanced metric that prevents single-sided saturation.

Admissibility is forbidden from:

- recall-only;
- precision-only;
- coverage-only;
- specificity-only;
- abstention-only;
- size-only score.

The future implementation must compute callable, provenance-recorded controls:

- `predict_all`;
- `predict_none`;
- `constant_k_sweep`;
- `random`;
- `majority`;
- size-only sweep from `0..N`.

Each control row must record producer/provenance fields and
`consumed_by_final_verdict=true`.

If any degenerate predictor reaches the ceiling band, final verdict must be
`rejected_metric_degenerate`.

No by-reference escape is allowed. Citing
`BASELINE-IMMUNITY-ADMISSION-STANDARD-001A` is not sufficient.

## 17. Size-Only Sweep `0..N`

The future implementation must compute an explicit size-only sweep:

- vary prediction-set size from `0..N`;
- hold content policy fixed or matched;
- record score as a function of output size;
- reject if score climbs to the ceiling band by size alone.

This sweep must be consumed by final verdict derivation. If size alone can drive
the score to the ceiling band, final verdict must be
`rejected_metric_degenerate`.

## 18. Positive Partial-Inferability Demonstration

Admissibility cannot be framed only as "baselines did not saturate." The future
preflight must provide positive, fail-able evidence that:

- passive observation does not trivially decode the target;
- the target is not independent of all fair observable channels;
- residual uncertainty remains under passive observation;
- the legal channel or intervention class can reduce residual uncertainty in
  principle.

Required controls:

- passive-only attacker family;
- visible-channel decodability check;
- random or matched-marginal baseline;
- non-reading oracle or null oracle negative control;
- fail-closed result if the fair channel has no signal.

If this demonstration is absent, final verdict must be
`blocked_missing_partial_inferability_demonstration` or
`rejected_no_fair_signal`.

## 19. Budget-Faithful Visible-Channel Oracle Rule

If an oracle is used to argue headroom, it must be:

- budget-faithful;
- visible-channel only;
- legal-access only;
- not reading a hidden answer key;
- not reading planted truth;
- not presupposing the mechanism.

It must clear the strongest fair baseline by at least the equivalence band.

Answer-key oracle may be included only as a diagnostic. It must never support
preflight admission. If oracle headroom is answer-key-only, final verdict must
be `rejected_no_fair_signal` or `rejected_answer_key_oracle_gap`.

## 20. Baseline-Immunity Standard As Static Contract Only

The future implementation must apply
`BASELINE-IMMUNITY-ADMISSION-STANDARD-001A` as a static normative contract.

It must not treat the standard or registry as an executor. It must record
whether each registry class is applicable, invoked, and consumed by final
verdict derivation.

The baseline-immunity standard does not by itself run a baseline, prove
admission, or authorize candidate work.

## 21. Provenance Verifier As Local Prefilter Only

The local provenance verifier may be used only as a provenance-shape prefilter
after the future task emits an evidence bundle.

Allowed interpretation: `provenance_wellformed_only` means the bundle shape and
internal cross-references are well formed.

Forbidden interpretation: any verifier output as Gate pass, admission,
baseline-immunity, replay validation, source validation, strongest-baseline
validation, candidate success, or mechanism validity.

## 22. Leakage Scanner With Fail-Able Positive Controls

The future task must include a leakage scanner with at least one positive
control on the same admission path.

Leakage channels must include, where applicable:

- observation names;
- action names;
- filenames and fixture names;
- artifact structure;
- baseline hints;
- planted answer maps;
- value-level fields;
- label ordering;
- generator metadata;
- source-pin paths.

A positive control must be fail-able: injecting the leak must flip the
corresponding gate to blocked. A scanner that always passes or always detects
only a hard-coded fixture is invalid.

## 23. Replay Recalculation Requirement, Or Explicit Non-Applicability

Replay must recompute behavior from serialized state plus observation.

Hash-only replay, stored-output replay, or natural-language trace comparison is
insufficient.

If replay is not applicable to the proposed surface, the future implementation
must state why and cannot use replay language as positive evidence.

## 24. Source-Pin / Readback Requirement

The future task must read source pins through authoritative file APIs and
record:

- input artifacts;
- source paths;
- SHA256 hashes;
- readback channels;
- conflicts;
- fail-closed behavior on conflict;
- code path hashes for each producer function.

Self-readback only is invalid.

## 25. Computed-Evidence Provenance Gate

Every future score, baseline result, ablation result, leakage result, replay
result, control result, and verdict must derive from callable computation paths.

Each future row must record:

- producer_function;
- input artifacts;
- run_id;
- seed/context/episode IDs where applicable;
- aggregation rule;
- code_path_hash;
- consumed_by_final_verdict.

Forbidden:

- literal verdicts;
- static score dictionaries;
- detached reports;
- unconsumed controls;
- tests that only assert pass;
- self-readback-only source validation.

## 26. Ablation Requirement, Or Explicit Non-Applicability

The future preflight must rerun applicable episodes under real interventions,
including no-action or no-transition ablations when action conditioning is being
tested.

Ablation results must be independent callable rows and consumed by final verdict
derivation.

If ablations are not applicable to the frozen surface, the future implementation
must state why and cannot use ablation language as positive evidence.

## 27. Acceptance Gate

Accept the future implementation only if all are true:

- hostile audit accepts this implementation card before execution;
- independently frozen surface spec exists before implementation;
- surface spec path, SHA256, author/source, freeze pointer, and canonical
  readback channel are recorded;
- implementer did not author, mutate, amend, normalize, or freeze the surface
  spec;
- target variable is clear and candidate-free;
- observable channel is legal and fixed;
- action/query budget is fixed before execution;
- metric reports precision plus recall, stated F-beta, cost-weighted utility, or
  another explicitly balanced metric;
- no single-sided metric supports admission;
- `predict_all`, `predict_none`, `constant_k_sweep`, `random`, `majority`, and
  size-only sweep are computed and consumed by final verdict;
- if any degenerate predictor or size-only sweep reaches the ceiling band,
  verdict is `rejected_metric_degenerate`;
- positive partial-inferability demonstration exists and is fail-able;
- any oracle supporting headroom is budget-faithful, visible-channel only,
  legal-access only, not answer-key based, and clears the strongest fair
  baseline by at least the equivalence band;
- full applicable baseline panel is invoked and consumed;
- all six graph-cache challengers are invoked when representational or
  environment claims are present;
- lookup imitation panel is invoked when replay or memory claims are present;
- direct objective optimizer is invoked when the objective admits one;
- leakage scanner has fail-able positive controls;
- replay recomputes where replay is claimed;
- source-pin/readback provenance is fail-closed;
- every evidence row records producer_function, input artifacts, run_id,
  seed/context/episode IDs where applicable, aggregation rule, code_path_hash,
  and consumed_by_final_verdict;
- evidence bundle is verifier-compatible, with verifier used as prefilter only;
- `BASELINE-IMMUNITY-ADMISSION-STANDARD-001A` is cited as static contract only,
  not executor;
- no old Gate1 positive inheritance is reused;
- no candidate, Gate run, Route C run, runtime path, bridge path, or admission
  path is added.

Success authorizes at most candidate-card drafting. It does not authorize
candidate implementation, Gate1 pass claims, Gate4/Gate5 progression, mainline
integration, runtime integration, admission wiring, or remote anchoring.

## 28. Stop Condition

Stop with a blocking or rejection verdict if:

- hostile audit of this card is absent;
- independently frozen surface spec is missing before execution starts;
- candidate code or candidate output is needed;
- future implementer authors, mutates, amends, normalizes, or freezes the
  surface spec;
- target, observable channel, or budget cannot be defined pre-run;
- metric definition or equivalence band is chosen post-hoc;
- a metric is recall-only, precision-only, coverage-only, specificity-only,
  abstention-only, size-only, or otherwise single-sided;
- any metric-degeneracy trigger from the baseline-immunity standard fires;
- any degenerate predictor reaches the ceiling band;
- size alone can drive score to the ceiling band;
- metric-degeneracy controls are computed but not consumed by final verdict;
- partial-inferability demonstration is missing;
- fair channel has no signal;
- passive observation trivially decodes the target;
- oracle headroom is answer-key-only;
- any fair baseline ties or beats the proposed admissibility target;
- any applicable baseline panel member is missing, uninvoked, non-independent, or
  not consumed by final verdict;
- any graph-cache challenger is omitted or saturates;
- any leakage positive control is absent, not detected, or not consumed;
- replay is hash-only or not recomputed where replay is claimed;
- source readback conflicts and does not fail closed;
- verifier output is treated as admission;
- baseline-immunity standard is treated as an executor;
- literal verdicts, static score dictionaries, detached reports, or tests that
  only assert pass are used as evidence;
- any Gate1/Gate4/Gate5, Route C, bridge, admission, runtime, EGO, UI, LLM,
  AIRI, deployment, or companion path is touched;
- implementation attempts to commit, push, tag, or remote-anchor.

## 29. Rollback Plan

Remove only files created by the future preflight implementation under its
explicitly authorized paths.

Do not modify or clean unrelated dirty worktree state. Do not rewrite old Gate1,
Gate2, Gate3, integrated-route, Gate4, Route C, bridge, admission, runtime, or
EGO artifacts.

## 30. Expected Changed Files For The Future Implementation Task

This draft card does not authorize implementation. If hostile audit and explicit
user/task-card authorization later allow implementation, the future
implementation must restrict changes to isolated paths such as:

- `artifacts/gate1_replacement_preflight_00xa/`
- `src/gate1_replacement_preflight_00xa/`
- `tests/test_gate1_replacement_preflight_00xa.py`

The exact future paths must be re-confirmed before coding. No file outside a
future explicit allowlist may be touched.

## 31. Forbidden Files And Changes

Forbidden:

- any surface specification creation, mutation, normalization, amendment, or
  freeze;
- Gate1 candidate source or tests;
- old Gate1 source/tests/artifacts except read-only inputs;
- Gate2/Gate3/integrated-route source/tests/artifacts except read-only inputs;
- Gate4/Gate5 source/tests/artifacts;
- Route C source/tests/modules;
- baseline-immunity standard or registry edits;
- baseline-immunity executor creation;
- provenance verifier source/tests edits;
- runtime/mainline/admission/bridge files;
- EGO mainline, UI, LLM, AIRI, deployment, companion, product,
  relationship-learning, emotion, or proactive behavior files;
- commit, push, tag, or remote-anchor.

## 32. Claim Ceiling

Implementation-card draft only.

No Gate1 pass, no Gate1 replacement admissibility, no mechanism validity, no
candidate success, no baseline-immunity enforcement, no Gate4/Gate5 readiness,
no mainline/runtime/live effect, no agency, autonomy, consciousness, emotion,
stable user benefit, or EGO readiness.

If a future implementation later emits `admissible_for_candidate_card_drafting_only`,
that verdict authorizes at most candidate-card drafting and still does not
authorize candidate implementation, Gate1 pass claims, Gate4/Gate5 progression,
runtime integration, mainline integration, admission wiring, push, tag, or
remote-anchor.

## 33. Next Minimal Closed-Loop Action

Send this implementation-card draft, plus the drafting `final_report.md` and
`validation_report.json`, to Claude for hostile audit.

Even if Claude accepts the implementation card, the actual implementation must
still fail closed unless an independently frozen surface specification pack
exists before implementation begins.
