# SAME-AGENT-KERNEL-DRIFT-AWARE-TOMBSTONE-FORK-001A

Status: DRAFT FORK DECISION CARD / ROUTE-GOVERNANCE ONLY / NON-EXECUTABLE.

This card does not implement a successor experiment. It decides how to route the
banked `SAME-AGENT-MINIMAL-KERNEL-BRIDGE-001A` negative result after the tiny
passive/offline contrast collapsed to a fair drift-aware continual-replay
baseline. It must not be used to rescue the closed contrast.

Auto-Remote-Anchor: forbidden

## Operator decision recorded for this card

Recorded decision for the current fork boundary:

- Verdict to preserve:
  `BASELINE_EQUIVALENCE / downgrade`.
- Closed object:
  tiny passive/offline contrast mechanism-headroom claim only.
- Closure wording:
  `baseline_equivalence_closure / no_mechanism_headroom_under_drift_aware_continual_replay`.
- Runtime-kernel-v0 engineering scaffold:
  preserved as engineering infrastructure; not validated as a mechanism and not
  closed as an engineering runtime/kernel direction.
- Default fork selection:
  **Option A** — close this contrast and keep kernel as engineering runtime.
- Conditional successor rule:
  open an Option C preflight only if an ex-ante active/interventional separation
  can be stated before implementation. No C preflight is authorized by this
  card.
- Option B:
  not selected as default; passive/offline successor work remains blocked unless
  a separate bounded card states a concrete ex-ante differentiator against
  drift-aware continual replay and survives collision review.

## Task id

`SAME-AGENT-KERNEL-DRIFT-AWARE-TOMBSTONE-FORK-001A`

## Problem definition

`SAME-AGENT-MINIMAL-KERNEL-BRIDGE-001A` produced a clean, callable tiny CPU
contrast, but the strongest fair baseline
`drift_aware_regime_inferring_continual_replay` tied the candidate at `1.0`.
That closes the tiny passive/offline contrast mechanism-headroom claim as:

```text
baseline_equivalence_closure /
no_mechanism_headroom_under_drift_aware_continual_replay
```

The fork problem is narrower than "does the runtime kernel exist?" The runtime
kernel scaffold may remain useful as engineering infrastructure. The decision
needed here is whether there is an ex-ante, testable successor contrast where an
active prediction-error-driven belief-update kernel should outperform
drift-aware regime-inferring continual replay under fair access, or whether
mechanism testing should stop for this route and the kernel should be retained
only as an engineering runtime scaffold.

## Current layer

Engineering implementation readback + mechanism-hypothesis route governance +
learning/adaptation boundary. No subjectivity-validation or
philosophical-consciousness layer is entered.

## Current repo / source readback

Preflight readback for this draft card:

- repo root: `D:/Project/AIProject/MyProject/intelligence-theory-lab`
- branch: `codex/meta-theory-scaffold`
- HEAD: `48269bd61d809b95cd3a4b1d3e9f7f064fd2626f`
- upstream relation: ahead `8`, behind `0`
- worktree/index before this card: clean
- `.git/index.lock`: absent

Route-state readback:

- `artifacts/ROUTE-STATE-MACHINE-001A/validation_report.json` reports
  `verdict=pass`, `validation_errors=[]`, local route-governance validation
  only.
- `N2-SBMC-ENV-REDESIGN-001A` remains
  `current_state=ADJUDICATED`, `closure_type=BASELINE_EQUIVALENCE`,
  `mechanism_validity=unknown`, `theory_validity=unknown`.
- N2 remains closed and must not be reopened, rescued, re-scored, or cited as
  mechanism evidence.
- `program_state.json` allows drafting a new bounded task card and forbids
  starting a successor route without one.

Ledger readback from `docs/research/FSP-STAGE-LEDGER.md`:

- `L-011`: PUM-ENV is terminal `INVALID_INSTRUMENT`; future active-axis or N2
  mechanism claims require their own environment certification.
- `L-015`: a closed loop buys ablation surfaces only; the integrated-cache and
  learning baseline families are the real bar; baseline equivalence to known
  replay/continual/meta methods was the honest likely outcome.
- `L-016`: the decisive kernel contrast was predeclared as kernel vs
  drift-aware regime-inferring continual replay in `G*`; tie means downgrade.
- `L-017` / `L-018`: N2 graph-closure baseline equivalence is terminal local
  route-governance negative evidence only.

Banked runtime-kernel readback from
`artifacts/SAME-AGENT-MINIMAL-KERNEL-BRIDGE-001A/`:

- `result.json`: `verdict=BASELINE_EQUIVALENCE`,
  `candidate_score=1.0`,
  `strongest_fair_baseline_id=drift_aware_regime_inferring_continual_replay`,
  `strongest_fair_baseline_score=1.0`,
  `run_id=same_agent_minimal_kernel_bridge_001a_tiny_cpu_001`.
- `baseline_comparison.json`: `missing_baseline_ids=[]`,
  `strongest_fair_is_max_over_full_battery=true`; `batch_precompute=1.0` and
  `strong_meta_learner=1.0` also matched the candidate.
- `failure_manifest.json`: `has_blocking_failure=true`,
  `preserve_as_negative_evidence=true`,
  blocker `baseline_tied_or_beat_candidate:drift_aware_regime_inferring_continual_replay`.
- `ablation_report.json`: ablation sensitivity passed, but does not rescue
  mechanism headroom because the fair baseline tied.
- `replay_report.json`: replay passed; behavior recomputed from
  `serialized_state`, `observation`, and `feedback`; not hash-only and not
  stored-output-only.
- `leakage_report.json`: clean scan passed, leakage not detected, positive
  control fired.

## Mainline target

No mainline target. This card targets only a docs-level route fork decision in
ITL. It does not authorize EGO runtime, EGO mainline, UI, companion behavior,
LLM/AIRI integration, deployment, API keys, external services, or background
execution.

## Mainline integration status

None. No runtime or production path is integrated or authorized by this card.

## Enabled status

Disabled. This card is not executable and creates no CLI entrypoint. The banked
kernel scaffold remains local explicit CLI only; this card does not enable it.

## Real-trigger evidence

The only real-trigger evidence cited here is the already banked local CLI run:

```text
python -m src.same_agent_minimal_kernel_bridge_001a --output-dir artifacts/SAME-AGENT-MINIMAL-KERNEL-BRIDGE-001A
```

No fresh experiment, rerun, retune, or scoring is authorized by this fork card.

## Hypothesis / fork question

Decision question:

```text
Can we state ex ante a task where active prediction-error-driven belief update
should outperform drift-aware regime-inferring continual replay under fair
access?
```

If yes, the output should be a new bounded successor task card with a frozen
baseline contract and a precise falsifier. If no, close mechanism testing for
this route and keep the runtime kernel only as engineering infrastructure.

Minimal admissibility statement for any successor:

```text
Candidate should do X because it can use [specific mechanism].
Drift-aware continual replay should fail or lag because it lacks
[specific capability] under equal data, fair access, and stated budget.
If this sentence cannot be written before coding, do not build the successor.
```

## Strongest baseline / shortcut explanation

The standing strongest explanation is that the candidate kernel re-derived
known learning/replay machinery:

- drift-aware regime-inferring continual replay matched candidate at `1.0`;
- batch precompute matched candidate at `1.0`;
- strong meta learner matched candidate at `1.0`;
- ablation sensitivity proved wiring was not decorative, but did not prove
  headroom over the strongest fair baselines.

Any successor must treat drift-aware continual replay, batch precompute, and
strong meta learner as active invalidators unless the successor card gives an
ex-ante reason they should fail or lag.

## Fork options

### Option A — Close this contrast; keep kernel as engineering runtime

- Decision: close the tiny passive/offline contrast permanently as mechanism
  headroom negative evidence.
- Preserve: runtime-kernel-v0 scaffold as auditable engineering infrastructure.
- Do not claim: mechanism validity, runtime-kernel pass, learning headroom,
  agency, autonomy, subjectivity, consciousness, EGO readiness, or mainline
  effect.
- Allowed next action: documentation-only closure / handoff / route-state packet
  under a separate exact-path task card if needed.
- Acceptance signal: no credible ex-ante differentiator against drift-aware
  continual replay can be stated.

### Option B — Design tombstone-targeting successor against drift-aware continual replay

- Decision: draft a new successor test card whose explicit target is the
  tombstone `drift_aware_regime_inferring_continual_replay tied candidate`.
- Required ex-ante differentiator: the card must state why the candidate uses a
  specific mechanism unavailable to, or resource-inadmissible for, the
  drift-aware continual baseline under fair access.
- Required falsifier: if drift-aware continual replay ties within the
  equivalence band on the first decisive contrast, stop and close; do not patch.
- No implementation under this fork card.
- Acceptance signal: a concrete differentiator survives collision review before
  code.

### Option C — Shift to active/interventional environment

- Decision: draft a successor card where the distinguishing variable is not
  passive/offline replay quality but active intervention: the candidate must
  choose information-gathering actions whose value depends on current belief,
  prediction error, action cost, and expected information gain.
- Required baseline: fair active baselines, including UCB, myopic information
  gain, structural Bayes/EVI where tractable, drift-aware active replay, and
  no-update/no-memory controls.
- Required falsifier: if a structural Bayes/EVI or drift-aware active baseline
  ties, stop and close or downgrade.
- No implementation under this fork card.
- Acceptance signal: the ex-ante difference is about active control of evidence,
  not another passive replay contest.

## Collision record

### Candidate 1: Close and preserve engineering runtime only

- Evidence it would produce: route-governance clarity; no further mechanism
  evidence.
- Strongest cheap baseline: already matched; no new baseline required.
- Leakage / hard-coding risk: low because no new test is built.
- Smallest falsifying test: operator can state a concrete ex-ante differentiator
  that the current baseline family should not match.
- Expected failure mode: route may stop too early if a valid active/interventional
  differentiator exists but is not articulated.

### Candidate 2: Tombstone-targeting passive successor

- Evidence it would produce: a narrower passive/offline successor aimed exactly
  at the drift-aware replay tombstone.
- Strongest cheap baseline that could match it: drift-aware continual replay
  with stronger segmentation, look-back relabeling, batch precompute, or meta
  learner.
- Leakage / hard-coding risk: high; the successor may smuggle regime identity,
  task phase, or target labels to make the candidate appear special.
- Smallest falsifying test: drift-aware replay with equal data/budget ties the
  first decisive contrast.
- Expected failure mode: another baseline-equivalence closure if the
  differentiator is really "better replay."

### Candidate 3: Active/interventional successor

- Evidence it would produce: tests whether active prediction-error-driven belief
  update changes information-seeking actions, not just held-out passive
  prediction.
- Strongest cheap baseline that could match it: structural Bayes/EVI, active
  information gain, UCB, or drift-aware active replay under fair access.
- Leakage / hard-coding risk: high if hidden rule identity is embedded in action
  names, observation fields, or reward shaping.
- Smallest falsifying test: structural Bayes/EVI or drift-aware active baseline
  ties the candidate; or action choice does not depend on serialized belief.
- Expected failure mode: active-specific baseline absorbs the candidate,
  producing valid negative evidence.

## Selection rule

This card does not select A, B, or C by itself. It fixes the decision rule:

1. If no concrete ex-ante differentiator against drift-aware continual replay is
   written and accepted, select Option A.
2. If the differentiator is still passive/offline and specifically targets the
   drift-aware replay tombstone, select Option B and draft a new bounded task
   card before any code.
3. If the differentiator requires controlled evidence-gathering actions, select
   Option C and draft a new active/interventional environment card before any
   code.
4. In all cases, any future tie with the strongest fair baseline means closure
   or downgrade, not rescue.

## Ablation requirement for any successor

Any future successor card must include real rerun ablations:

- no-update;
- no-memory-read;
- no-replay/consolidation;
- corrupted replay;
- prediction-error shuffle;
- action-conditioning off;
- counterfactual serialized memory/belief;
- if active/interventional: no-information-gain, random-action, cost-blind, and
  oracle/EVI reference controls where tractable.

Ablations remain necessary but not sufficient. They cannot rescue a tie with the
strongest fair baseline.

## Trace / replay requirement for any successor

Any successor must recompute behavior from serialized state plus observations
and feedback/action outcomes. Stored-output replay and hash-only replay are
insufficient. If the successor is active, the trace must additionally record
action candidates, expected information value or equivalent action rationale,
action costs, and belief-state dependencies used by the selector.

## Computed-evidence provenance gate for any successor

Any future result must come from callable producer paths and record:

- `producer_function`;
- input artifacts;
- `run_id`;
- seed/context/episode IDs;
- aggregation rule;
- `code_path_hash`;
- independent callable baselines;
- positive-control leakage scan;
- replay recomputation path;
- unused seed/context/heldout/counterfactual checks.

Static verdict dictionaries, hand-written scores, and tests that only assert
pass are forbidden.

## Acceptance gate for this card

This fork card is accepted only if it:

- preserves the banked `BASELINE_EQUIVALENCE` negative result;
- states that the closed object is only the tiny passive/offline
  mechanism-headroom contrast;
- preserves runtime-kernel-v0 as engineering scaffold, not as validated
  mechanism;
- defines Options A/B/C and the ex-ante differentiator rule;
- forbids implementation, rerun, retune, rescue, push, tag, and remote-anchor;
- keeps claim ceiling at route-governance / bounded negative-evidence routing
  only.

## Claim ceiling

This card can claim only route-governance fork design over a banked bounded
offline negative result. It cannot claim mechanism validity, learning headroom,
runtime-kernel pass, agency, autonomy, subjectivity, consciousness, emotion,
EGO readiness, companion readiness, production readiness, user benefit, or
mainline effect.

## Stop condition

Stop and report if:

- any attempt is made to rerun, retune, patch, or rescue
  `SAME-AGENT-MINIMAL-KERNEL-BRIDGE-001A`;
- N2 is reopened, rescored, or reinterpreted;
- the closed object is misstated as runtime-kernel-v0 engineering closure rather
  than tiny contrast mechanism-headroom closure;
- a successor is proposed without a clear ex-ante differentiator against
  drift-aware continual replay;
- baseline family is weakened;
- route-state is mutated without a separate exact-path route-state task card;
- claim language upgrades beyond the ceiling.

## Rollback plan

If rejected, remove only:

```text
docs/codex/tasks/SAME-AGENT-KERNEL-DRIFT-AWARE-TOMBSTONE-FORK-001A.md
```

Do not modify or delete the banked negative evidence from
`SAME-AGENT-MINIMAL-KERNEL-BRIDGE-001A`.

## Expected changed files

Current task:

- create
  `docs/codex/tasks/SAME-AGENT-KERNEL-DRIFT-AWARE-TOMBSTONE-FORK-001A.md`

No source, tests, artifacts, route-state files, ledger entries, or remote refs
are expected.

## Forbidden changes

- source code implementation;
- experiment reruns or scoring;
- N2 artifacts or route packets;
- route-state mutation;
- EGO mainline/runtime/UI/companion/LLM/AIRI/API/deployment paths;
- baseline weakening;
- push, tag, or remote-anchor.

## Next minimal closed-loop action

Operator chooses one:

```text
A. close this contrast and keep kernel as engineering runtime;
B. draft tombstone-targeting successor against drift-aware continual replay;
C. draft active/interventional successor environment.
```

If no ex-ante differentiator can be written, choose A.

## What this does not prove

This card does not prove mechanism validity, runtime-kernel pass, learning
headroom, agency, autonomy, subjectivity, consciousness, EGO readiness,
companion readiness, production readiness, user benefit, or mainline effect. It
only preserves the correct fork after a bounded offline tiny contrast was
absorbed by fair baselines.
