# LRGG-CANDIDATE-FREE-CHEAP-TIER-EXECUTION-001A

Task ID: `LRGG-CANDIDATE-FREE-CHEAP-TIER-EXECUTION-001A`

Drafting task ID: `LRGG-CANDIDATE-FREE-CHEAP-TIER-EXECUTION-001A-DRAFT-ONLY`

Status: draft execution-card contract requiring one-point independent confirm re-audit before
any implementation, run, commit, push, tag, remote anchor, route upgrade, or candidate work.

Latest repair task ID:

```text
LRGG-CANDIDATE-FREE-CHEAP-TIER-EXECUTION-001A-ADMISSION-VERDICT-REPAIR-ONLY
```

Latest repair trigger:

```text
requires_execution_card_repair_before_authorization
```

Auto-Remote-Anchor: forbidden.

## 1. Status Block

Current layer:

```text
engineering-governance / candidate-free cheap-tier execution-card drafting
```

Mainline integration status:

```text
none
```

No EGO runtime, EGO mainline, bridge, admission, UI, product behavior, agent capability,
mechanism candidate, self/boundary route, 001C path, or deployment path is integrated or
touched by this draft.

Enabled status:

```text
none
```

This draft enables no generator, scorer, oracle, baseline, replay, leakage scanner,
provenance checker, training path, Gate run, or EGO path.

Real trigger evidence:

```text
confirm_accept_for_cheap_tier_execution_card_drafting
```

The upstream bounded B2 confirm re-audit accepted drafting only after the repaired preflight
card closed:

- B1: oracle taxonomy plus equal-access / do(.) parity.
- B2: registry-compatible baseline matrix including trivial predictors and metric-degeneracy
  controls.
- B3: Tier 3 deferred; first future execution limited to Tier 0-2 cheap-tier plus
  leakage/replay/provenance debug split.
- R2 strengthening: budget robustness protocol plus LCB plus no-headroom / INVALID handling.

Claim ceiling:

```text
This draft may define a bounded future execution-card contract for the LRGG candidate-free
cheap-tier debug preflight only.
```

It cannot prove LRGG admissibility, candidate-free headroom, oracle validity, baseline
failure, replay/provenance validity, leakage scanner validity, mechanism evidence, agency,
self, subjectivity, emotion, consciousness, autonomy, EGO readiness, H0/H1, 001A downgrade,
001C authorization, or TLGP-001B-R2 reinterpretation.

Next minimal closed-loop action:

```text
one-point independent confirm re-audit of the admission-verdict repair before any operator
freeze, implementation, or execution authorization
```

## Canonical Source Readback

This draft is grounded in direct repo-local content readback from:

- `docs/codex/tasks/LRGG-CANDIDATE-FREE-PREFLIGHT-001A.md`
- `docs/codex/contracts/BASELINE-IMMUNITY-ADMISSION-STANDARD-001A.md`
- `docs/codex/contracts/BASELINE-IMMUNITY-ADMISSION-STANDARD-001A.registry.json`

Readback status:

```text
required_sources_readable
```

No stale mount metadata, apparent file size, or mtime is used as the authority for this
draft. If any future executor cannot read these sources directly, the only permitted
blocking label is:

```text
blocked_pending_canonical_readback
```

## Task Card Fields

Problem definition:

```text
Define a draft-only execution card for the first LRGG candidate-free cheap-tier preflight
that can later test generator/scorer/oracle/baseline/leakage/replay/provenance plumbing on
a debug split without authoring a mechanism candidate or touching EGO paths.
```

Current stage:

```text
draft execution-card contract only
```

Mainline target:

```text
none
```

Enabled-state requirement:

```text
future execution remains disabled until independent hostile audit accepts this draft and a
separate operator authorization freezes placeholders before results
```

Real-trigger evidence requirement for future execution:

```text
callable producer paths must invoke generator, scorer, oracle, baselines, leakage positive
controls, replay, provenance checks, and tamper probes on recorded debug-split inputs
```

Hypothesis for future execution:

```text
If the LRGG cheap-tier route is even preliminarily executable, then Tier 0-2 debug-split
plumbing should be callable, replayable, provenance-pinned, leakage-positive-control
fail-able, and cheap-baseline-challenged without needing a mechanism candidate or heavy
training.
```

Strongest baseline:

```text
The strongest cheap or degenerate legal-access baseline may already saturate the debug
metric. If that happens, the correct future outcome is rejection/closure, not repair until
positive-looking output appears.
```

Ablation requirement:

```text
future ablations must rerun episodes under real interventions; post-hoc score edits,
stored-result rewrites, and synthetic degradation flags are forbidden
```

Trace/replay requirement:

```text
future replay must recompute the official score from serialized state and action trace;
stored JSON scores are not sufficient evidence
```

Computed-evidence provenance gate:

```text
future scores, verdicts, baseline comparisons, leakage alarms, ablations, replay reports,
and tamper probes must be produced by callable paths with function IDs, source hashes,
input artifacts, run IDs, seed/context IDs, aggregation rule, and code path hash
```

Acceptance gate:

```text
independent hostile audit confirms this draft preserves Tier 0-2 debug-only scope, excludes
admissible_for_candidate_preflight as a Tier 0-2 terminal verdict, preserves equal access,
required baselines, positive-control leakage, replay recomputation, full provenance fields,
hard stop conditions, threshold placeholders, and no mechanism/runtime authorization
```

Stop condition:

```text
any scope drift into implementation, candidate work, Tier 3 without separate authorization,
Tier 4-5, runtime/mainline/bridge/admission paths, threshold selection, remote anchor,
admissible_for_candidate_preflight emission by Tier 0-2, or 001C authorization blocks this
draft
```

Rollback plan:

```text
revise or remove this single draft document; no source, test, artifact, contract, research,
runtime, bridge, admission, or external-service path is part of rollback
```

Expected changed files for the drafting task:

```text
docs/codex/tasks/LRGG-CANDIDATE-FREE-CHEAP-TIER-EXECUTION-001A.md
```

Forbidden changes:

```text
src/**
tests/**
artifacts/**
docs/research/**
docs/codex/contracts/**
EGO runtime/mainline/bridge/admission paths
SAME_AGENT_BRIDGE* docs
```

Auto-Remote-Anchor decision:

```text
forbidden
```

## 2. Authorization Boundary

Allowed future implementation scope, if separately authorized after audit and operator
freeze:

- isolated LRGG debug-split generator.
- scorer.
- `budget_faithful_visible_channel_oracle`.
- cheap baselines.
- leakage positive controls.
- replay/provenance.
- debug split only.

Forbidden:

- mechanism candidate.
- self/boundary route.
- Tier 3 unless separately authorized.
- Tier 4-5.
- training-heavy runs.
- EGO runtime/mainline/bridge/admission.
- UI, companion behavior, product behavior, LLM/AIRI integration, relationship learning,
  emotion systems, self-awareness simulation, deployment, API keys, or external services.
- remote anchor.
- 001C.
- threshold tuning after results.

This draft itself does not authorize implementation or execution.

## 3. Upstream Contract Freeze

The repaired upstream preflight card
`docs/codex/tasks/LRGG-CANDIDATE-FREE-PREFLIGHT-001A.md` is frozen upstream governance for
this execution-card draft.

All threshold placeholders remain unresolved until operator freeze before any run. This
draft does not choose numeric thresholds, beta, weights, seed counts, cardinality values,
confidence intervals, budgets, train/test sizes, or aggregation values.

The required placeholder value for unresolved thresholds and budget/metric parameters is:

```text
OPERATOR_FREEZE_BEFORE_RESULTS
```

Any future threshold, budget, metric, split, aggregation, or confidence rule selected or
changed after observing outputs is a hard stop and cannot be used to rescue a verdict.

## 4. First Executable Stage Definition

The only future executable stage described by this draft is:

```text
Tier 0-2 cheap-tier + leakage/replay/provenance debug split
```

Required future scope:

- Tier 0: generator / scorer / replay smoke.
- Tier 1: `budget_faithful_visible_channel_oracle` solvability under equal legal access.
- Tier 2: cheap-baseline failure on debug split.
- leakage positive controls.
- replay/provenance.
- tamper probes.

Tier 3 reduced supervised witness is not part of this first executable stage unless a
separate task card explicitly authorizes it.

Tier 4-5 strong learner training is forbidden by this first executable stage.

`admissible_for_candidate_preflight` is NOT a permissible terminal verdict for the Tier 0-2 cheap-tier stage.

Per the bound baseline-immunity admission standard, `admissible_for_candidate_preflight`
requires passing the full required-baseline registry with no stop condition, including the
Tier 4 fitted cross-episode amortized / ideal-observer learner and the Tier 5
structural-heldout retention. Both are outside the Tier 0-2 cheap-tier scope.

Therefore Tier 0-2 cheap-tier may not authorize candidate-card drafting.

Tier 0-2 cheap-tier may emit only:

- `rejected_metric_degenerate`
- `rejected_no_fair_signal`
- `rejected_trivially_decodable`
- `rejected_baseline_saturated`
- `INVALID`
- `blocked_pending_canonical_readback`
- `cheap_tier_plumbing_valid_not_saturated__proceed_to_separately_authorized_heavier_tiers`

`cheap_tier_plumbing_valid_not_saturated__proceed_to_separately_authorized_heavier_tiers`
is a bounded non-admission proceed label.

It means only that Tier 0-2 debug-split plumbing did not trigger a cheap-tier block or
invalidation condition under frozen placeholders.

It does NOT prove candidate-free headroom.

It does NOT authorize mechanism candidate drafting.

It does NOT authorize `admissible_for_candidate_preflight`.

It only permits the operator to request a separate bounded authorization for heavier tiers,
such as Tier 3 reduced supervised witness or Tier 4-5 strong learner / structural-heldout
evaluation.

## 5. Oracle Contract

The only allowed upper-bound oracle term is:

```text
budget_faithful_visible_channel_oracle
```

Do not use `mechanism_oracle` as the LRGG upper bound.

Forbidden oracle upper-bound token:

```text
mechanism_oracle
```

The oracle contract for future execution must include:

- equal legal access to observations, actions, interventions, queries, and do(.) budget as
  the fair panel.
- no direct hidden graph reads.
- no direct remapping reads.
- no direct latent-value reads.
- no generator-truth imports.
- no scorer-truth imports.
- no candidate-circular logic.
- no unequal do(.) budget.
- nonreading oracle ablation.
- oracle import-path positive control.
- twin-pair residual uncertainty as necessary but not sufficient.

If the oracle has access denied to the fair panel, classify the apparent gap as:

```text
forbidden interventional-minus-observational
```

Do not classify that gap as headroom.

Nonreading oracle ablation requirement:

```text
Disable, zero, or shuffle the oracle's latent-estimation path while preserving legal
observation/action/intervention access. The ablated oracle must collapse to the
operator-frozen chance/baseline band.
```

Oracle import-path positive control requirement:

```text
If hidden latent, hidden graph, remapping, generator truth, scorer truth, or other
forbidden truth is injected through an oracle import path, the provenance/import-path audit
must alarm. If it does not alarm, stop.
```

Twin-pair limitation:

```text
Twin-pair residual uncertainty can show passive indistinguishability under P(X), but it
does not by itself prove equal access, forbid direct truth reads, verify budget parity, or
establish headroom.
```

## 6. Generator Contract

The future generator must be specified before implementation and must produce:

- debug split.
- serialized hidden rule graph.
- serialized remapping.
- initial state.
- legal action space.
- observation function.
- transition function.
- reward/scoring target.
- deterministic seed/context behavior.
- task-space metadata sufficient for entropy/enumerability checks.

The generator must not expose hidden rule graph, remapping, latent values, split membership,
score keys, task-family IDs, generator truth, or scorer truth to agents except in explicit
leakage positive-control branches.

Candidate-authored ground truth is forbidden.

## 7. Scorer Contract

The future scorer must:

- be callable.
- be source-hashed.
- compute official debug metric from replayed trace.
- support balanced/cost-weighted metric rule placeholder.
- consume trivial-predictor checks.
- never read stored JSON score as official evidence without recomputation.

Required scorer placeholders:

```text
metric_balance_rule = OPERATOR_FREEZE_BEFORE_RESULTS
aggregation_method = OPERATOR_FREEZE_BEFORE_RESULTS
confidence_interval_lower_confidence_bound_rule = OPERATOR_FREEZE_BEFORE_RESULTS
```

Single-sided metrics and metrics defeated by trivial predictors are inadmissible.

## 8. Required Baselines

All future baselines must be independent callable implementations under legal access. A
stored narrative row, static verdict dictionary, or unused baseline declaration is not
evidence.

### trivial_predictors

Required members:

```text
predict_all
predict_none
constant_k_sweep
```

`constant_k_sweep` must sweep k over the relevant set/cardinality for the metric under
test.

Blocking rule:

```text
If predict_all, predict_none, or any constant_k_sweep reaches the oracle/ceiling band or
defeats the declared metric, verdict = rejected_metric_degenerate.
```

### graph_cache_family

Required six-member family:

```text
graph_lookup
transition_table
successor_map
count_table
fsm_planner
episodic_traversal
```

Blocking rule:

```text
If any graph-cache family member reaches oracle-equivalence band, verdict =
rejected_baseline_saturated.
```

### direct_objective_optimizer

Examples:

```text
discounted_wls
least_squares
convex_objective_solver
```

Blocking rule:

```text
If a direct objective optimizer reaches oracle-equivalence under legal access, verdict =
rejected_baseline_saturated.
```

### task_specific_classical

Examples:

```text
hand-coded dynamic-programming solver under legal access
finite-state filter under legal access
classical planner under legal access
```

Blocking rule:

```text
If a task-specific classical solver reaches oracle-equivalence under legal access,
verdict = rejected_baseline_saturated.
```

### Other cheap baselines

Required members:

- random.
- majority / mode.
- obs-only predictor.
- memoryless policy.
- lookup table.
- n-gram / short-history cache.
- trajectory nearest-neighbor retrieval.
- raw-observation latent decoder probe.
- exhaustive legal probe on reduced worlds.
- value-level attacker family:
  - mean.
  - variance.
  - correlation.
  - PCA.
  - cross-episode.
  - supervised.
  - membership.

Use `family_max` for value-level attacker decisions.

If lookup, n-gram, trajectory nearest-neighbor, raw-observation latent decoder, exhaustive
legal probe, or value-level attacker reaches the operator-frozen block band, stop with the
registry-compatible rejection/invalidation label.

## 9. R2 Headroom Rule for Future Execution

Future debug execution does not establish final headroom.

Any provisional candidate-free headroom metric must be defined exactly as:

```text
budget_faithful_visible_channel_oracle - max(fair_panel_including_amortized_meta_learner_and_degenerate_controls)
```

The fair panel must include:

- fitted cross-episode amortized / ideal-observer-approximating learner as future Tier 4
  item.
- degenerate controls / trivial predictors.
- cheap baselines.
- value-level attackers.
- graph-cache family.
- direct objective optimizer.
- task-specific classical solver.

For this first cheap-tier execution, if any cheap baseline or trivial predictor saturates,
the route closes or rejects before strong learner stages.

Do not define headroom as oracle minus a weak baseline, candidate minus cheap baseline,
interventional-minus-observational access gap, or any comparison that excludes degenerate
controls from the fair-panel max.

## 10. Leakage Positive Controls

Future execution must require callable positive controls with expected alarm behavior:

- hidden rule-ID injection.
- latent graph exposure.
- task-family ID exposure.
- score-key / reward-shaping artifact exposure.
- train/test split membership leakage scan.
- seed/config/file-name leakage scan.
- observation-field audit.
- serialized-state audit.
- generator/scorer/oracle import-path audit.
- value-level attacker planted leak.

If any positive control fails to trigger the expected alarm, stop.

Name-only leakage scans are insufficient. Value-level and structural leakage channels must
be covered.

## 11. Replay / Provenance Schema

Future evidence rows must include all fields:

```text
generator_fn_id
generator_source_sha256
scorer_fn_id
scorer_source_sha256
oracle_fn_id
oracle_source_sha256
baseline_producer_fn_id
run_id
split_id
seed_ids
context_ids
hidden_rule_graph_id
hidden_rule_graph_hash
remapping_id
remapping_hash
initial_state_serialization
action_trace
observation_trace_hash
reward_trace_hash
replay_recomputed_score
aggregation_code_path_sha256
code_version
commit_hash
failure_path_tests
tamper_probe
```

Replay must recompute the official score from serialized state and action trace.

Stored JSON score is not sufficient.

Tamper probes must include:

- mutate one action.
- mutate source byte.
- mutate hidden graph/remapping.
- mutate observation trace.
- mutate reward trace.

Expected tamper behavior must be frozen before results. If tamper probes fail to change the
recomputed score where they should, or fail to trip provenance where they should, stop.

## 12. Verdict Taxonomy

Full registry verdicts recognized by the broader standard:

- `admissible_for_candidate_preflight` — reserved for full multi-tier panel only; not
  emittable by Tier 0-2 cheap-tier.
- `rejected_metric_degenerate`
- `rejected_no_fair_signal`
- `rejected_trivially_decodable`
- `rejected_baseline_saturated`
- `blocked_pending_canonical_readback`
- `INVALID`

Tier 0-2 cheap-tier terminal verdicts permitted by this card:

- `rejected_metric_degenerate`
- `rejected_no_fair_signal`
- `rejected_trivially_decodable`
- `rejected_baseline_saturated`
- `INVALID`
- `blocked_pending_canonical_readback`
- `cheap_tier_plumbing_valid_not_saturated__proceed_to_separately_authorized_heavier_tiers`

The non-admission proceed label means only that Tier 0-2 debug-split plumbing did not
trigger a cheap-tier block or invalidation condition under frozen placeholders. It does not
prove candidate-free headroom, does not authorize mechanism candidate drafting, does not
authorize `admissible_for_candidate_preflight`, and only permits the operator to request a
separate bounded authorization for heavier tiers.

Draft closeout labels allowed:

- `execution_card_admission_verdict_repaired_requires_one_point_confirm`
- `execution_card_draft_created_requires_hostile_audit`
- `blocked_pending_canonical_readback`
- `refused_due_scope_violation_prevented`

Forbidden labels:

- `pass`
- `ready`
- `integrated`
- `live`
- `mechanism-valid`
- `EGO-ready`
- `001C-authorized`

## 13. Stop Conditions

Hard-stop future execution if:

- oracle cannot solve debug distribution.
- oracle has unequal access.
- oracle reads hidden truth directly.
- nonreading oracle ablation does not collapse.
- trivial predictors defeat metric.
- metric is single-sided or unbalanced.
- graph-cache family saturates.
- direct objective optimizer saturates.
- task-specific classical solver saturates.
- lookup / n-gram / trajectory nearest-neighbor saturates.
- obs-only/value-level attacker decodes latent above ceiling.
- exhaustive legal probe reaches oracle band.
- replay cannot recompute score.
- leakage positive controls fail to alarm.
- provenance fields are missing.
- tamper probes fail.
- thresholds were changed after results.
- any implementation touches forbidden mainline/runtime/bridge/admission paths.

Stop means close/reject/redesign, not patch until positive-looking output appears.

If a stop condition triggers, preserve the negative or invalid evidence under the separately
authorized future task scope and do not retune thresholds, weaken baselines, reinterpret the
result, anchor remotely, or proceed to candidate work.

Even after Tier 0-2 implementation/run is separately authorized and executed, a non-blocked
result can at most produce
`cheap_tier_plumbing_valid_not_saturated__proceed_to_separately_authorized_heavier_tiers`.
It cannot produce `admissible_for_candidate_preflight` or authorize candidate-card drafting.

## 14. Operator Freeze Requirements Before Any Run

Before any implementation or run, the operator must freeze all unresolved placeholders,
including:

- `tau_oracle`
- random / majority ceiling
- trivial predictor oracle-band threshold
- metric balance rule
- obs-only block threshold
- lookup / graph-cache block threshold
- direct objective optimizer block threshold
- task-specific classical block threshold
- n-gram block threshold
- raw-observation latent decodability ceiling
- cheap-tier debug split seed/context count
- aggregation method
- confidence interval / LCB rule
- max train/test leakage
- minimum task-space entropy / max enumerability
- oracle failability band
- tamper-probe expected behavior

Do not choose these values in this task.

The canonical placeholder remains:

```text
OPERATOR_FREEZE_BEFORE_RESULTS
```

## 15. Execution-Card Acceptance Gate

This drafted execution card is acceptable only if an independent hostile audit confirms:

- scope is limited to Tier 0-2 cheap-tier plus leakage/replay/provenance debug split.
- no implementation is authorized by the draft itself.
- oracle contract preserves equal access.
- trivial predictors are present and fail-able.
- graph-cache family has six members.
- direct objective optimizer and task-specific classical are included.
- leakage controls are positive-control based.
- replay recomputes score.
- provenance schema is complete.
- stop conditions are hard.
- thresholds remain placeholders.
- no mechanism candidate or runtime/mainline path is authorized.
- `admissible_for_candidate_preflight` is excluded from Tier 0-2 terminal verdicts.
- the non-admission proceed label authorizes no candidate-card drafting and only permits a
  request for separate bounded heavier-tier authorization.

If hostile audit finds scope drift, missing baseline family, missing failability, unresolved
source-readback conflict, threshold selection, or hidden execution authorization, this draft
must be revised or closed before implementation.

Even if this one-point repair is accepted, the next effect is only operator permission to
freeze placeholders and separately authorize a Tier 0-2 cheap-tier implementation/run. The
draft still cannot directly authorize execution, Tier 3, Tier 4-5, candidate-card drafting,
runtime/mainline work, remote anchor, or 001C.

## What This Does Not Prove

This draft does not prove:

- LRGG admissibility.
- candidate-free headroom.
- oracle validity.
- baseline failure.
- replay/provenance validity.
- leakage scanner validity.
- mechanism evidence.
- agency, self, subjectivity, emotion, consciousness, or autonomy evidence.
- EGO readiness.
- H0/H1.
- 001A downgrade.
- 001C authorization.
- TLGP-001B-R2 reinterpretation.
