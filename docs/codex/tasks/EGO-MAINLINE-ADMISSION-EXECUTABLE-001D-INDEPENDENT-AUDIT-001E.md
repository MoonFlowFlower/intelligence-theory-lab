# EGO-MAINLINE-ADMISSION-EXECUTABLE-001D-INDEPENDENT-AUDIT-001E

## Task Identity

```text
task_id = EGO-MAINLINE-ADMISSION-EXECUTABLE-001D-INDEPENDENT-AUDIT-001E
layer = bounded independent audit / red-team audit of EGO-MAINLINE-ADMISSION-EXECUTABLE-001B-BASELINE-LEGAL-INPUT-REPAIR-001D only
claim_ceiling = bounded independent audit evidence for 001D baseline-legal-input repair under synthetic / controlled conditions only
```

## Problem Definition

Independently audit whether 001D repaired the exact 001C baseline legal-input
blocker and the two named failure-path coverage gaps without rewriting 001B,
001C, or 001D artifacts and without authorizing EGO runtime, product, LLM/RAG,
relationship, emotion, user-memory, or Ego repository work.

Correct framing:

```text
Did 001D genuinely repair the exact 001C baseline legal-input blocker under
independent audit, while preserving 001C as historical blocker, avoiding
expected-label leakage, proving failure-path controls, and keeping claims bounded?
```

Wrong framing:

```text
Is EGO ready?
Can mainline admission begin?
Can 001B become positive evidence automatically?
Does 001D prove mechanism validity, agency, selfhood, consciousness, real
emotion, or relationship learning?
```

## Current Stage

Bounded independent audit of the already materialized 001D repair package.
This task may read 001B, 001C, 001D, parent contracts, source, tests, artifacts,
and git anchors. It must emit a new isolated 001E audit package and artifacts.

## Hypothesis

If 001D genuinely repaired the 001C baseline legal-input blocker, then an
independent audit can verify legal baseline inputs, equivalent-label exclusion,
runtime guard coverage, static forbidden-reference scan quality, baseline
invocation and independence, named failure-control failability, callable metric
provenance, revalidation quality, old-artifact immutability, negative-evidence
preservation, and claim ceiling preservation from 001D source and artifacts.

## Strongest Baseline Explanation

001D may still be a repair self-pass: it may produce internally consistent JSON
while baseline inputs contain equivalent expected-label information, guard
coverage misses a baseline generation path, failure controls are declared but
not fail-able, provenance points to wrappers rather than true producers, or
revalidation reports are copied or asserted instead of independently checked.

## Strongest Reason This Task May Be Invalid

This audit would be invalid if it only reran 001D and treated its own pass
report as proof, or if it converted the 001C blocker into a caveat.

## Baseline

Use 001C's blocker as the baseline negative evidence:

```text
baseline generator reads verifier_expected_action_id while generating baseline actions
missing explicit failure-path control for baseline_non_invocation
missing explicit failure-path control for missing_same_surface_positive_control
```

The audit must verify that 001D addresses exactly those blockers and does not
expand repair scope without explicit justification.

## Ablation

Audit whether 001D's ablation revalidation is independently supported by
callable reruns and not copied from 001B or asserted in summaries.

## Trace / Replay Requirement

Audit whether 001D behavior-causal replay recomputes behavior from
serialized_state + observation, and whether hash-only replay is not used for a
behavior claim.

## Acceptance Gate

Pass only if every required audit dimension is independently satisfied:
parent anchors, computed-evidence contract, 001C preservation, exact repair
scope, legal baseline input schema, equivalent-label exclusion, guard coverage,
static scan quality, baseline invocation and independence, both named failure
controls, callable metric provenance, ablation/leakage/manual-injection/
metadata-whitelist/replay/frozen-input/source-integrity/old-artifact checks,
negative evidence, scope, and claim ceiling.

## Stop Condition

Stop and report the most specific blocker if any of the required dimensions
fail. In particular, block on missing parent anchors, 001C rewrite, repair scope
expansion, baseline legal-input gap, equivalent-label leakage, runtime guard
gap, static scan gap, baseline invocation gap, baseline independence gap,
failure-path gap, metric provenance gap, ablation gap, leakage gap, replay gap,
unused frozen input, old artifact mutation, negative evidence rewrite, scope
leak, or claim inflation.

## Rollback Plan

If blocked, preserve 001E audit artifacts, do not rewrite 001D artifacts, do
not rewrite 001C artifacts, do not rewrite 001B artifacts, do not weaken 001A,
do not convert the 001C blocker into a caveat, do not restore 001B positive
status, do not enter the Ego repository, and repair only the exact new blocker
in a later bounded task.

## What This Cannot Prove

This cannot prove 001B positive evidence restoration, EGO readiness, EGO
mainline readiness, runtime admissibility, bridge readiness, companion
readiness, mechanism validity, theory validity, agency, selfhood,
consciousness, real emotion, real relationship learning, stable user benefit,
production readiness, or correctness of any future EGO runtime.
