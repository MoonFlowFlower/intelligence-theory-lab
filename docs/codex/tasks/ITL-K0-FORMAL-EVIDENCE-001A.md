# ITL-K0-FORMAL-EVIDENCE-001A

Status: BLOCKED ON ACCEPTED IMMUTABLE FREEZE / FORMAL RUN CARD

Auto-Remote-Anchor: forbidden

## Task identity and problem

- Task id: `ITL-K0-FORMAL-EVIDENCE-001A`
- Parent commit: `4e4700ca6e00b1a0e2dc3adf6a6e473b2f6ef6be`
- Problem: after construction and instrument validation, run one frozen system
  against frozen families, controls, rivals, protocols, budgets, heldout seal,
  and component mapping without tuning or importing mutable source.

## Layer, mainline, enabled state, trigger

Learning/adaptation measurement within an engineering mechanism-hypothesis
laboratory. Claim ceiling is component-scoped proxy evidence only. No EGO
mainline/runtime integration. Disabled until the exact immutable freeze is
accepted. Real trigger is the frozen H1 runner invoking the installed wheel/CLI
in a clean environment after heldout commitment verification/reveal.

## Preconditions

- canonical parent, H0, H1, Foundation, K0-R, and freeze are all locally banked
  and hash-pinned;
- callable H0/H1 acceptance states and every freeze-manifest field verify;
- freeze manifest verifies before every run;
- raw heldout seed reveal matches the H0 commitment and occurs only after freeze;
- no source, metric, threshold, generator, panel, adapter, checkpoint, optimizer,
  or dependency change occurs after reveal;
- formal runner imports only the installed frozen Ego wheel and frozen H1 runner
  bundle, never either working tree.

Failure of any precondition stops before scoring and emits a failure manifest.

## Formal protocols

### Fresh-init

For each family independently reset initial checkpoint, optimizer, memory,
replay buffer, kernel/RNG state, and history. Architecture, hyperparameters,
ABI, and budgets remain fixed. This can establish only bounded within-family
learnability by one architecture; it cannot alone set `V_transfer` present.

### Persistent-sequence

Retain checkpoint, optimizer, memory, replay buffer, kernel/RNG state across the
H0-frozen A/B/C orders and finite permutations. Required carriers: full
carryover, full fresh-init, checkpoint-only, replay-reset, and memory-reset.
Report forward transfer, retention/backward transfer, interference/forgetting,
order sensitivity, and bounded memory/buffer/compute growth.

`family_id`, split, order, latent rule, and heldout label remain evaluator-only.

## Arms and panels

Execute every frozen candidate, mapped causal control, shortcut control, and
rival arm under channel/access/budget parity. Required families include the full
graph/cache panel, no-update, shuffled outcomes, checkpoint swap, planner
bypass, replay-off/corruption, memory/source/history interventions, batched own
rule, from-scratch and carrier resets, plus the frozen rivals. An uninvoked arm
or unused seed/context/counterfactual pair blocks the affected result.

All ablations rerun episodes under real interventions. Replay recomputes actions
and metrics from serialized state plus observation; stored hashes alone are not
replay evidence.

Before candidate scoring in the exact formal environment/platform, rerun the H1
artificial-updater/no-update pair, observation-decodable and no-signal controls,
label-leak scanner positive control, replay-corruption detector, access-parity
audit, and heldout commitment verifier. Historical H1 reports or hash readback
are insufficient. Any failed global control stops candidate scoring; a mapped
control failure invalidates only its frozen dependent components.

## Two-axis component verdict

The formal runner emits one record each for `V_model`, `V_online`, `V_replay`,
`V_memory`, `V_transfer`, and `V_special` using the parent schema:

```text
evidence_state = NOT_TESTED | INVALID_INSTRUMENT | ABSENT | PRESENT_BOUNDED
comparison_state = NOT_APPLICABLE | RIVAL_NOT_RUN | RIVAL_SATURATED | RIVAL_SEPARATED
```

Rules:

- no global `K - max(C)` pass and no scalar route-success verdict;
- rival qualifier never upgrades/erases component evidence;
- invalid/unrun rival -> `RIVAL_NOT_RUN` with reason;
- underpowered/MDE failure -> `INVALID_INSTRUMENT`, not `ABSENT`;
- replay-off affects `V_replay` unless the frozen dependency matrix propagates
  replay-state dependence elsewhere;
- batched own-rule can deny online advantage but not learned-model existence;
- `V_memory.claim_subtype` is limited to `memory_conditioning/use` at K0;
- fresh-init alone cannot make `V_transfer=PRESENT_BOUNDED`;
- `V_special` is only relative to frozen panel P, budget B, distribution D and
  maps rival saturation to `ABSENT / RIVAL_SATURATED`;
- `NOT_TESTED` has null score provenance; every other evidence state has complete
  computed provenance.

Failure propagation must be produced by the frozen callable dependency resolver,
not hand-authored report prose.

## Hypothesis, strongest baseline, and falsifier

Hypothesis: the frozen reference kernel may show a non-empty subset of bounded,
ablation-sensitive component evidence across the frozen distributions.

Strongest baseline: legal history/graph/transition learners, candidate-own-rule
batch amortization, and frozen rival learners can absorb some or all effects.

Falsifier: mapped controls tie or reverse the candidate, transfer disappears
under proper carrier controls, or integrity dependencies fail. Such results are
valid negative/component-absent evidence, not a reason to tune/rerun.

Evidence still insufficient: even all components present under this card cannot
establish agency, autonomy, subjectivity, consciousness, emotion, functional
subject status, electronic life, companion readiness, or general intelligence.

## Collision record

### 1. One aggregate pass/fail
- Evidence: simple headline.
- Cheap match: strongest single control hides component distinctions.
- Falsifier: replay-off ties while no-update separates.
- Failure: semantic erasure; forbidden.

### 2. Per-family score table without interventions
- Evidence: behavioral performance.
- Cheap match: lookup/history/rival learners.
- Falsifier: component ablation has no directional effect.
- Failure: resemblance only.

### 3. Frozen component-vector run (selected)
- Evidence: causal component states plus independent rival qualifiers.
- Cheap match: full frozen controls/rivals.
- Falsifier: any required arm/input/provenance path unused or invalid.
- Expected failure: mixed vector, including absent/saturated components.

## Trace and computed-evidence contract

Per step record task/run/episode/context, protocol/carrier/arm, state hashes,
observation, legal actions, model prediction/uncertainty, candidate rankings,
selected typed action, outcome, prediction error, parameter/update delta,
memory/replay lineage, ablation, adapter/freeze hashes, and seeds. Each score
records producer function, input hashes, run id, seed/context/episode ids,
aggregation, code-path hash, and freeze manifest hash.

## Required artifacts

Only the frozen runner may produce:

```text
artifacts/ITL-K0-FORMAL-EVIDENCE-001A/
  run_manifest.json
  heldout_reveal.json
  heldout_commitment_verification.json
  formal_positive_control_report.json
  formal_negative_control_report.json
  formal_access_parity_report.json
  formal_scanner_report.json
  trace.jsonl
  fresh_init_report.json
  persistent_sequence_report.json
  component_verdicts.json
  baseline_comparison.json
  ablation_report.json
  replay_report.json
  leakage_report.json
  computed_evidence_provenance.json
  result.json
  claim_ceiling.txt
  failure_manifest.json  # whenever anything fails/is invalid/unused
```

Formal execution authorizes no source/test/card/threshold change.

## Acceptance gate

Accept an evidence packet only if all pre-run hashes match; heldout reveal
verifies; every frozen input/arm is invoked; formal-environment access parity,
leakage/scanner, provenance, and independent H1 controls are recomputed and
valid before candidate scoring; replay recomputes; both protocols complete;
dependency-scoped verdicts and rival qualifiers are callable; metrics meet
frozen power/MDE rules; artifacts parse and cross-reference; and no post-reveal
change or second logic path exists.

An accepted packet may contain only `ABSENT`, `INVALID_INSTRUMENT`, or saturated
results. Packet acceptance is evidence hygiene, not mechanism success.

## Stop conditions

Stop on hash/source/dependency drift; early/invalid heldout reveal; unused frozen
input; missing/invalid baseline or intervention; access asymmetry; leakage;
replay mismatch; threshold rescue; rerun/retune request; hand-written verdict;
working-tree import; or any scope expansion into EGO/UI/LLM/runtime.

## Rollback

Never overwrite a formal run. Preserve raw traces and failure manifest. An
implementation defect requires a new H1/freeze/run id; a semantic change requires
new H0/H1/freeze. Negative results are not rollback targets.

## Claim ceiling

Claims come only from frozen per-component templates:

- `V_model`: frozen learned outcome predictor was load-bearing under the named
  model interventions;
- `V_online`: online parameter updating had bounded incremental contribution;
- `V_replay`: replay training had bounded incremental contribution;
- `V_memory`: declared structured memory-conditioning/use was load-bearing;
- `V_transfer`: full carryover changed transfer/retention relative to carrier
  controls;
- `V_special`: candidate separated from panel P under budget B/distribution D.

Emit a template only for `PRESENT_BOUNDED` and append its comparison qualifier.
A combined "learned, ablation-sensitive, replay-valid" sentence is forbidden
unless every referenced component is independently present. No global pass, mechanism
validity, developmental generality outside the distributions, initiative,
agency, autonomy, subjectivity, consciousness, functional subject, electronic
life, AGI, EGO readiness, companion readiness, user benefit, or mainline effect.

## Expected changed files and commit policy

- card bank: `docs/codex/tasks/ITL-K0-FORMAL-EVIDENCE-001A.md`
- formal run: additions only for the exact artifact names in `Required
  artifacts`; undeclared filenames require a superseding card
- forbidden: all source/tests, old artifacts, cards/contracts, EGO paths,
  thresholds, push, tag, remote anchor.

Exact-path local card/evidence commits are separately authorized when
prerequisites and gates hold. This landing turn banks only the card. Remote
publication is forbidden.

## Next action / what this does not prove

No action until immutable freeze is accepted. This card defines a formal run; it
does not execute one or establish any component.
