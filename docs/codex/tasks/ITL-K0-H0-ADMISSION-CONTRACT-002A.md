# ITL-K0-H0-ADMISSION-CONTRACT-002A

Status: PHASE A BANKED CONTRACT PENDING ROUTE ENFORCEMENT / H0 DISABLED / NO H0 EXECUTION

Auto-Remote-Anchor: forbidden

## 1. Task identity and problem definition

- Task id: `ITL-K0-H0-ADMISSION-CONTRACT-002A`.
- Current stage: replacement/simplification of the H0 admission boundary before
  any H0 bank or implementation.
- Problem definition: the banked H0 card, Red-field addendum, and Red-field
  correction overlap and contain C1/C4 semantic conflicts. Continuing to layer
  overlays would make admission depend on implicit inheritance and literal
  reconciliation rather than one executable contract.
- Real objective: bank one self-contained effective H0 admission contract and
  then make route validation enforce it while H0 remains false pending
  independent review and a separate READY transition.
- This task does **not** execute H0, H1, K0-R, Freeze, Formal, scoring, or an
  experiment.

## 2. Layer, mainline, enabled state, and real trigger

- Current layer: engineering evidence-governance plus mechanism-hypothesis
  schema.
- Mainline target: repo-local ITL route control plane only. EGO runtime and EGO
  mainline are out of scope.
- Mainline integration status: none.
- Enabled-state requirement after enforcement: route remains
  `READY_TO_IMPLEMENT`; Foundation remains true; H0 becomes false; H1, K0-R,
  Freeze, and Formal remain false.
- Real-trigger evidence requirement: exact committed Phase-A card/contract
  object readback followed by callable contract, resolver, bijection, path
  authority, route-state, ledger, and event validation in Phase B.
- No live H0 trigger is authorized. The only observed trigger may be the local
  governance transition event
  `h0_admission_contract_002a_banked_review_required`.

## 3. Hypothesis, strongest baseline, falsifier, and insufficiency

- Hypothesis: a single explicit semantic source plus structured validators can
  make H0 admission total, exclusive, deterministic, and fail-closed without
  erasing component evidence when comparators match the candidate.
- Strongest baseline: a validator that only exact-matches a set of mutually
  contradictory literals. It can produce a green report without resolving the
  contract.
- Smallest falsifier: with all five evidence components
  `PRESENT_BOUNDED`, integrity valid, `CONTROL_EQUIVALENT`, and
  `RIVAL_SEPARATED`, the resolver must return one non-contradictory result:
  component evidence remains `PRESENT_BOUNDED`, while the panel-relative
  specialness claim is inadmissible. Any erased component evidence, a second
  result, or a fourth evidence axis falsifies the framing.
- Evidence still insufficient: schema validation, route validation, tests, and
  committed object pins do not establish H0 pass, instrument validity,
  headroom, learning, replay, memory, transfer, initiative, mechanism evidence,
  or mainline effect.

## 4. Collision record

### A. Another local overlay — rejected

- Evidence produced: another pinned prose/JSON delta.
- Strongest cheap match: add another exact literal to the validator.
- Leakage/hard-coding risk: high; current contradictions remain inherited.
- Smallest falsifier: a field not mentioned by the new overlay changes meaning.
- Expected failure mode: audit-Zeno with no increase in discriminative evidence.

### B. Keep string rules and widen only the file allowlist — rejected

- Evidence produced: new filenames pass path checks.
- Strongest cheap match: a static allowlist with no semantic resolver.
- Leakage/hard-coding risk: high; C1/C4 remain unresolved and comparison parity
  can still erase causal evidence.
- Smallest falsifier: the all-present/control-equivalent/rival-separated case
  yields conflicting terminals.
- Expected failure mode: apparent enforcement without total semantics.

### C. One consolidated executable admission contract — selected

- Evidence produced: a single explicit semantic source, callable resolver,
  exhaustive cross-field validation, tuple/contrast bijection validation, and
  exact path authority.
- Strongest cheap match: whole-document equality against a blessed JSON blob.
- Leakage/hard-coding risk: bounded by field-structured validation, mutation
  tests, and callable scenario resolution rather than a static verdict table.
- Smallest falsifier: delete or duplicate an atomic tuple, supply aggregate-only
  arm coverage, mutate a ceiling, authorize H0 during review, or present an
  unlisted H0 path; each must fail.
- Expected failure mode: contract incompleteness or non-total resolver, which is
  a stop rather than a reason to add another overlay.

Selection rationale: C is the only candidate that removes implicit inheritance
and makes conflicts executable. It tests governance/schema consistency, not a
mechanism or behavioral resemblance.

## 5. Frozen effective authority

The sole effective semantic source is:

`artifacts/ITL-K0-H0-ADMISSION-CONTRACT-002A/effective_h0_contract.json`

The old H0 card, Red addendum card/contract, and Red correction card/contract
remain immutable historical provenance at their committed pins. They have no
implicit residual authority: a field omitted from the effective contract is not
effective.

The effective contract must explicitly and completely freeze:

1. the five evidence components and their four-state `evidence_state`;
2. separate control and rival comparison axes;
3. `V_special` as a claim-admissibility predicate, never as evidence state;
4. causal absence proof kinds and underpower mappings;
5. component/panel/arm/role membership plus atomic contrast bijection schema;
6. access, firewall, leakage, determinism, power/MDE, trace/recompute,
   provenance, and integrity blast-radius rules;
7. the exact future H0 repo write allowlist and one allowed external custody
   side effect;
8. `h0_acceptance.json` sibling-hash and object-store readback rules;
9. separate exact claim ceilings for this admission task and future H0.

## 6. Baseline, ablation, trace/replay, and provenance requirements

- Baseline requirement: preserve random/majority/observation-only,
  history/lookup/nearest-neighbor, full graph/cache family, candidate-own-rule,
  matched no-update/no-replay, persistent carrier controls, and the finite rival
  panel under access parity.
- Ablation requirement: future evidence-state `ABSENT` may be produced only by
  a powered real intervention proving `CAUSAL_NO_CONTRIBUTION` or
  `CAUSAL_WRONG_SIGN`. Failure to reject and causal underpower do not prove
  absence.
- Trace/replay requirement: this admission task runs no episode. Its validation
  must freeze future recomputation from serialized state plus observation,
  distinct fresh-process determinism evidence, and dependency-scoped failure
  propagation; stored-hash comparison alone is insufficient.
- Computed-evidence provenance gate: every future score or decision must record
  producer function, input artifacts/hashes, run id, seed/context/episode ids,
  aggregation rule, and code-path hash. Baselines are independent callables;
  ablations rerun interventions; leakage includes a positive control; replay
  recomputes behavior. Any unused frozen input blocks the evidence claim.

## 7. Acceptance gate

Phase A accepts only if:

- exactly this card and `effective_h0_contract.json` are added;
- JSON parses and is self-contained with no implicit-inheritance clause;
- the old provenance pins read back from committed Git objects;
- staged scope is exact and `git diff --cached --check` passes;
- the Phase-A commit is read back with card blob, contract blob, and contract
  SHA-256.

Phase B accepts only if:

- exactly one L-024 and exactly one named event are appended;
- L-020 through L-023 and all prior event bytes remain unchanged;
- route state is `READY_TO_IMPLEMENT` with phase
  `FOUNDATION_READY_H0_ADMISSION_002A_REVIEW_REQUIRED`;
- Foundation is true, H0 is false, and every downstream child is false;
- the new pin is `BANKED_ENFORCED_REVIEW_REQUIRED` and old pins remain
  historical provenance only;
- callable structured resolver, exhaustive cross-field validator, atomic
  tuple/contrast bijection validator, and effective H0 path-authority validator
  pass their positive and mutation tests;
- focused tests and `routectl validate/status/dashboard` pass;
- staged scope is exact and `git diff --cached --check` passes.

Acceptance signal: local governance reports pass and committed object readback
matches. This signal authorizes review only, not H0.

## 8. Claim ceilings

- Admission task ceiling (exact): `consolidated H0 admission task-card/schema/control-plane enforcement only`.
- Future H0 ceiling (exact): `immutable instrument preregistration contract banked only`.

The two ceilings are non-interchangeable. Neither permits instrument validity,
component evidence, learning, replay, memory, transfer, initiative, mechanism,
agency, autonomy, subjectivity, consciousness, EGO readiness, user benefit, or
mainline effect.

## 9. Stop conditions

Stop if another overlay or fourth verdict axis is required; the resolver is not
total/exclusive/deterministic; H0/H1/K0-R/Freeze/Formal must become true; old
cards/contracts/ledger/event bytes must be changed; scope expands; any required
validator/test fails; or a cheap exact-literal baseline remains sufficient to
match the purported structured enforcement.

## 10. Rollback plan

- Before the Phase-A commit, remove only the two new Phase-A paths if acceptance
  fails.
- After the Phase-A commit, never rewrite its objects.
- If Phase B fails, preserve the Phase-A bank, leave H0 false, record the
  blocker, and do not start H0.
- Revert only a separately reviewed Phase-B commit if rollback is later
  authorized; do not rewrite historical route events or ledger entries.

## 11. Exact changed paths

Phase A additions only:

- `docs/codex/tasks/ITL-K0-H0-ADMISSION-CONTRACT-002A.md`
- `artifacts/ITL-K0-H0-ADMISSION-CONTRACT-002A/effective_h0_contract.json`

Phase B modifications only:

- `docs/research/FSP-STAGE-LEDGER.md`
- `src/route_state_machine_001a/state_machine.py`
- `src/route_state_machine_001a/validator.py`
- `tests/route_state_machine_001a/test_validator.py`
- `artifacts/ROUTE-STATE-MACHINE-001A/routes/K0-DUAL-TRACK-SUPERSESSION-001A/state.json`
- `artifacts/ROUTE-STATE-MACHINE-001A/routes/K0-DUAL-TRACK-SUPERSESSION-001A/events.jsonl`
- `artifacts/ROUTE-STATE-MACHINE-001A/program_state.json`
- `artifacts/ROUTE-STATE-MACHINE-001A/STATUS.md`
- `artifacts/ROUTE-STATE-MACHINE-001A/validation_report.json`

## 12. Forbidden changes

Do not modify the original H0 card, either Red card/contract, L-020 through
L-023, existing event lines, any H0/H1/K0-R/Freeze/Formal source/artifact, EGO
runtime/mainline, thresholds after results, remote refs, tags, or unlisted files.
Do not view or implement a K0-R candidate. No reset, stash, rebase, amend, push,
tag, or remote anchor is authorized.

## 13. Prior negative evidence

- RESIDUE/window dominance: shuffled/reverse replay matched the candidate and a
  small order-2 window model dominated, so window/history/cache controls remain
  mandatory (`307da776b20de615d73b54b4e28f3f97da98a816`).
- PET P0 hostile audit: high score without access parity and causal attribution
  remains non-attributive (`6948427ad4f58535db5b66c8b4bf656f1d772f77`).
- TLGP source/freeze drift: replayable outputs did not repair a source mismatch
  or tautological drift gate, so committed-object/ancestry/end-run provenance is
  mandatory (`63d5177480506f05c9783e00525177d4d048489e`).
- VOI 003A: fresh-seed powered comparison ended `STATIC_SUFFICIENT`; a strong
  falsifier collapse is attribution success, not automatic absence
  (`f2c19955528c746cd2de610679de3089ee52138b`).
- Ledger L-007, L-015/L-016, and L-017/L-018 preserve tested-scale negative
  evidence, the drift-aware fair-rival gate, and N2 graph-closure baseline
  equivalence. Schema sophistication cannot upgrade those results.

## 14. Next minimal closed-loop action

After both local commits, obtain independent review of the banked/enforced
contract. Only a separate bounded transition may change the new pin to a READY
status and set H0 true. The endpoint of this task is review-required governance,
not H0 readiness.

## 15. What this does not prove

This card and its enforcement do not prove H0, instrument validity, headroom,
any K0 component, learning, replay, memory, transfer, initiative, mechanism
validity, agency, autonomy, subjectivity, consciousness, EGO readiness,
production readiness, user benefit, or mainline effect.
