# Phase2B Program Needs Reframing Checkpoint 001A

Task id: `RESEARCH-CAMPAIGN-PHASE2B-PROGRAM-NEEDS-REFRAMING-CHECKPOINT-001A`

Source route-decision checkpoint:
`artifacts/research_campaign/phase2b_no_headroom_route_decision_001a.json`

Source route-decision audit:
`artifacts/research_campaign/phase2b_no_headroom_route_decision_audit_001a.json`

## Purpose

Open a bounded terminal-contract checkpoint after two audited candidate-free
no-headroom surfaces and an audited route-decision checkpoint.

This card tests whether the controller card's allowed terminal verdict
`program_needs_reframing` is justified by current machine-readable evidence:

- Phase 2 `MINIMAL-ENV-SPEC-001A` produced `no_headroom_baseline_saturated`;
- Phase2B `phase2b_minimal_env_reframe_spec_001a` also produced
  `no_headroom_baseline_saturated`;
- the audited route decision blocked candidates, Phase 3, route tournament,
  runtime/EGO mainline, push, tag, remote anchor, and program-completion claims
  until a separately bounded redesign, closure, downgrade, or terminal
  assessment checkpoint exists.

This card authorizes only a local governance/readback terminal assessment. It
does not authorize a new environment spec, new harness, candidate mechanism,
Phase 3, route tournament, runtime/EGO mainline, UI, LLM, AIRI, external
service, deployment, commit, push, tag, or remote anchor.

## Current Layer

`engineering_implementation + mechanism_hypothesis_governance`

This is a terminal-contract governance checkpoint. It is not a mechanism
experiment, not learning/adaptation evidence, not subjectivity validation, and
not philosophical consciousness work.

## Bounded Audit Before Execution

Real objective: decide whether the current campaign branch must be recorded as
`program_needs_reframing` under the standing controller contract, without
inflating no-headroom negative evidence into mechanism failure or mechanism
success.

Problem-definition risk: a terminal checkpoint can become progress theater if
it merely restates stale no-headroom evidence, or claim inflation if it treats a
reframing verdict as proof that the broader theory is false or solved.

Strongest baseline explanation: both candidate-free surfaces exposed enough
visible structure for the fair `count_table` baseline to match the visible
oracle at macro F1 `1.0` inside the `0.03` equivalence band.

Strongest invalidating reason: if the two surfaces are not actually consecutive
failed phase loops, or if the latest route decision is not recorded and audited,
then `program_needs_reframing` is not justified.

Falsifier for terminal recording: if plan/progress/scorecard/ledger disagree,
if any source negative-evidence artifact is missing or stale, if a candidate or
Phase 3 path has already been opened, or if the terminal verdict lacks reviewer
audit, this checkpoint must remain `active_not_complete`.

Evidence that is still insufficient:

- this task-card creation alone;
- a local JSON parse pass alone;
- a single reviewer response that is not recorded in artifacts and ledger;
- two no-headroom reports without the route-decision audit;
- any narrative claim that no future representation could have headroom.

Mechanism-vs-behavior classification: this task tests no mechanism. It records
a governance verdict about the current problem representation and campaign
branch.

Hard-coding / leakage / weak-baseline checks:

- no candidate mechanism;
- no Phase 3 search;
- no route tournament;
- no new spec or harness execution;
- no weakening or reinterpreting the `count_table` saturation result;
- no mutation or refreeze of prior specs;
- no hidden terminal verdict outside the terminal checkpoint artifact,
  audit artifact, ledger, scorecard, and progress checkpoint;
- no runtime, EGO mainline, UI, LLM, AIRI, deployment, commit, push, tag, or
  remote anchor.

## Problem Definition

The campaign has reached a repeated no-headroom boundary:

```text
Phase 2 verdict = no_headroom_baseline_saturated
Phase 2 strongest fair baseline = count_table
Phase 2 strongest fair baseline macro F1 = 1.0
Phase 2 visible-channel oracle macro F1 = 1.0

Phase2B verdict = no_headroom_baseline_saturated
Phase2B strongest fair baseline = count_table
Phase2B strongest fair baseline macro F1 = 1.0
Phase2B visible-channel oracle macro F1 = 1.0

Latest route-decision reviewer verdict = success_reached
```

The controller card permits `program_needs_reframing` when two consecutive
phase loops failed to increase discriminative evidence, and the reframing
decision is recorded in artifacts and ledger.

This checkpoint asks whether that condition is now met for the current campaign
branch. It must not claim all possible mechanism routes are exhausted unless a
separate route-exhaustion checkpoint proves that stronger condition.

## Mainline Target

None. This task has no runtime, EGO mainline, UI, LLM, AIRI, external service,
deployment, product, or companion target.

## Enabled-State Requirement

Only local terminal-governance artifacts and campaign bookkeeping are enabled:

- create a terminal/reframing checkpoint artifact;
- create a focused validation artifact;
- create or update a read-only reviewer audit artifact;
- update `docs/research_campaign/plan.md`;
- update `docs/OVERALL_PROGRESS.md`;
- update `artifacts/research_campaign/stage_scorecard.json`;
- update `artifacts/research_campaign/goal_stage_audit_loop_validation_001a.json`;
- append to `artifacts/research_campaign/experiment_log.jsonl`.

No new environment spec, harness, source implementation, tests, candidate,
Phase 3, route tournament, runtime/EGO mainline, push, tag, commit, or remote
anchor is enabled.

## Real-Trigger Evidence Requirement

Execution must read and record:

- repo root, branch, HEAD, upstream/ahead-behind, and
  `git status --short --branch`;
- this task card;
- `docs/research_campaign/goal_stage_audit_loop_001a.md`;
- `docs/research_campaign/plan.md`;
- `docs/OVERALL_PROGRESS.md`;
- `artifacts/research_campaign/stage_scorecard.json`;
- `artifacts/research_campaign/experiment_log.jsonl`;
- `artifacts/research_campaign/phase2_no_headroom_negative_evidence_001a.json`;
- `artifacts/research_campaign/phase2b_no_headroom_negative_evidence_001a.json`;
- `artifacts/research_campaign/phase2b_no_headroom_route_decision_001a.json`;
- `artifacts/research_campaign/phase2b_no_headroom_route_decision_audit_001a.json`;
- current memory/search evidence that no-headroom blocks candidate work until
  a new bounded representation is defined.

## Hypothesis

If two consecutive candidate-free phase loops are audited no-headroom and the
audited route decision blocks candidate/Phase 3 continuation, then the current
campaign branch should record `program_needs_reframing` rather than continue
patching the same surface family.

## Strongest Baseline

The strongest false explanation remains that the current task family is a
surface-design problem rather than a mechanism-discovery problem: lookup,
count-table, graph-cache, transition-table, successor-map, FSM planner,
episodic traversal, trace-only replay, full-bundle decoder,
serialized-state decoder, exhaustive legal query, or observation-only decoder
behavior can match the visible oracle under the same budget.

## Ablation Requirement

This terminal-governance checkpoint runs no mechanism ablation.

Any future representation redesign must predeclare claim-critical ablations and
must rerun real interventions before any candidate work can be authorized.

## Trace / Replay Requirement

This checkpoint preserves that replay evidence cannot rescue a saturated
surface. Future executable work must retain replay recomputation from:

```text
serialized_state + current_observation + legal_action_or_query_schema + budget_state
```

Hash equality and stored report agreement remain insufficient.

## Computed-Evidence Provenance Gate

The terminal/reframing checkpoint artifact must be derived from current
machine-readable evidence, not narrative recollection. It must include:

- producer command or structured readback source;
- input artifact paths and hashes;
- consumed-by-decision flags;
- the two no-headroom surfaces and their strongest fair baseline scores;
- route-decision audit status;
- terminal verdict rule;
- whether the controller-card `program_needs_reframing` condition is met;
- explicit forbidden interpretations.

## Acceptance Gate

This checkpoint is accepted only if:

- this task card exists under `docs/research_campaign/`;
- the terminal/reframing artifact exists and records both no-headroom loops;
- source hashes for the negative-evidence and route-decision artifacts match
  live files;
- plan/progress/scorecard/goal-validation/ledger agree on terminal/reframing
  state;
- candidate mechanism, Phase 3, route tournament, runtime/EGO mainline, push,
  tag, commit, and remote anchor remain false/blocked;
- focused validation passes;
- read-only reviewer audit returns `success_reached`, or any failure is
  preserved and repaired without widening scope.

`program_needs_reframing` may be recorded as a terminal verdict only after the
focused validation and reviewer audit are recorded in artifacts and ledger.

## Claim Ceiling

This checkpoint can claim only that the current campaign branch needs problem
representation reframing after repeated candidate-free no-headroom, if and only
if the acceptance gate passes.

It does not prove mechanism validity, self-awareness, subjective experience,
real emotion, autonomy, agency success, EGO readiness, companion readiness,
runtime readiness, user benefit, or mainline effect. It does not prove all
possible routes are exhausted.

## Stop Conditions

Stop and record a blocker if:

- any source evidence artifact is missing, unparsable, or hash-mismatched;
- plan/progress/scorecard/ledger disagree after repair;
- a candidate, Phase 3 search, route tournament, new harness, new spec
  execution, runtime/EGO mainline path, UI, LLM, AIRI, external service,
  deployment, commit, push, tag, or remote anchor is attempted;
- old no-headroom evidence is rewritten into a pass;
- baseline families are weakened or thresholds tuned after saturation;
- a stronger route-exhaustion or candidate-validation terminal verdict is
  claimed without its own evidence chain;
- the terminal verdict is recorded without reviewer audit.

## Rollback Plan

Rollback this checkpoint only by reverting:

- `docs/research_campaign/phase2b_program_needs_reframing_checkpoint_001a.md`;
- `artifacts/research_campaign/phase2b_program_needs_reframing_checkpoint_001a.json`;
- `artifacts/research_campaign/phase2b_program_needs_reframing_checkpoint_validation_001a.json`;
- `artifacts/research_campaign/phase2b_program_needs_reframing_checkpoint_audit_001a.json`;
- the matching plan/progress/scorecard/goal-validation/ledger updates.

Do not delete or rewrite Phase 0, Phase 1, Phase 2, Phase2B, failed audit,
repair, no-headroom, route-decision, or historical evidence artifacts.

## Expected Changed Files

- `docs/research_campaign/phase2b_program_needs_reframing_checkpoint_001a.md`
- `artifacts/research_campaign/phase2b_program_needs_reframing_checkpoint_001a.json`
- `artifacts/research_campaign/phase2b_program_needs_reframing_checkpoint_validation_001a.json`
- `artifacts/research_campaign/phase2b_program_needs_reframing_checkpoint_audit_001a.json`
- `docs/research_campaign/plan.md`
- `docs/OVERALL_PROGRESS.md`
- `artifacts/research_campaign/stage_scorecard.json`
- `artifacts/research_campaign/goal_stage_audit_loop_validation_001a.json`
- `artifacts/research_campaign/experiment_log.jsonl`

## Forbidden Changes

- No source implementation changes.
- No test changes.
- No candidate mechanism implementation.
- No Phase 3 mechanism search.
- No route tournament.
- No new environment spec or harness execution.
- No formal Gate execution.
- No runtime, EGO mainline, UI, LLM, AIRI, external service, deployment,
  commit, push, tag, or remote anchor.
- No mutation, normalization, amendment, or refreeze of prior specs.
- No rewriting prior failed artifacts into passes.
- No consciousness, real-emotion, autonomy, EGO-readiness, companion-readiness,
  mechanism-validity, or route-exhaustion claim.

## Auto-Remote-Anchor

`forbidden`

## Immediate Next Action After This Card

Create the terminal/reframing checkpoint artifact from current
machine-readable Phase 2, Phase2B, and route-decision evidence, then run
focused validation. If validation passes, run read-only reviewer audit.
Candidates, Phase 3, route tournament, runtime/EGO mainline, push, tag, commit,
and remote anchor remain blocked.
