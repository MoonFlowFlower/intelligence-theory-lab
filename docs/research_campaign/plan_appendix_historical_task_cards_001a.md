# Plan Appendix Historical Task Cards 001A

Captured at: 2026-06-18T07:21:00.3633766-05:00

Source plan: `docs/research_campaign/plan.md`

Pre-compaction SHA-256: `b960eaf54f5dd4ef33d7e6435b00491fec72a162139cd50e33c4c7a1c346b8f0`

Pre-compaction line count: `1245`

Task card: `docs/research_campaign/plan_representation_compaction_task_card_001a.md`

Claim ceiling: append-only preservation of the pre-compaction plan text and
section inventory only. This appendix is not mechanism evidence, Phase 3
authorization, route exhaustion proof, or program completion.

Snapshot preservation level: text-level historical snapshot with original
pre-compaction SHA-256 recorded. Final validation records that the fenced snapshot
is not byte-exact because the first transform trimmed trailing whitespace; this is
preserved as a caveat, not rewritten into a byte-exact pass.

## Section Inventory

- line 5: ## Program Goal
- line 21: ## Standing Governance Card
- line 39: ## Current Phase
- line 108: ## Superseded Interrupted Checkpoint: Phase2B Program Needs Reframing 001A
- line 123: ## Prior Checkpoint: Phase2B No-Headroom Route Decision 001A
- line 199: ## Prior Checkpoint: Phase2B No-Headroom Preservation Reframing 001A
- line 220: ## Prior Result: Phase2B Candidate-Free Headroom 001A
- line 248: ## Prior Task Card: Phase2B Spec-Freeze / Readback Execution 001A
- line 308: ## Prior Task Card: Phase2B Start-State / Spec-Freeze Readback 001A
- line 366: ## Prior Task Card: Phase2B Execution Start-State / Spec-Freeze 001A
- line 424: ## Prior Task Card: Phase2B Candidate-Free Headroom 001A
- line 477: ## Prior Task Card: Phase2B Minimal Environment Reframe Spec 001A
- line 595: ## Active Task Card: Phase 2 No-Headroom Reframing 001A
- line 699: ## Active Task Card: Phase 2 Baseline-First Headroom 001A
- line 820: ## Phase 2 Execution Result
- line 874: ## Phase 0 Closeout Result
- line 889: ## Phase 1 Audit Result
- line 905: ## Completed Task Card: Phase 1 Problem Formalization 001A
- line 1007: ## Historical Task Card: Phase 0 Recovery Closeout 001A
- line 1099: ## Phase 0 Task Card
- line 1169: ## Campaign Loop Rules
- line 1181: ## Research Phases
- line 1212: ## Standing Anti-False-Pass Rules
- line 1224: ## Next Minimal Closed-Loop Action

## Full Pre-Compaction Plan Snapshot

`````markdown
# Strict Intelligence Mechanism Research Campaign Plan

Last updated: 2026-06-18T07:17:15-05:00

## Program Goal

Build a long-running, falsifiable, recovery-safe research campaign for finding
and eliminating candidate mechanisms that could support bounded functional-subject
proxies: self-modeling, affect/value regulation, active exploration, long-term
learning, and self/environment boundary tracking.

This campaign does not aim to prove consciousness, subjective experience, real
emotion, autonomy, electronic life, AGI, EGO readiness, companion readiness, or
stable user benefit. Those claims remain forbidden unless a later bounded task
defines computed evidence strong enough to support a narrower proxy claim.

Program goal status: `active_not_complete`.

Program terminal condition: `not_met`.

## Standing Governance Card

Task id: `RESEARCH-CAMPAIGN-GOAL-STAGE-AUDIT-LOOP-001A`

Card:
`docs/research_campaign/goal_stage_audit_loop_001a.md`

Purpose: define the strict phase-by-phase loop and program terminal contract for
opening a phase, recording state before work, executing only the bounded task
surface, writing ledger and progress immediately, running a read-only
reviewer/subagent audit, preserving failures, repairing only the bounded failure
surface, re-auditing until `success_reached` or `needs_reframing`, then opening
the next phase only after the prior audit pass is recorded in artifacts.

This standing card does not execute Phase 2, run a baseline battery, implement a
candidate mechanism, record a chat-only reviewer verdict as evidence, or
complete the program goal.

## Current Phase

Task id:
`RESEARCH-CAMPAIGN-PHASE2C-HIDDEN-LATENT-HELDOUT-SURFACE-CONTRACT-001A`

Source task id:
`RESEARCH-CAMPAIGN-PHASE2B-NO-HEADROOM-ROUTE-DECISION-001A`

Task card:
`docs/research_campaign/phase2c_hidden_latent_heldout_surface_contract_task_card_001a.md`

Surface contract:
`docs/research/phase2c_hidden_latent_heldout_surface_contract_001a.md`

Contract artifact:
`artifacts/research_campaign/phase2c_hidden_latent_heldout_surface_contract_001a.json`

Validation artifact:
`artifacts/research_campaign/phase2c_hidden_latent_heldout_surface_contract_validation_001a.json`

Source route-decision artifact:
`artifacts/research_campaign/phase2b_no_headroom_route_decision_001a.json`

Source route-decision audit:
`artifacts/research_campaign/phase2b_no_headroom_route_decision_audit_001a.json`

Superseded interrupted checkpoint:
`docs/research_campaign/phase2b_program_needs_reframing_checkpoint_001a.md`

Layer: engineering implementation + mechanism-hypothesis governance.

Stage goal: create a bounded problem-representation redesign contract whose
future surface requires learning, exploration, or memory over hidden latent
structure and heldout task families, rather than merely making the task larger.

Measured route input: Phase 2 and Phase2B both produced
`no_headroom_baseline_saturated`, with visible-channel oracle macro F1 `1.0`,
strongest fair baseline `count_table` macro F1 `1.0`, and equivalence band
`0.03`.

Validation state: focused local validation passed pending read-only reviewer
audit. The interrupted
`program_needs_reframing` checkpoint is superseded by the user design
correction and is not terminal evidence.

Mainline integration status: none.

Enabled status: local design-contract checkpoint only. No source
implementation, tests, candidate mechanism, Phase 3, route tournament, new
harness execution, runtime/EGO mainline, push, tag, commit, or remote anchor is
enabled.

Real trigger evidence: audited Phase 2 and Phase2B no-headroom artifacts, the
audited Phase2B no-headroom route-decision artifact, and the user design
correction requiring hidden latent state, heldout surfaces, interventions, and
preserved strong baselines.

Claim ceiling: problem-representation redesign contract only. This is no
mechanism validity, learning/adaptation success, subjectivity, consciousness,
real emotion, autonomy, EGO readiness, companion readiness, mainline-effect,
route-exhaustion, terminal-verdict, or candidate-validation evidence.

Auto-Remote-Anchor: forbidden.

Next minimal closed-loop action: run read-only reviewer audit of the validated
hidden-latent heldout surface contract. Candidate mechanisms, Phase 3, route
tournament, harness execution, runtime/EGO mainline, push, tag, commit, remote
anchor, terminal verdicts, and stronger route-exhaustion claims remain blocked.

## Superseded Interrupted Checkpoint: Phase2B Program Needs Reframing 001A

Task id:
`RESEARCH-CAMPAIGN-PHASE2B-PROGRAM-NEEDS-REFRAMING-CHECKPOINT-001A`

Status: superseded by user design correction before reviewer audit.

Reason: the next useful lesson is not to record an early terminal
`program_needs_reframing` verdict. It is to design a surface where visible
surface baselines cannot solve heldout latent structure without learning,
exploration, memory, transfer, and intervention sensitivity.

Claim ceiling: interrupted local checkpoint only. It is not terminal evidence,
not reviewer-audited, and not a program-completion or route-exhaustion claim.

## Prior Checkpoint: Phase2B No-Headroom Route Decision 001A

Task id:
`RESEARCH-CAMPAIGN-PHASE2B-NO-HEADROOM-ROUTE-DECISION-001A`

Source task id:
`RESEARCH-CAMPAIGN-PHASE2B-NO-HEADROOM-PRESERVATION-REFRAMING-001A`

Task card:
`docs/research_campaign/phase2b_no_headroom_route_decision_task_card_001a.md`

Route-decision artifact:
`artifacts/research_campaign/phase2b_no_headroom_route_decision_001a.json`

Validation artifact:
`artifacts/research_campaign/phase2b_no_headroom_route_decision_task_card_validation_001a.json`

Source Phase2B preservation audit:
`artifacts/research_campaign/phase2b_no_headroom_preservation_reframing_audit_001a.json`

Source Phase 2 negative evidence:
`artifacts/research_campaign/phase2_no_headroom_negative_evidence_001a.json`

Source Phase2B negative evidence:
`artifacts/research_campaign/phase2b_no_headroom_negative_evidence_001a.json`

Layer: engineering implementation + mechanism-hypothesis governance.

Stage goal: open a bounded route-decision checkpoint after two audited
candidate-free no-headroom surfaces.

Measured route input: Phase 2 and Phase2B both produced
`no_headroom_baseline_saturated`.

Measured scores: both surfaces had visible-channel oracle macro F1 `1.0`;
both had strongest fair baseline `count_table` macro F1 `1.0`; equivalence
band `0.03`.

Validation state: task card and route-decision artifact opened. Focused dry
validation passed for required task-card clauses, repeated no-headroom inputs,
blocked candidate/Phase 3 flags, campaign JSON parseability, and ledger JSONL
parseability. Laplace read-only reviewer audit returned
`needs_more_implementation` for stale bookkeeping/readback fields only; the
finding is preserved, the bounded repair was applied, and the re-audit returned
`success_reached`. The source preservation/reframing checkpoint is
reviewer-audited `success_reached` after two preserved
stale-readback/provenance repair findings.

Mainline integration status: none.

Enabled status: local route-decision artifact reviewer-audited success. No
candidate mechanism, Phase 3, route tournament, new environment spec execution,
new harness execution, runtime/EGO mainline, push, tag, or remote anchor is
enabled.

Real trigger evidence: audited Phase 2 and Phase2B no-headroom artifacts,
preservation/reframing records, and reviewer-audit success were read and hashed
into the route-decision artifact.

Claim ceiling: route-governance decision after repeated candidate-free
no-headroom only. This is no mechanism validity, subjectivity, consciousness,
real emotion, autonomy, EGO readiness, companion readiness, mainline-effect, or
program-completion evidence.

Auto-Remote-Anchor: forbidden.

Source preservation/reframing reviewer audit status: `success_reached`,
recorded in
`artifacts/research_campaign/phase2b_no_headroom_preservation_reframing_audit_001a.json`.

Next minimal closed-loop action: if the campaign continues, open only a
separately bounded problem-representation redesign, surface-family closure, or
claim-downgrade checkpoint. Candidate mechanisms, Phase 3, route tournament,
runtime/EGO mainline, push, tag, remote anchor, and program completion remain
blocked.

## Prior Checkpoint: Phase2B No-Headroom Preservation Reframing 001A

Task id:
`RESEARCH-CAMPAIGN-PHASE2B-NO-HEADROOM-PRESERVATION-REFRAMING-001A`

Task card:
`docs/research_campaign/phase2b_no_headroom_preservation_reframing_001a.md`

Negative-evidence record:
`artifacts/research_campaign/phase2b_no_headroom_negative_evidence_001a.json`

Preservation/reframing artifact:
`artifacts/research_campaign/phase2b_no_headroom_preservation_reframing_001a.json`

Reviewer audit status: `success_reached`, recorded in
`artifacts/research_campaign/phase2b_no_headroom_preservation_reframing_audit_001a.json`.

Claim ceiling: Phase2B no-headroom negative-evidence preservation and
reframing only. The audited checkpoint does not authorize candidates, Phase 3,
route tournament, runtime/EGO mainline, push, tag, or remote anchor.

## Prior Result: Phase2B Candidate-Free Headroom 001A

Task id: `RESEARCH-CAMPAIGN-PHASE2B-CANDIDATE-FREE-HEADROOM-001A`

Source task id:
`RESEARCH-CAMPAIGN-PHASE2B-CANDIDATE-FREE-HARNESS-RUN-001A`

Task card:
`docs/research_campaign/phase2b_candidate_free_harness_run_task_card_001a.md`

Harness source:
`src/phase2b_candidate_free_headroom_001a/`

Harness artifacts:
`artifacts/phase2b_candidate_free_headroom_001a/`

Campaign result:
`artifacts/research_campaign/phase2b_candidate_free_headroom_001a.json`

Validation artifact:
`artifacts/research_campaign/phase2b_candidate_free_headroom_validation_001a.json`

Reviewer audit status: `success_reached`, recorded in
`artifacts/research_campaign/phase2b_candidate_free_headroom_audit_001a.json`.

Claim ceiling: Phase2B candidate-free baseline-first headroom evidence only.
The audited verdict is no-headroom, not mechanism evidence.

## Prior Task Card: Phase2B Spec-Freeze / Readback Execution 001A

Task id: `RESEARCH-CAMPAIGN-PHASE2B-SPEC-FREEZE-READBACK-EXECUTION-001A`

Task card:
`docs/research_campaign/phase2b_spec_freeze_readback_execution_task_card_001a.md`

Source task card:
`docs/research_campaign/phase2b_start_state_spec_freeze_readback_task_card_001a.md`

Source spec draft:
`docs/research/phase2b_minimal_env_reframe_spec_001a.md`

Problem definition: the Phase2B start-state/spec-freeze readback task card is
validated and reviewer-audited, but no freeze manifest, start-state readback
result, harness implementation, oracle score, baseline score, replay score,
ablation score, leakage score, candidate mechanism, or Phase 3 work is
authorized. This task card opens a future spec-freeze/readback execution
checkpoint while keeping all freeze/readback and execution paths disabled.

Current stage/layer: engineering implementation + mechanism-hypothesis
governance. This is task-card opening only, not spec freeze, start-state
readback execution, or Phase2B execution.

Mainline target: none. No runtime or EGO mainline target.

Enabled-state requirement: only task-card drafting, bookkeeping, and focused
validation are enabled. Spec freeze, start-state readback execution, Phase2B
execution, oracle scoring, baseline execution, candidate mechanisms, Phase 3,
route tournament, Gate, runtime, EGO mainline, UI, LLM, AIRI, external
services, deployment, push, tag, and remote anchor remain disabled.

Acceptance gate for this checkpoint:

- this task card exists;
- it references the audited Phase2B start-state/spec-freeze readback task card;
- it references the Phase2B spec draft;
- it preserves Phase 2 no-headroom negative evidence;
- it states no freeze file or readback result is created in this checkpoint;
- it requires future freeze/readback before harness implementation or
  execution;
- it preserves candidate-free baseline-first headroom measurement before
  candidate work;
- it preserves the full saturation-family baseline battery;
- `phase2b_spec_freeze_authorized = false`;
- `phase2b_start_state_readback_authorized = false`;
- `phase2b_execution_authorized = false`;
- `candidate_mechanism_run = false`;
- `phase3_opened = false`;
- no `src/`, `tests/`, freeze file, readback result, harness output,
  candidate, runtime/EGO mainline, push, tag, or remote anchor is touched.

Claim ceiling: Phase2B spec-freeze/readback execution task-card reviewer-audit
success only.
No spec freeze, start-state readback execution, Phase2B execution, measured
headroom, self-awareness, subjective experience, real emotion, autonomy, agency
success, EGO readiness, companion readiness, or mainline-effect claim.

Auto-Remote-Anchor: forbidden.

## Prior Task Card: Phase2B Start-State / Spec-Freeze Readback 001A

Task id: `RESEARCH-CAMPAIGN-PHASE2B-START-STATE-SPEC-FREEZE-READBACK-001A`

Task card:
`docs/research_campaign/phase2b_start_state_spec_freeze_readback_task_card_001a.md`

Source task card:
`docs/research_campaign/phase2b_execution_start_state_spec_freeze_task_card_001a.md`

Source spec draft:
`docs/research/phase2b_minimal_env_reframe_spec_001a.md`

Problem definition: the Phase2B execution start-state/spec-freeze task card is
validated and reviewer-audited, but no freeze manifest, start-state readback
result, harness implementation, oracle score, baseline score, replay score,
ablation score, leakage score, candidate mechanism, or Phase 3 work is
authorized. This task card opens a future freeze/readback checkpoint while
keeping all freeze/readback and execution paths disabled.

Current stage/layer: engineering implementation + mechanism-hypothesis
governance. This is task-card reviewer-audit only, not spec freeze or start-state
readback execution.

Mainline target: none. No runtime or EGO mainline target.

Enabled-state requirement: only task-card audit success is recorded. Spec
freeze, start-state readback execution, Phase2B execution, oracle
scoring, baseline execution, candidate mechanisms, Phase 3, route tournament,
Gate, runtime, EGO mainline, UI, LLM, AIRI, external services, deployment, push,
tag, and remote anchor remain disabled.

Acceptance gate for this checkpoint:

- it references the audited Phase2B execution start-state/spec-freeze task
  card;
- it references the Phase2B spec draft;
- it preserves Phase 2 no-headroom negative evidence;
- it states no freeze file or readback result is created in this checkpoint;
- it requires future freeze/readback before harness implementation or
  execution;
- it preserves candidate-free baseline-first headroom measurement before
  candidate work;
- it preserves the full saturation-family baseline battery;
- `phase2b_spec_freeze_authorized = false`;
- `phase2b_start_state_readback_authorized = false`;
- `phase2b_execution_authorized = false`;
- `candidate_mechanism_run = false`;
- `phase3_opened = false`;
- no forbidden future freeze/readback path is touched.

Claim ceiling: Phase2B start-state/spec-freeze readback task-card reviewer-audit
success only. No spec freeze, start-state readback execution, Phase2B execution, measured
headroom, self-awareness, subjective experience, real emotion, autonomy, agency
success, EGO readiness, companion readiness, or mainline-effect claim.

Auto-Remote-Anchor: forbidden.

## Prior Task Card: Phase2B Execution Start-State / Spec-Freeze 001A

Task id: `RESEARCH-CAMPAIGN-PHASE2B-EXECUTION-START-STATE-SPEC-FREEZE-001A`

Task card:
`docs/research_campaign/phase2b_execution_start_state_spec_freeze_task_card_001a.md`

Source task card:
`docs/research_campaign/phase2b_candidate_free_headroom_task_card_001a.md`

Source spec draft:
`docs/research/phase2b_minimal_env_reframe_spec_001a.md`

Problem definition: the Phase2B candidate-free headroom task card is validated
and reviewer-audited, but no freeze, start-state validation, harness
implementation, oracle score, baseline score, replay score, ablation score,
leakage score, candidate mechanism, or Phase 3 work is authorized. This task
card opens a future start-state/spec-freeze checkpoint while keeping all
execution paths disabled.

Current stage/layer: engineering implementation + mechanism-hypothesis
governance. This is task-card opening only, not spec freeze or execution.

Mainline target: none. No runtime or EGO mainline target.

Enabled-state requirement: only task-card drafting, bookkeeping, and focused
validation are enabled. Spec freeze, Phase2B execution, oracle scoring,
baseline execution, candidate mechanisms, Phase 3, route tournament, Gate,
runtime, EGO mainline, UI, LLM, AIRI, external services, deployment, push, tag,
and remote anchor remain disabled.

Acceptance gate for this checkpoint:

- task card exists;
- it references the audited Phase2B candidate-free headroom task card;
- it references the Phase2B spec draft;
- it preserves Phase 2 no-headroom negative evidence;
- it states no freeze file is created in this checkpoint;
- it requires future freeze/readback before harness implementation or
  execution;
- it preserves candidate-free baseline-first headroom measurement before
  candidate work;
- `phase2b_spec_freeze_authorized = false`;
- `phase2b_execution_authorized = false`;
- `candidate_mechanism_run = false`;
- `phase3_opened = false`;
- plan/progress/scorecard/ledger agree on task-card opening or validation
  state;
- no `src/`, `tests/`, freeze file, harness output, candidate, runtime/EGO
  mainline, push, tag, or remote anchor is touched.

Claim ceiling: Phase2B execution start-state/spec-freeze task-card opening
only. No spec freeze, Phase2B execution, measured headroom, self-awareness,
subjective experience, real emotion, autonomy, agency success, EGO readiness,
companion readiness, or mainline-effect claim.

Auto-Remote-Anchor: forbidden.

## Prior Task Card: Phase2B Candidate-Free Headroom 001A

Task id: `RESEARCH-CAMPAIGN-PHASE2B-CANDIDATE-FREE-HEADROOM-001A`

Task card:
`docs/research_campaign/phase2b_candidate_free_headroom_task_card_001a.md`

Source spec draft:
`docs/research/phase2b_minimal_env_reframe_spec_001a.md`

Problem definition: Phase 2 measured no headroom on `MINIMAL-ENV-SPEC-001A`.
The audited Phase2B reframe spec defines a stricter candidate-free environment
surface, but it is not executable evidence. This task card opens the future
candidate-free headroom measurement step while keeping execution unauthorized
until focused validation and read-only reviewer audit succeed.

Current stage/layer: engineering implementation + mechanism-hypothesis
governance. This is task-card opening only, not harness execution.

Mainline target: none. No runtime or EGO mainline target.

Enabled-state requirement: only task-card drafting, bookkeeping, and focused
validation are enabled. Phase2B execution, oracle scoring, baseline execution,
candidate mechanisms, Phase 3, route tournament, Gate, runtime, EGO mainline,
UI, LLM, AIRI, external services, deployment, push, tag, and remote anchor
remain disabled.

Acceptance gate for this checkpoint:

- task card exists;
- it references the audited Phase2B spec draft;
- it preserves Phase 2 no-headroom negative evidence;
- it requires future spec freeze/readback before execution;
- it requires candidate-free baseline-first headroom measurement before
  candidate work;
- it preserves the full saturation-family baseline battery;
- it requires leakage positive controls, replay recomputation, ablations, and
  computed provenance consumed by final verdict;
- `phase2b_execution_authorized = false`;
- `candidate_mechanism_run = false`;
- `phase3_opened = false`;
- plan/progress/scorecard/ledger agree on task-card opening or validation
  state;
- no `src/`, `tests/`, harness output, candidate, runtime/EGO mainline, push,
  tag, or remote anchor is touched.

Claim ceiling: Phase2B candidate-free headroom task-card opening only. No
Phase2B execution, measured headroom, mechanism validity, self-awareness,
subjective experience, real emotion, autonomy, agency success, EGO readiness,
companion readiness, or mainline-effect claim.

Auto-Remote-Anchor: forbidden.

## Prior Task Card: Phase2B Minimal Environment Reframe Spec 001A

Task id: `RESEARCH-CAMPAIGN-PHASE2B-MINIMAL-ENV-REFRAME-SPEC-001A`

Task card:
`docs/research_campaign/phase2b_minimal_env_reframe_spec_001a.md`

Spec draft:
`docs/research/phase2b_minimal_env_reframe_spec_001a.md`

Problem definition: audited Phase 2 evidence shows `MINIMAL-ENV-SPEC-001A`
has no measured headroom. A fair `count_table` baseline matched the
visible-channel oracle at macro F1 `1.0` inside the `0.03` equivalence band.
The likely failure family is that the legal action-score/query channel exposed
answer-key-like action-aligned structure. Phase2B must open a new candidate-free
environment surface that separates legal interaction from target labels,
final-action scores, answer maps, and full legal response bundles.

Current stage/layer: engineering implementation + mechanism-hypothesis
governance. This is representation governance only, not mechanism execution.

Mainline target: none. No runtime or EGO mainline target.

Enabled-state requirement: only card/spec drafting and focused validation are
enabled. Phase2B execution, oracle scoring, baseline execution, candidate
mechanisms, Phase 3, route tournament, Gate, runtime, EGO mainline, UI, LLM,
AIRI, external services, deployment, push, tag, and remote anchor remain
disabled.

Real-trigger evidence requirement: current git readback; Phase 2 audited
no-headroom artifact; Phase 2 no-headroom reframing audit success; source hashes
for raw Phase 2 harness artifacts; campaign plan/progress/scorecard/ledger
agreement.

Hypothesis: a new candidate-free environment surface that forbids
candidate-visible target labels, final-action scores, answer maps, full legal
response bundles, and semantic action-label leaks can create a valid future
surface for measuring headroom again.

Strongest baseline: lookup/count-table/graph-cache/exhaustive-query saturation.
Future Phase2B execution must keep strongest fair baseline as max over the full
callable battery, including count table, graph-cache family, exhaustive legal
query, full-bundle decoder, serialized-state decoder, trace-only replay, and
passive observation attackers.

Ablation requirement: no executable ablation runs in this card/spec-opening
step. Future execution must include fail-able controls for legal-query
removal/corruption, answer-map injection, final-action-score injection,
semantic action-label leakage, source-provenance omission, replay tamper,
strongest-baseline equality, passive observation shortcut, full-bundle decoder
shortcut, and serialized-state decoder shortcut.

Trace/replay requirement: future replay must recompute from `serialized_state +
current_observation + legal_action_or_query_schema + budget_state`. Hash-only,
stored-output, or self-reported replay is invalid.

Computed-evidence provenance gate: this card/spec-opening step must record
source artifacts and hashes in the validation artifact. Future scores and final
verdicts must be derived from callable producer outputs with input artifacts,
run ids, seed/context/episode ids, aggregation rule, code path hash/source
digest, and consumed-by-final-verdict flags.

Acceptance gate for this checkpoint:

- Phase2B task card exists;
- Phase2B spec draft exists;
- both documents state candidate-free, task-card/spec-only scope;
- Phase 2 no-headroom result is preserved as negative evidence;
- `MINIMAL-ENV-SPEC-001A` is not mutated or refrozen;
- target labels, final-action scores, answer maps, full legal response bundles,
  and semantic action-label leaks are forbidden;
- future baseline battery includes the known saturation families;
- plan/progress/scorecard/ledger agree on Phase2B card opened pending
  validation;
- candidate search, Phase 3, route tournament, Gate, runtime/EGO mainline, and
  remote actions remain false/unauthorized.

Claim ceiling: Phase2B task-card/spec opening only. No measured headroom,
mechanism validity, self-awareness, subjective experience, real emotion,
autonomy, agency success, EGO readiness, companion readiness, or mainline-effect
claim.

Stop condition:

- Stop if `MINIMAL-ENV-SPEC-001A` is edited or refrozen.
- Stop if Phase2B execution, oracle scoring, baseline execution, candidate
  search, Phase 3, route tournament, Gate, runtime, EGO mainline, UI, LLM, AIRI,
  external service, deployment, push, tag, or remote anchor is attempted.
- Stop if the new spec still permits candidate-visible target labels,
  final-action scores, answer maps, full legal response bundles, or semantic
  action-label answer leaks.
- Stop if no-headroom is rewritten as mechanism evidence.

Rollback plan: revert only the Phase2B task card, Phase2B spec draft,
validation artifact, and matching plan/progress/scorecard/ledger entries. Do
not delete or rewrite Phase 0, Phase 1, Phase 2 harness, Phase 2 audit,
no-headroom, reframing, audit, or historical evidence artifacts.

Expected changed files:

- `docs/research_campaign/phase2b_minimal_env_reframe_spec_001a.md`
- `docs/research/phase2b_minimal_env_reframe_spec_001a.md`
- `artifacts/research_campaign/phase2b_minimal_env_reframe_spec_task_card_validation_001a.json`
- `docs/research_campaign/plan.md`
- `docs/OVERALL_PROGRESS.md`
- `artifacts/research_campaign/stage_scorecard.json`
- `artifacts/research_campaign/experiment_log.jsonl`

Forbidden changes:

- No `src/` or `tests/` changes for Phase2B in this checkpoint.
- No Phase2B execution, oracle scores, baseline scores, candidate mechanisms,
  Phase 3, route tournament, Gate, runtime/EGO mainline, UI, LLM, AIRI, external
  service, deployment, push, tag, commit, or remote anchor.
- No mutation or refreeze of `docs/research/MINIMAL-ENV-SPEC-001A.md`.

Auto-Remote-Anchor: forbidden.

## Active Task Card: Phase 2 No-Headroom Reframing 001A

Task id: `RESEARCH-CAMPAIGN-PHASE2-NO-HEADROOM-REFRAMING-001A`

Task card:
`docs/research_campaign/phase2_no_headroom_reframing_001a.md`

Source negative-evidence record:
`artifacts/research_campaign/phase2_no_headroom_negative_evidence_001a.json`

Problem definition: audited Phase 2 evidence shows the frozen
`MINIMAL-ENV-SPEC-001A` surface has no measured headroom. Candidate search and
Phase 3 must remain blocked while the campaign records this as negative
evidence and defines a bounded reframing task before any new candidate-free
environment spec.

Current stage/layer: engineering implementation + mechanism-hypothesis
governance. It is representation governance only, not mechanism execution.

Mainline target: none. No runtime or EGO mainline target.

Enabled-state requirement: only future reframing-artifact work is enabled after
this card-opening checkpoint validates. Candidate mechanisms, route
tournaments, Phase 3, Gate execution, runtime, EGO mainline, UI, LLM, AIRI,
external services, deployment, push, tag, and remote anchor are not enabled.

Real-trigger evidence requirement: current git readback; Phase 2 audited
no-headroom artifact; source hashes for raw Phase 2 harness artifacts; campaign
plan/progress/scorecard/ledger agreement.

Hypothesis: preserving the no-headroom result as route-blocking negative
evidence will force the next useful step to be a candidate-free representation
revision instead of candidate rescue.

Strongest baseline: representation theater. A new task can look stricter while
remaining reducible to lookup, count-table, graph-cache, exhaustive legal
query, serialized-state decoder, or passive observation-only baselines.

Ablation requirement: this card-opening step runs no mechanism ablation. Future
reframing must define fail-able controls for legal-query removal/corruption,
observation-only shortcuts, hidden answer-map injection, source-provenance
omission, replay tamper, and strongest-baseline equality.

Trace/replay requirement: future reframing must preserve recomputation from
`serialized_state + current_observation + legal_action_or_query_schema +
budget_state`; report self-consistency is insufficient.

Computed-evidence provenance gate: the no-headroom negative-evidence record
must cite current machine-readable Phase 2 artifacts and source hashes.

Acceptance gate for this card-opening checkpoint:

- no-headroom negative-evidence artifact exists and parses;
- reframing task card exists;
- plan/progress/scorecard/ledger agree on reframing-card-opened state;
- candidate search and Phase 3 remain false/not opened;
- no candidate, route tournament, Gate, runtime, EGO mainline, UI, LLM, AIRI,
  external service, deployment, push, tag, or remote anchor is run;
- local validation confirms JSON/JSONL parseability and expected dirty scope.

Claim ceiling: no-headroom negative-evidence preservation and reframing
task-card opening only. No mechanism validity, self-awareness, subjective
experience, real emotion, autonomy, agency success, EGO readiness, companion
readiness, or mainline-effect claim.

Stop condition:

- Stop if any source hash in the no-headroom evidence record mismatches.
- Stop if `MINIMAL-ENV-SPEC-001A` is mutated or refrozen.
- Stop if candidate search, Phase 3, route tournament, Gate, runtime, EGO
  mainline, UI, LLM, AIRI, external service, deployment, push, tag, or remote
  anchor is attempted.
- Stop if no-headroom is rewritten as mechanism evidence.

Rollback plan: revert only the no-headroom negative-evidence artifact, the
reframing task card, validation artifact, and matching plan/progress/scorecard
ledger entries. Do not delete or rewrite Phase 0, Phase 1, Phase 2 harness,
Phase 2 audit, repair, or historical evidence artifacts.

Expected changed files:

- `docs/research_campaign/phase2_no_headroom_reframing_001a.md`
- `artifacts/research_campaign/phase2_no_headroom_negative_evidence_001a.json`
- `artifacts/research_campaign/phase2_no_headroom_reframing_task_card_validation_001a.json`
- `docs/research_campaign/plan.md`
- `docs/OVERALL_PROGRESS.md`
- `artifacts/research_campaign/stage_scorecard.json`
- `artifacts/research_campaign/experiment_log.jsonl`

Forbidden changes:

- No candidate mechanism implementation.
- No Phase 3 mechanism search.
- No route tournament.
- No formal Gate execution.
- No runtime, EGO mainline, UI, LLM, AIRI, external service, deployment, push,
  tag, or remote anchor.
- No mutation, normalization, amendment, or refreeze of
  `docs/research/MINIMAL-ENV-SPEC-001A.md`.
- No rewrite of prior failed artifacts into passes.
- No threshold or budget tuning after seeing scores.

Auto-Remote-Anchor: forbidden.

## Active Task Card: Phase 2 Baseline-First Headroom 001A

Task id: `RESEARCH-CAMPAIGN-PHASE2-BASELINE-FIRST-HEADROOM-001A`

Problem definition: Phase 1 formalized broad proxy families, but no
candidate-free baseline headroom has been measured. Phase 2 must test whether
the frozen `MINIMAL-ENV-SPEC-001A` surface has enough headroom over fair cheap
baselines before any candidate mechanism search.

Current stage/layer: engineering implementation + mechanism-hypothesis
governance. It is not candidate mechanism execution and not subjectivity
validation.

Mainline target: no runtime or EGO mainline target. This task only opens the
offline candidate-free baseline-first headroom harness task.

Enabled-state requirement: future execution is limited to candidate-free
baseline/oracle harness work under this card after start-state validation.
Candidate mechanisms, route tournaments, Gate execution, runtime, EGO mainline,
UI, LLM, AIRI, external services, deployment, push, tag, and remote anchor are
not enabled.

Real-trigger evidence requirement: current git readback; Phase 1 audited pass;
`docs/research/MINIMAL-ENV-SPEC-001A.md`; freeze metadata; source readback;
prior baseline-saturation and graph-cache negative-evidence challenge families.

Hypothesis: if the frozen minimal environment has discriminative headroom, then
a candidate-free harness will measure `battery_max_fair_baseline <
visible_channel_oracle - equivalence_band` under the same budget and input
boundary.

Strongest baseline: max over all applicable callable battery members, including
random/majority/constant predictors, passive decoders, nearest-neighbor and
episodic replay, lookup/transition-table/graph-cache family, exhaustive legal
query under the same budget, trace-only replay, and applicable classical
methods.

Ablation requirement: no candidate ablation runs in this card-opening step.
Future execution must include fail-able controls for removed legal observation
fields, removed query budget, forced strongest-baseline equality, leakage
positive control, replay tamper, and omitted source/generator provenance.

Trace/replay requirement: future replay must recompute from `serialized_state +
current_observation + legal_action_or_query_schema + budget_state`; hash-only,
stored-output, or self-reported replay is invalid.

Computed-evidence provenance gate: future scores and final verdict must be
derived from callable producer outputs with input artifacts, run ids,
seed/context/episode ids, aggregation rule, code path hash or source digest,
and consumed-by-final-verdict flags.

Acceptance gate for future execution:

- frozen spec hash matches
  `bf48145b165c5c847cecd7ecda6c2a78818ce326c44f5ad92be391d003daf658`;
- visible-channel oracle macro F1 is at least `0.90`;
- passive, degenerate, and size-only family maxima remain below `0.87`;
- strongest fair baseline, exhaustive legal query, and every graph-cache
  challenger remain below `visible_channel_oracle - 0.03`;
- leakage positive controls pass;
- replay recomputes from serialized state plus observation and legal context;
- generator/source provenance is complete and consumed;
- no source/test/runtime path outside the card's allowed scope is touched.

Claim ceiling: Phase 2 task-card opening and future candidate-free
headroom/no-headroom evidence only. No mechanism validity, self-awareness,
subjective experience, real emotion, autonomy, agency success, EGO readiness,
companion readiness, or mainline-effect claim.

Stop condition:

- Stop if the frozen spec hash mismatches.
- Stop if `MINIMAL-ENV-SPEC-001A` is mutated, normalized, amended, or refrozen.
- Stop if Phase 2 execution starts before this card and start-state validation
  are recorded.
- Stop if a candidate mechanism, route tournament, Gate, runtime, EGO mainline,
  UI, LLM, AIRI, external service, deployment, push, tag, or remote anchor is
  attempted.
- Stop if strongest fair baseline is hand-picked rather than max over the full
  battery.
- Stop if any graph-cache challenger, leakage positive control, replay
  recomputation, or provenance gate is omitted.
- Stop if no-headroom is found and candidate work is still proposed.

Rollback plan: revert only the Phase 2 task card, plan/progress/scorecard/log
entries, and the Phase 2 task-card validation artifact. Do not delete or rewrite
Phase 0, Phase 1, `MINIMAL-ENV-SPEC-001A`, 00XA, Route C, graph-cache, or other
historical negative-evidence artifacts.

Expected changed files for this card-opening step:

- `docs/research_campaign/phase2_baseline_first_headroom_001a.md`
- `docs/research_campaign/plan.md`
- `docs/OVERALL_PROGRESS.md`
- `artifacts/research_campaign/experiment_log.jsonl`
- `artifacts/research_campaign/stage_scorecard.json`
- `artifacts/research_campaign/phase2_baseline_first_headroom_task_card_validation_001a.json`

Future allowed execution paths:

- `src/baseline_first_harness_001a/`
- `tests/baseline_first_harness_001a/`
- `artifacts/baseline_first_harness_001a/`
- `artifacts/research_campaign/phase2_baseline_first_headroom_001a.json`
- `artifacts/research_campaign/phase2_baseline_first_headroom_audit_001a.json`

Forbidden changes:

- No candidate mechanism implementation.
- No Phase 3 mechanism search.
- No route tournament.
- No formal Gate execution.
- No runtime, EGO mainline, UI, LLM, AIRI, external service, deployment, push,
  tag, or remote anchor.
- No mutation, normalization, amendment, or refreeze of
  `docs/research/MINIMAL-ENV-SPEC-001A.md`.
- No rewrite of prior failed artifacts into passes.
- No threshold or budget tuning after seeing scores.

Auto-Remote-Anchor: forbidden.

## Phase 2 Execution Result

Task id: `RESEARCH-CAMPAIGN-PHASE2-BASELINE-FIRST-HEADROOM-001A`

Harness verdict: `no_headroom_baseline_saturated`.

Execution artifact:
`artifacts/research_campaign/phase2_baseline_first_headroom_001a.json`

Harness artifact directory:
`artifacts/baseline_first_harness_001a/`

Focused test command:
`python -m pytest tests\baseline_first_harness_001a\test_baseline_first_harness_001a.py -q`

Focused test result: `3 passed`.

Harness command:
`$env:PYTHONPATH='src'; python -m baseline_first_harness_001a.runner --output-dir artifacts\baseline_first_harness_001a`

Measured result:

- visible-channel oracle macro F1: `1.0`
- strongest fair baseline id: `count_table`
- strongest fair baseline macro F1: `1.0`
- equivalence band: `0.03`
- episode count: `100`
- candidate mechanism run: `false`
- Phase 3 opened: `false`
- runtime or EGO mainline touched: `false`

Interpretation: the frozen `MINIMAL-ENV-SPEC-001A` surface showed no measured
headroom over the full fair callable baseline battery in this bounded Phase 2
harness. Candidate search and Phase 3 remain blocked until the execution
artifacts receive read-only reviewer audit and the campaign records the
negative evidence / reframing decision.

Reviewer/repair status: the first read-only reviewer audit returned
`needs_repair`. It found replay input-consumption overstatement, leakage scanning
that did not inspect generated artifacts, and inflated ablation/provenance
coverage. The failure is preserved in
`artifacts/research_campaign/phase2_baseline_first_headroom_001a.json`. Repairs
now require replay reads for `serialized_state`, `current_observation`,
`legal_action_or_query_schema`, and `budget_state`; leakage scans generated
candidate-visible artifacts with four positive controls; ablation reports six
callable detected controls; and provenance has generator/source/spec/control and
final-verdict records. The read-only re-audit returned `success_reached` and is
recorded in
`artifacts/research_campaign/phase2_baseline_first_headroom_audit_001a.json`.

Claim ceiling: Phase 2 candidate-free baseline-first harness evidence only. No
mechanism validity, subjectivity, consciousness, real emotion, autonomy, EGO
readiness, companion readiness, or mainline-effect claim.

## Phase 0 Closeout Result

Task id: `RESEARCH-CAMPAIGN-PHASE0-RECOVERY-CLOSEOUT-001A`

Audited verdict: `phase0_recovery_closeout_audited_pass`.

Auditor verdict: `success_reached`.

Audit artifact:
`artifacts/research_campaign/phase0_recovery_closeout_audit_001a.json`

Claim ceiling: Phase 0 recovery closeout only. This did not validate recovered
artifacts as mechanism evidence and did not authorize mechanism experiments,
runtime integration, push, tag, or remote anchor.

## Phase 1 Audit Result

Task id: `RESEARCH-CAMPAIGN-PHASE1-PROBLEM-FORMALIZATION-001A`

Audited verdict: `phase1_problem_formalization_audited_pass`.

Reviewer verdict: `success_reached`.

Audit artifact:
`artifacts/research_campaign/phase1_problem_formalization_audit_001a.json`

Claim ceiling: Phase 1 proxy formalization only. This does not validate a
mechanism, prove baseline headroom, authorize candidate search, run Phase 2,
or support consciousness, real emotion, autonomy, EGO readiness, companion
readiness, or mainline-effect claims.

## Completed Task Card: Phase 1 Problem Formalization 001A

Task id: `RESEARCH-CAMPAIGN-PHASE1-PROBLEM-FORMALIZATION-001A`

Problem definition: the program goal mentions self-awareness, emotion,
subjective agency, learning, and boundary tracking, but those terms are too
broad and claim-inflating for direct experiment execution. Phase 1 must split
them into measurable proxy variables, falsifiable evidence contracts, baseline
families, ablation/replay requirements, stop conditions, and claim ceilings.

Current stage/layer: engineering implementation + mechanism-hypothesis
governance. It is not subjectivity validation and not philosophical
consciousness proof.

Mainline target: no runtime or EGO mainline target. This task only defines the
offline proxy formalization needed before Phase 2 baseline-first headroom.

Enabled-state requirement: none. No experiment, baseline battery, candidate
mechanism, Gate, bridge, runtime, UI, LLM integration, external service, push,
tag, or remote anchor may be enabled.

Real-trigger evidence requirement: current git readback; Phase 0 audited pass;
machine-readable proxy schema; human-readable formalization; validation artifact
showing required proxies, baselines, ablations, replay requirements, leakage
controls, and claim ceilings are present.

Hypothesis: a bounded proxy formalization can convert vague target terms into
testable variables without smuggling in a consciousness or real-emotion claim.

Strongest baseline: a language-only taxonomy could look rigorous while failing
to define observable inputs, state variables, output metrics, independent
baselines, ablations, replay, leakage controls, or stop conditions. Phase 1
must defeat that baseline with structured requirements.

Ablation requirement: no mechanism ablation is run in Phase 1. Instead, every
proxy must define future ablation requirements that would remove or perturb the
claim-critical variable.

Trace/replay requirement: every proxy must define future trace fields and replay
requirements. Replay must recompute behavior or score from serialized state plus
observation when a future executable task exists; Phase 1 itself produces no
mechanism replay evidence.

Computed-evidence provenance gate: Phase 1 verdict must be derived from
callable validation checks over the proxy schema and formalization artifacts.
No handwritten pass-only verdict is sufficient.

Acceptance gate:

- `docs/research_campaign/phase1_problem_formalization_001a.md` exists.
- `artifacts/research_campaign/phase1_problem_formalization_001a.json` exists
  and parses.
- The five required proxy families are present: self-model, affect/value
  regulation, active exploration, long-term update, and self/environment
  boundary.
- Each proxy defines observable variables, legal inputs, illegal leaked inputs,
  metrics, independent baseline families, ablations, trace/replay requirements,
  leakage controls, stop conditions, and claim ceiling.
- `experiment_log.jsonl`, `stage_scorecard.json`, and `OVERALL_PROGRESS.md`
  are updated immediately after Phase 1 execution.
- A subagent/reviewer audit returns `success_reached` before Phase 2 opens.

Claim ceiling: Phase 1 proxy formalization only. It does not prove
self-awareness, subjective experience, real emotion, autonomy, agency success,
learning success, EGO readiness, companion readiness, or mechanism validity.

Stop condition:

- Stop if any proxy cannot define observable variables and anti-leakage boundary.
- Stop if the schema uses language performance or self-report as mechanism
  evidence.
- Stop if any proxy omits independent baseline families, ablations, replay, or
  leakage controls.
- Stop if Phase 2 baseline-first headroom would start before Phase 1 subagent
  audit is recorded.

Rollback plan: revert only Phase 1 plan/progress/scorecard/log entries and the
Phase 1 formalization/validation/audit artifacts. Do not delete or rewrite Phase
0 closeout records or historical evidence.

Expected changed files:

- `docs/research_campaign/plan.md`
- `docs/OVERALL_PROGRESS.md`
- `docs/research_campaign/phase1_problem_formalization_001a.md`
- `artifacts/research_campaign/phase1_problem_formalization_001a.json`
- `artifacts/research_campaign/phase1_problem_formalization_validation_001a.json`
- `artifacts/research_campaign/phase1_problem_formalization_audit_001a.json`
- `artifacts/research_campaign/experiment_log.jsonl`
- `artifacts/research_campaign/stage_scorecard.json`

Forbidden changes:

- No `src/` or `tests/` changes.
- No candidate mechanism, baseline battery execution, mechanism ablation,
  mechanism replay, formal Gate, runtime wiring, EGO mainline work, UI, LLM
  integration, external service, deployment, push, tag, or remote anchor.
- No consciousness, real emotion, autonomy, selfhood, electronic-life, EGO
  readiness, companion-readiness, or user-benefit claim.

Auto-Remote-Anchor: forbidden.

## Historical Task Card: Phase 0 Recovery Closeout 001A

Task id: `RESEARCH-CAMPAIGN-PHASE0-RECOVERY-CLOSEOUT-001A`

Problem definition: Phase 0 recovery produced a corrected dirty-state inventory
and the repository now has local preservation commits, but the campaign state
still says the next action is to decide closeout or audit. Continuing into
Phase 1 without a closeout boundary would make the recovery state ambiguous.

Current stage/layer: engineering implementation + mechanism-hypothesis
governance.

Mainline target: no runtime or EGO mainline target. This task only closes the
offline research-campaign recovery checkpoint.

Enabled-state requirement: none. No experiment, Gate, bridge, runtime, UI,
LLM integration, external service, push, tag, or remote anchor may be enabled.

Real-trigger evidence requirement: current git readback, visible dirty-count,
local preservation commit list, JSON/JSONL parse checks, and explicit
closeout/audit artifacts.

Hypothesis: if Phase 0 recovery is complete, a closeout artifact can show that
the dirty artifacts were preserved or ignored by category, the campaign ledger
is parseable, and the next allowed step is Phase 1 problem formalization rather
than mechanism execution.

Strongest baseline: a narrative closeout could simply restate prior progress
without checking the current worktree, ledger parseability, or claim ceiling.
This task must defeat that weak baseline by using current command readback and
machine-readable closeout fields.

Ablation requirement: no mechanism ablation is run. Closeout must remain
distinguishable from evidence acceptance by recording `mechanism_experiment_run:
false` and `evidence_acceptance: false`.

Trace/replay requirement: the closeout must record command inputs and artifact
paths sufficient to replay the closeout validation from current repo state; it
does not establish mechanism replay evidence.

Computed-evidence provenance gate: closeout verdict must be derived from
callable checks: git status, JSON parse, JSONL parse, required-file existence,
and claim-ceiling scan. No handwritten pass-only verdict may stand without
those check results.

Acceptance gate:

- Phase 0 closeout artifact exists and parses as JSON.
- Required campaign files exist and parse where applicable.
- `experiment_log.jsonl` receives an append-only closeout entry.
- `stage_scorecard.json` marks Phase 0 closeout pending audit or passed only
  after audit evidence exists.
- `docs/OVERALL_PROGRESS.md` records the closeout boundary and next Phase 1
  action.
- No mechanism experiment, Gate, runtime, EGO mainline, push, tag, or remote
  anchor is executed.

Claim ceiling: Phase 0 recovery closeout only; no mechanism validity,
consciousness, subjectivity, real emotion, autonomy, agency success, EGO
readiness, companion readiness, or mainline effect.

Stop condition:

- Stop if required JSON/JSONL parse checks fail.
- Stop if worktree has visible dirty files outside the closeout expected files.
- Stop if an audit finds that Phase 0 rewrote, deleted, promoted, or accepted
  pre-existing evidence as mechanism evidence.
- Stop if Phase 1 would start before closeout audit is recorded.

Rollback plan: revert only the closeout plan/progress/scorecard/log entries and
`artifacts/research_campaign/phase0_recovery_closeout_001a.json`. Do not remove
or rewrite any preserved historical artifact or prior recovery record.

Expected changed files:

- `docs/research_campaign/plan.md`
- `docs/OVERALL_PROGRESS.md`
- `artifacts/research_campaign/experiment_log.jsonl`
- `artifacts/research_campaign/stage_scorecard.json`
- `artifacts/research_campaign/phase0_recovery_closeout_001a.json`
- `artifacts/research_campaign/phase0_recovery_closeout_audit_001a.json`

Forbidden changes:

- No `src/` or `tests/` changes.
- No candidate mechanism, baseline battery, ablation, replay, formal Gate,
  runtime wiring, EGO mainline work, UI, LLM integration, external service,
  deployment, push, tag, or remote anchor.
- No historical artifact rewrite or failure-to-pass conversion.

Auto-Remote-Anchor: forbidden.

## Phase 0 Task Card

Problem definition: the repository is already ahead of origin with a modified
decision log and many untracked evidence-like artifacts. Continuing mechanism
experiments without recovering and classifying that state risks losing progress,
burying failures, or creating false pass narratives.

Hypothesis: a minimal recovery pass can make the current campaign state
auditable by adding a canonical plan entry, a computed dirty-state inventory,
an append-only ledger entry, an updated scorecard, and a progress checkpoint
without accepting any unverified artifact as mechanism evidence.

Strongest baseline explanation: the current dirty state may be a mixed pile of
prior user work, audit bundles, generated artifacts, and implementation surfaces;
file presence alone does not establish validity, acceptance, or readiness.

Ablation requirement: no candidate mechanism, baseline battery, ablation run,
replay run, or formal Gate is executed in Phase 0. The recovery output must
remain distinguishable from evidence acceptance.

Trace/replay requirement: the recovery inventory must be reproducible from live
`git status --porcelain=v1` plus repo readback. Future mechanism tasks must
define replay recomputation from `serialized_state + observation`, not hash-only
comparison.

Computed-evidence provenance gate: future scores must record producer function,
input artifacts, run id, seed/context/episode ids, aggregation rule, and code
path hash. Phase 0 records no mechanism scores.

Acceptance gate:

- `docs/research_campaign/plan.md` exists and states the active campaign rules.
- `artifacts/research_campaign/phase0_recovery_inventory_001a.json` parses as JSON.
- `artifacts/research_campaign/experiment_log.jsonl` has one append-only Phase 0 entry.
- `artifacts/research_campaign/stage_scorecard.json` parses as JSON and points to Phase 0.
- `docs/OVERALL_PROGRESS.md` has a current Phase 0 checkpoint.
- No existing evidence artifact is rewritten, deleted, promoted, pushed, tagged, or anchored.

Stop condition:

- Stop if JSON validation fails.
- Stop if `docs/decision_log.md` or pre-existing untracked evidence bundles would need to be
  overwritten or reinterpreted to complete the task.
- Stop if any command would run a mechanism experiment, Gate, runtime, EGO mainline,
  external service, push, tag, or remote anchor.

Rollback plan: remove only the Phase 0 files/entries created by this task:
`docs/research_campaign/plan.md`, the Phase 0 recovery inventory/validation
artifacts, the appended Phase 0 JSONL ledger entry, and the Phase 0 checkpoint
edits. Do not delete or rewrite pre-existing artifacts.

Expected changed files:

- `docs/research_campaign/plan.md`
- `docs/OVERALL_PROGRESS.md`
- `artifacts/research_campaign/experiment_log.jsonl`
- `artifacts/research_campaign/stage_scorecard.json`
- `artifacts/research_campaign/phase0_recovery_inventory_001a.json`
- `artifacts/research_campaign/phase0_recovery_validation_001a.json`

Forbidden changes:

- No `src/` implementation or candidate mechanism changes.
- No `tests/` changes except future task-specific validation cards.
- No historical artifact rewrites.
- No old failure repair into pass-shaped language.
- No threshold tuning, baseline weakening, schema migration, Gate execution,
  runtime wiring, EGO mainline work, UI, LLM integration, external services,
  push, tag, or remote anchor.

## Campaign Loop Rules

Every future task must update the campaign state immediately:

1. Before work: write or update this plan with the task card, claim ceiling,
   expected changed files, forbidden changes, and stop condition.
2. During work: keep failures and blockers in the ledger; do not wait for a pass.
3. After work: append `experiment_log.jsonl`, update `stage_scorecard.json`,
   update `docs/OVERALL_PROGRESS.md`, and record the next minimal closed-loop action.
4. If two consecutive tasks do not increase discriminative evidence, route to
   `needs_reframing`; do not continue patching toward a pass.

## Research Phases

Phase 1: Problem formalization. Split self-awareness, emotion, agency, and
learning into measurable proxy variables: self-model, affect/value regulation,
active exploration, long-term update, and self/environment boundary.

Phase 2: Baseline-first headroom. Run candidate-free baseline batteries before
candidate mechanisms. If lookup, graph/cache, heuristic, or exhaustive baselines
saturate the surface, freeze no-headroom negative evidence and reframe.

Phase 3: Mechanism search. Search candidates only inside measured-headroom
surfaces. Change one mechanism variable per run. Do not tune thresholds after
seeing results.

Phase 4: Structure extraction. Extract low-complexity structure from passes and
failures using MDL, compressed causal structure, and replayable state transitions.
Do not treat language appearance as mechanism evidence.

Phase 5: Large-scale validation. Run distribution-shift, counterfactual,
intervention, ablation, source-deletion, replay-recomputation, and leakage
positive-control checks.

Phase 6: Formalization. Produce local formal proofs only for stable,
reproducible, baseline-resistant structures. State proof scope and failure
boundaries explicitly.

Program cycle rule: Phase 1 through Phase 6 may repeat. If Phase 6 yields only a
local or partial result, return to Phase 1 or Phase 2 with a stronger problem
representation. Do not mark program-level complete from a single task card,
single phase pass, local validation pass, or chat-only subagent verdict.

## Standing Anti-False-Pass Rules

- No static verdict dictionaries, handwritten scores, unconditional clean reports,
  or tests that only assert pass.
- Baselines must be independent callable implementations under the same budget
  and input boundary as the candidate.
- Ablations must rerun episodes under real interventions.
- Leakage scans must include a real scanner and at least one positive control.
- Replay must recompute behavior from serialized state and observation.
- Any unused frozen seed, heldout context, train context, or counterfactual pair
  blocks the evidence claim.

## Next Minimal Closed-Loop Action

If the campaign continues, open only a separately bounded
problem-representation redesign, surface-family closure, or claim-downgrade
checkpoint after the audited route decision:

- task card:
  `docs/research_campaign/phase2b_no_headroom_route_decision_task_card_001a.md`
- route-decision artifact:
  `artifacts/research_campaign/phase2b_no_headroom_route_decision_001a.json`
- validation artifact:
  `artifacts/research_campaign/phase2b_no_headroom_route_decision_task_card_validation_001a.json`

Candidate mechanisms, Phase 3, route tournament, runtime/EGO mainline, push,
tag, and remote anchor remain blocked because the strongest fair baseline
matched the oracle within the equivalence band on both measured surfaces. The
next checkpoint must not implement candidates or open Phase 3. Program
completion also remains blocked until a separate terminal assessment satisfies
the controller card terminal contract.

Program status remains `active_not_complete`.

`````
