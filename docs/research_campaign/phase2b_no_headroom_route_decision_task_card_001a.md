# Phase2B No-Headroom Route Decision Task Card 001A

Task id: `RESEARCH-CAMPAIGN-PHASE2B-NO-HEADROOM-ROUTE-DECISION-001A`

Source audited checkpoint:
`artifacts/research_campaign/phase2b_no_headroom_preservation_reframing_audit_001a.json`

Source negative-evidence record:
`artifacts/research_campaign/phase2b_no_headroom_negative_evidence_001a.json`

Source preservation/reframing record:
`artifacts/research_campaign/phase2b_no_headroom_preservation_reframing_001a.json`

## Purpose

Open a bounded route-decision checkpoint after two candidate-free no-headroom
surfaces:

- Phase 2 `MINIMAL-ENV-SPEC-001A` saturated against `count_table`;
- Phase2B `phase2b_minimal_env_reframe_spec_001a` also saturated against
  `count_table`.

This card authorizes only a local governance/readback decision about whether
the current route needs problem-representation redesign, local closure, or
downgrade before any further experimental work.

It does not authorize a candidate mechanism, Phase 3, a route tournament, a new
environment spec, a new harness, runtime/EGO mainline work, push, tag, or remote
anchor.

## Current Layer

`engineering_implementation + mechanism_hypothesis_governance`

This is route governance after negative evidence. It is not a mechanism
experiment, not subjectivity validation, and not philosophical consciousness
work.

## Bounded Audit Before Execution

Real objective: decide the next route boundary after repeated candidate-free
no-headroom without turning no-headroom into a success claim or jumping to
candidate rescue.

Problem-definition risk: a "route decision" can become a disguised route
tournament, candidate selection board, or speculative architecture plan. That
would violate the audited no-headroom boundary.

Strongest baseline explanation: both measured surfaces exposed enough structure
for a fair `count_table` baseline to match the visible-channel oracle at macro
F1 `1.0` inside the `0.03` equivalence band.

Strongest invalidating reason: if this checkpoint only renames the next surface
or weakens the baseline family, it does not increase discriminative mechanism
evidence.

Falsifier for candidate continuation: if no candidate-free surface has measured
headroom, any candidate work on the same evidence family is explainable as
baseline-equivalent behavior.

Insufficient evidence: this card, a local parse pass, or a reviewer success on
the route decision does not prove any mechanism, does not validate a new
surface, and does not complete the program.

Mechanism-vs-behavior classification: this task tests no mechanism. It records
a route-governance decision from existing negative evidence.

Hard-coding / leakage / weak-baseline checks:

- no candidate mechanism;
- no Phase 3 mechanism search;
- no route tournament;
- no new spec or harness execution;
- no baseline weakening;
- no threshold tuning after saturation;
- no mutation or refreeze of prior specs;
- no runtime, EGO mainline, UI, LLM, AIRI, deployment, push, tag, or remote
  anchor;
- no program terminal verdict unless a separate terminal artifact and audit
  explicitly satisfy the controller card's terminal contract.

## Problem Definition

The campaign has now produced two audited candidate-free no-headroom results:

```text
Phase 2 verdict = no_headroom_baseline_saturated
Phase 2 strongest fair baseline = count_table
Phase 2 strongest fair baseline macro F1 = 1.0
Phase 2 visible-channel oracle macro F1 = 1.0

Phase2B verdict = no_headroom_baseline_saturated
Phase2B strongest fair baseline = count_table
Phase2B strongest fair baseline macro F1 = 1.0
Phase2B visible-channel oracle macro F1 = 1.0
```

The next useful checkpoint is not candidate authoring. It is a bounded route
decision that can only classify the current line as requiring:

- `needs_problem_representation_redesign`;
- `close_current_surface_family_with_negative_evidence`;
- `downgrade_current_mechanism_claim_surface`;
- or a separately audited program-terminal assessment if all currently
  definable routes are claimed exhausted.

This checkpoint does not itself execute any of those follow-up tasks.

## Mainline Target

None. This task has no runtime, EGO mainline, UI, LLM, AIRI, external service,
deployment, product, or companion target.

## Enabled-State Requirement

Only local route-governance artifacts and campaign bookkeeping are enabled:

- create a route-decision artifact;
- validate source hashes and cross-file agreement;
- update `plan.md`, `OVERALL_PROGRESS.md`, `stage_scorecard.json`,
  `goal_stage_audit_loop_validation_001a.json`, and `experiment_log.jsonl`;
- run read-only reviewer audit.

No environment spec, harness, candidate, Phase 3, route tournament, runtime,
EGO mainline, push, tag, or remote anchor is enabled.

## Real-Trigger Evidence Requirement

Execution must read and record:

- repo root, branch, HEAD, upstream/ahead-behind, and `git status --short --branch`;
- this task card;
- `docs/research_campaign/goal_stage_audit_loop_001a.md`;
- `docs/research_campaign/plan.md`;
- `docs/OVERALL_PROGRESS.md`;
- `artifacts/research_campaign/stage_scorecard.json`;
- tail or append target for `artifacts/research_campaign/experiment_log.jsonl`;
- `artifacts/research_campaign/phase2_no_headroom_negative_evidence_001a.json`;
- `artifacts/research_campaign/phase2_no_headroom_reframing_001a.json`;
- `artifacts/research_campaign/phase2b_no_headroom_negative_evidence_001a.json`;
- `artifacts/research_campaign/phase2b_no_headroom_preservation_reframing_001a.json`;
- `artifacts/research_campaign/phase2b_no_headroom_preservation_reframing_audit_001a.json`;
- current memory/search evidence that candidate work remains blocked after
  no-headroom saturation.

## Hypothesis

If two candidate-free surfaces are saturated by the same strongest fair baseline
family, then the current line should route to problem-representation redesign,
surface-family closure, or claim downgrade before any candidate or Phase 3 work.

## Strongest Baseline

The strongest false explanation for any future candidate improvement remains
cheap reconstruction: lookup, count-table, graph-cache, transition-table,
successor-map, FSM planner, episodic traversal, trace-only replay, full-bundle
decoder, serialized-state decoder, exhaustive legal query, or observation-only
decoder behavior under the same budget.

Any future redesign must explain why those families would no longer saturate
the visible-channel oracle before it can authorize candidate work.

## Ablation Requirement

This route-decision checkpoint runs no mechanism ablation.

Any future executable redesign must predeclare ablations for the claim-critical
variable and must rerun real interventions. A future governance-only redesign
must define those ablations without executing them.

## Trace / Replay Requirement

This checkpoint must preserve that trace/replay evidence is insufficient by
itself if baselines saturate the surface. Future executable work must retain
replay recomputation from:

```text
serialized_state + current_observation + legal_action_or_query_schema + budget_state
```

Hash equality and stored report agreement remain insufficient.

## Computed-Evidence Provenance Gate

The route-decision artifact must be derived from current machine-readable
evidence, not narrative recollection. It must include:

- producer command or structured readback source;
- input artifact paths and hashes;
- consumed-by-decision flags;
- aggregation rule for repeated no-headroom;
- route decision rule;
- program terminal status explicitly set to `active_not_complete` unless a
  separate terminal assessment is opened and audited.

## Acceptance Gate

This checkpoint is accepted only if:

- this task card exists and parses as the active checkpoint;
- the route-decision artifact exists and records repeated no-headroom;
- source hashes for Phase 2 and Phase2B negative-evidence artifacts match live
  files;
- plan/progress/scorecard/goal-validation/ledger agree on route-decision state;
- candidate mechanism, Phase 3, route tournament, runtime/EGO mainline, push,
  tag, and remote anchor remain false/blocked;
- program status remains `active_not_complete`;
- focused validation passes;
- read-only reviewer audit returns `success_reached`, or any failure is
  preserved and repaired without widening scope.

## Claim Ceiling

This checkpoint can claim only a bounded route decision after repeated
candidate-free no-headroom.

It does not prove mechanism validity, self-awareness, subjective experience,
real emotion, autonomy, agency success, EGO readiness, companion readiness,
runtime readiness, user benefit, or mainline effect. It also does not prove
program completion.

## Stop Conditions

Stop and record a blocker if:

- any source evidence artifact is missing, unparsable, or hash-mismatched;
- a candidate, Phase 3 search, route tournament, new harness, new spec
  execution, runtime/EGO mainline path, UI, LLM, AIRI, external service,
  deployment, push, tag, or remote anchor is attempted;
- old no-headroom evidence is rewritten into a pass;
- baseline families are weakened or thresholds tuned after saturation;
- the checkpoint claims `program_complete` without satisfying the controller
  card terminal contract;
- plan/progress/scorecard/ledger disagree after repair.

## Rollback Plan

Rollback this checkpoint only by reverting:

- `docs/research_campaign/phase2b_no_headroom_route_decision_task_card_001a.md`;
- `artifacts/research_campaign/phase2b_no_headroom_route_decision_001a.json`;
- `artifacts/research_campaign/phase2b_no_headroom_route_decision_task_card_validation_001a.json`;
- the matching plan/progress/scorecard/goal-validation/ledger updates;
- the matching reviewer audit artifact if one is generated.

Do not delete or rewrite Phase 0, Phase 1, Phase 2, Phase2B, failed audit,
repair, no-headroom, or historical evidence artifacts.

## Expected Changed Files

- `docs/research_campaign/phase2b_no_headroom_route_decision_task_card_001a.md`
- `artifacts/research_campaign/phase2b_no_headroom_route_decision_001a.json`
- `artifacts/research_campaign/phase2b_no_headroom_route_decision_task_card_validation_001a.json`
- `artifacts/research_campaign/phase2b_no_headroom_route_decision_audit_001a.json`
- `docs/research_campaign/plan.md`
- `docs/OVERALL_PROGRESS.md`
- `artifacts/research_campaign/stage_scorecard.json`
- `artifacts/research_campaign/goal_stage_audit_loop_validation_001a.json`
- `artifacts/research_campaign/experiment_log.jsonl`

## Forbidden Changes

- No candidate mechanism implementation.
- No Phase 3 mechanism search.
- No route tournament.
- No new environment spec or harness execution.
- No formal Gate execution.
- No runtime, EGO mainline, UI, LLM, AIRI, external service, deployment, push,
  tag, commit, or remote anchor.
- No mutation, normalization, amendment, or refreeze of prior specs.
- No rewriting prior failed artifacts into passes.
- No program-completion claim.

## Auto-Remote-Anchor

`forbidden`

## Immediate Next Action After This Card

Create the route-decision artifact from current machine-readable Phase 2 and
Phase2B no-headroom evidence, then run focused validation. If validation passes,
run read-only reviewer audit. Candidate mechanisms, Phase 3, route tournament,
runtime/EGO mainline, push, tag, and remote anchor remain blocked.
