# Phase2B Minimal Environment Reframe Spec 001A

Task id: `RESEARCH-CAMPAIGN-PHASE2B-MINIMAL-ENV-REFRAME-SPEC-001A`

Status: task-card opened pending focused validation.

Auto-Remote-Anchor: forbidden.

## Problem Definition

Phase 2 measured `no_headroom_baseline_saturated` on the frozen
`MINIMAL-ENV-SPEC-001A` surface. The visible-channel oracle macro F1 was `1.0`,
and the strongest fair baseline was `count_table` with macro F1 `1.0`, inside
the `0.03` equivalence band. The audited reframing artifact records the likely
failure family: the legal action-score/query channel exposed enough
action-aligned structure for lookup/count-table and related baselines to match
the oracle under the same budget.

This task opens only a candidate-free Phase2B environment-reframe card and spec
draft. It does not run an oracle, run baselines, freeze the new spec, implement
a harness, implement a candidate, open Phase 3, or touch runtime/EGO mainline.

## Current Stage / Layer

`engineering_implementation + mechanism_hypothesis_governance`

This is representation governance for a future candidate-free headroom test. It
is not mechanism execution, subjectivity validation, learning/adaptation
evidence, or philosophical consciousness analysis.

## Mainline Target

None. There is no runtime, EGO mainline, UI, LLM, AIRI, external service,
deployment, product, Gate, or bridge target.

## Enabled-State Requirement

Enabled now:

- write a bounded campaign task card;
- write a candidate-free environment-reframe spec draft;
- update progress, scorecard, ledger, and validation records;
- run focused local validation of document/state consistency.

Not enabled:

- Phase2B execution;
- oracle scoring;
- baseline battery execution;
- candidate mechanism implementation;
- Phase 3 mechanism search;
- route tournament;
- Gate execution;
- runtime/EGO mainline, UI, LLM, AIRI, external service, deployment;
- commit, push, tag, or remote anchor.

## Real-Trigger Evidence Requirement

This card is opened only from current repo readback plus these artifacts:

- `artifacts/research_campaign/phase2_no_headroom_negative_evidence_001a.json`
- `artifacts/research_campaign/phase2_no_headroom_reframing_001a.json`
- `artifacts/research_campaign/phase2_no_headroom_reframing_audit_001a.json`
- `artifacts/baseline_first_harness_001a/result.json`
- `artifacts/baseline_first_harness_001a/baseline_comparison.json`
- `docs/research/MINIMAL-ENV-SPEC-001A.md`
- `docs/research/MINIMAL-ENV-SPEC-001A.freeze.json`

The old frozen spec is a blocked source surface, not a mutable draft. Phase2B
must use a new file and must not rewrite or refreeze `MINIMAL-ENV-SPEC-001A`.

## Bounded Audit Before Implementation

Layer: engineering implementation + mechanism-hypothesis governance.

Real objective: define a new candidate-free environment surface that prevents
the specific count-table / graph-cache / exhaustive-query saturation family
from being inherited, while preserving replayability and future baseline-first
measurement.

Wrong-problem risk: if Phase2B only renames fields or hides target labels while
leaving a full action-score or answer-map legal channel, it has not reframed the
problem.

Strongest baseline explanation: apparent future success could still be solved
by lookup, count table, graph cache, exhaustive legal query, full-bundle decoder,
serialized-state decoder, trace-only replay, or passive observation decoding.

Strongest invalidity reason: a spec draft can look stricter while relying on
unmeasured narrative assumptions about headroom. Headroom must be measured by a
future candidate-free harness.

Falsifier for this framing: focused validation finds that the Phase2B draft
still allows candidate-visible direct target labels, final-action scores,
answer maps, full legal response bundles, semantic action labels that encode the
answer, or a future harness that can weaken the baseline battery.

Insufficient evidence: a clean document, parseable JSON, or task-card creation
does not prove headroom. It only authorizes future validation/audit of the
reframed surface.

Mechanism vs resemblance classification: this card tests neither. It is a
governance/specification step for a future mechanism-discriminating test.

Hard-coding / leakage / weak-baseline checks:

- no direct target label or final-action score may be candidate-visible;
- no full legal response bundle may be visible before budgeted interaction;
- no semantic final-action label may encode the correct response;
- no schema split may create a cleaner second logic path only used by tests;
- no baseline may be removed because it saturated Phase 2;
- no threshold, budget, or equivalence band may be tuned after scores;
- no report-only provenance or handwritten verdict may count as evidence.

Minimal validation: current files parse; Phase2B card/spec exist; required
anti-leakage clauses are present; old spec hash is preserved; candidate search,
Phase 3, execution, runtime/mainline, and remote actions remain unauthorized.

Stop condition: stop before any execution if the card/spec cannot block the
known saturation family at the specification layer.

Rollback plan: remove only the Phase2B card/spec and matching plan/progress/
scorecard/ledger/validation entries. Do not delete, mutate, or reinterpret
Phase 2 negative evidence or `MINIMAL-ENV-SPEC-001A`.

Acceptance signal: local focused validation records a pass for task-card/spec
opening only, with no execution and no candidate authorization.

## Hypothesis

If the candidate-free environment surface separates legal interaction from any
answer-key-like action-score/query channel, then a future Phase2B baseline-first
harness can measure whether there is discriminative headroom before candidate
work is considered.

This hypothesis is unproven until a future candidate-free harness runs.

## Strongest Baseline

The strongest baseline is the maximum over the full fair callable battery, not
a selected subset. Future Phase2B execution must include at least:

- `count_table`
- `graph_lookup`
- `transition_table`
- `successor_map`
- `fsm_planner`
- `episodic_traversal`
- `exhaustive_legal_query`
- `greedy_uncertainty_query_under_budget`
- `trace_only_replay`
- `full_bundle_decoder`
- `serialized_state_decoder`
- `observation_only`
- passive nearest-neighbor and supervised passive attackers
- random, majority, and constant predictors
- strongest known classical method for the generated task type

If any fair baseline reaches `visible_channel_oracle - equivalence_band`, the
future harness must stop with no-headroom negative evidence before candidate
work.

## Ablation Requirement

No executable ablation runs in this card. The Phase2B spec must require future
ablation/control families:

- legal-query removal/corruption;
- answer-map injection positive control;
- final-action-score injection positive control;
- semantic action-label leak positive control;
- source-provenance omission;
- replay tamper;
- strongest-baseline equality;
- passive observation shortcut;
- full-bundle decoder shortcut;
- serialized-state decoder shortcut.

Each future ablation must rerun episodes or controls through callable producer
paths and be consumed by the final verdict.

## Trace / Replay Requirement

The future Phase2B harness must replay from:

```text
serialized_state + current_observation + legal_action_or_query_schema + budget_state
```

Replay must recompute behavior or scores. Hash equality, stored-output replay,
or report self-consistency is insufficient.

## Computed-Evidence Provenance Gate

This task-card/spec-opening step must record source artifacts and hashes in the
validation artifact. Future execution must record for every score:

- producer function;
- input artifacts;
- run id;
- seed/context/episode ids;
- aggregation rule;
- code path hash or source digest;
- generated artifacts;
- consumed-by-final-verdict flag;
- failure paths and positive controls.

Static verdict dictionaries, handwritten scores, unconditional clean reports,
and tests that only assert pass are forbidden.

## Acceptance Gate

This card-opening checkpoint passes only if:

- `docs/research_campaign/phase2b_minimal_env_reframe_spec_001a.md` exists;
- `docs/research/phase2b_minimal_env_reframe_spec_001a.md` exists;
- both documents state candidate-free, task-card-only scope;
- both documents preserve the Phase 2 no-headroom result as negative evidence;
- both documents forbid mutation/refreeze of `MINIMAL-ENV-SPEC-001A`;
- the spec draft forbids candidate-visible target labels, final-action scores,
  answer maps, full legal response bundles, and semantic action-label leaks;
- the future baseline battery includes count-table, graph-cache challengers,
  exhaustive legal query, full-bundle decoder, serialized-state decoder, and
  passive observation attackers;
- plan/progress/scorecard/ledger agree on Phase2B card opened pending
  validation or validated;
- candidate mechanism remains unrun;
- Phase 3 remains unopened;
- Phase2B execution remains unauthorized;
- program goal remains `active_not_complete`;
- program terminal condition remains `not_met`;
- no forbidden dirty paths appear.

## Claim Ceiling

Phase2B task-card/spec opening only. This does not prove headroom, mechanism
validity, subjectivity, consciousness, real emotion, autonomy, agency success,
EGO readiness, companion readiness, runtime readiness, or mainline effect.

## Stop Conditions

Stop if any of these occur:

- `MINIMAL-ENV-SPEC-001A` is edited, normalized, moved, or refrozen;
- any Phase2B oracle, baseline, replay, ablation, or leakage execution starts;
- a candidate mechanism, Phase 3, route tournament, Gate, runtime/EGO mainline,
  UI, LLM, AIRI, external service, deployment, push, tag, or remote anchor is
  attempted;
- the new spec still permits candidate-visible target labels, final-action
  scores, answer maps, or full legal response bundles;
- the baseline battery is weakened relative to the saturation evidence;
- no-headroom negative evidence is reworded as mechanism progress.

## Rollback Plan

Rollback is limited to:

- `docs/research_campaign/phase2b_minimal_env_reframe_spec_001a.md`
- `docs/research/phase2b_minimal_env_reframe_spec_001a.md`
- `artifacts/research_campaign/phase2b_minimal_env_reframe_spec_task_card_validation_001a.json`
- matching entries in `docs/research_campaign/plan.md`
- matching entries in `docs/OVERALL_PROGRESS.md`
- matching entries in `artifacts/research_campaign/stage_scorecard.json`
- matching append-only entry in `artifacts/research_campaign/experiment_log.jsonl`, only if rollback is explicitly authorized and recorded

Do not delete or rewrite Phase 0, Phase 1, Phase 2, Phase 2 no-headroom,
reframing, audit, or historical evidence artifacts.

## Expected Changed Files

- `docs/research_campaign/phase2b_minimal_env_reframe_spec_001a.md`
- `docs/research/phase2b_minimal_env_reframe_spec_001a.md`
- `artifacts/research_campaign/phase2b_minimal_env_reframe_spec_task_card_validation_001a.json`
- `docs/research_campaign/plan.md`
- `docs/OVERALL_PROGRESS.md`
- `artifacts/research_campaign/stage_scorecard.json`
- `artifacts/research_campaign/experiment_log.jsonl`

## Forbidden Changes

- No `src/` or `tests/` changes for Phase2B in this checkpoint.
- No Phase2B execution.
- No oracle or baseline scores.
- No candidate mechanism implementation.
- No Phase 3 mechanism search.
- No route tournament.
- No formal Gate execution.
- No runtime, EGO mainline, UI, LLM, AIRI, external service, deployment.
- No push, tag, commit, or remote anchor.
- No mutation or refreeze of `docs/research/MINIMAL-ENV-SPEC-001A.md`.
- No rewrite of prior failed artifacts into pass-shaped evidence.

## Auto-Remote-Anchor Decision

Forbidden.

## Next Minimal Closed-Loop Action

Run focused local validation of this Phase2B card/spec opening. If it passes,
record the validation and then request/read a read-only reviewer audit before
any Phase2B execution task is opened.
