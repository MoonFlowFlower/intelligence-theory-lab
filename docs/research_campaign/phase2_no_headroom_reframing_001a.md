# Phase 2 No-Headroom Reframing 001A

Task id: `RESEARCH-CAMPAIGN-PHASE2-NO-HEADROOM-REFRAMING-001A`

Source negative-evidence record:
`artifacts/research_campaign/phase2_no_headroom_negative_evidence_001a.json`

## Purpose

Open a bounded reframing task after audited Phase 2 no-headroom evidence.

This task card does not implement a candidate, open Phase 3, run a route
tournament, mutate the frozen `MINIMAL-ENV-SPEC-001A` surface, or rerun the
Phase 2 harness. It only authorizes a future governance/evidence task that
turns the audited no-headroom result into a stricter problem representation for
the next candidate-free environment-spec attempt.

## Current Layer

`engineering_implementation + mechanism_hypothesis_governance`

This is not subjectivity validation and not philosophical consciousness work.

## Bounded Audit Before Execution

Real objective: preserve the no-headroom negative evidence and identify the
smallest representation change required before another candidate-free minimal
environment spec can be opened.

Problem-definition risk: treating no-headroom as an implementation nuisance
would route directly into candidate rescue, threshold tuning, or route
tournaments. The correct route is reframing before candidate search.

Strongest baseline explanation: the current frozen surface exposed an allowed
legal action-score/query channel that made the visible-channel oracle reducible
to a cheap `count_table` baseline under the same budget.

Strongest invalidating reason: if the reframing simply hides labels, renames
fields, weakens baselines, or removes the fair legal-query budget without a new
auditable environment boundary, it does not increase discriminative mechanism
evidence.

Falsifier for the current framing: a future baseline-first harness on the
reframed surface again reaches the visible-channel oracle within the
equivalence band using lookup, count-table, graph-cache, serialized-state, or
exhaustive legal-query challengers.

Insufficient evidence: this card, JSON parse success, progress-file agreement,
or a reviewer success on the card does not prove a new environment has headroom.

Mechanism-vs-behavior classification: this task is representation governance.
It does not test or validate a candidate mechanism.

Hard-coding / leakage / weak-baseline checks:

- no hidden label relocation;
- no renaming answer-bearing fields to evade the scanner;
- no threshold or budget tuning after seeing scores;
- no baseline weakening;
- no candidate-specific affordance;
- no second logic path used only by future tests;
- no mutation or refreeze of `MINIMAL-ENV-SPEC-001A`;
- no Phase 3, route tournament, Gate, runtime, EGO mainline, UI, LLM, AIRI,
  deployment, push, tag, or remote anchor.

## Problem Definition

Phase 2 produced audited negative evidence:

```text
verdict = no_headroom_baseline_saturated
visible_channel_oracle_macro_f1 = 1.0
strongest_fair_baseline_id = count_table
strongest_fair_baseline_macro_f1 = 1.0
equivalence_band = 0.03
candidate_mechanism_run = false
phase3_opened = false
```

The program must record this as a route-blocking no-headroom result for the
current frozen surface and define a bounded reframing task before any new
candidate-free spec, candidate mechanism, or Phase 3 search.

## Mainline Target

None. This task has no runtime, EGO mainline, UI, LLM, AIRI, external service,
deployment, product, or companion target.

## Enabled-State Requirement

Only a future documentation/evidence reframing pass is enabled. The pass may
create a reframing artifact and a future task card for a new candidate-free
minimal environment spec, but it may not execute that new spec or any candidate.

## Real-Trigger Evidence Requirement

Future execution must read and record:

- repo root, branch, HEAD, upstream/ahead-behind, and
  `git status --short --branch -uall`;
- this task card;
- `docs/research_campaign/plan.md`;
- `docs/OVERALL_PROGRESS.md`;
- `artifacts/research_campaign/stage_scorecard.json`;
- tail of `artifacts/research_campaign/experiment_log.jsonl`;
- `artifacts/research_campaign/phase2_no_headroom_negative_evidence_001a.json`;
- `artifacts/research_campaign/phase2_baseline_first_headroom_audit_001a.json`;
- `artifacts/baseline_first_harness_001a/result.json`;
- `artifacts/baseline_first_harness_001a/baseline_comparison.json`;
- `artifacts/baseline_first_harness_001a/computed_evidence_provenance.json`;
- relevant prior baseline-saturation and graph-cache challenge families.

## Hypothesis

If the campaign treats the audited no-headroom result as route-blocking negative
evidence, then the next useful step is a candidate-free problem-representation
revision that removes baseline-saturating shortcut structure while preserving
trace/replay, leakage-positive-control, ablation, and provenance requirements.

## Strongest Baseline

The strongest false explanation for any future reframing is representation
theater: the new wording looks stricter but the next surface remains reducible
to lookup, count-table, graph-cache, exhaustive legal query, serialized-state
decoder, or other cheap baselines under the same budget.

Future reframing must explicitly challenge:

- count-table / frequency table;
- graph lookup;
- transition table;
- successor map;
- FSM planner;
- episodic traversal;
- trace-only replay / n-gram trace lookup;
- full-bundle and serialized-state decoders;
- exhaustive legal query under the same budget;
- passive observation-only decoders.

## Ablation Requirement

This card-opening step runs no mechanism ablation. Future reframing must specify
which claim-critical variable a later candidate-free environment spec would
ablate, and must define fail-able controls for:

- legal-query removal or corruption;
- observation-only shortcut removal;
- hidden answer-map injection;
- source-provenance omission;
- replay tamper;
- strongest-baseline equality.

## Trace / Replay Requirement

Future reframing must preserve replay recomputation from:

```text
serialized_state + current_observation + legal_action_or_query_schema + budget_state
```

Hash equality, stored-output equality, and report self-consistency are not
sufficient.

## Computed-Evidence Provenance Gate

The reframing artifact must be derived from current machine-readable Phase 2
evidence, not narrative recollection. It must include:

- producer command or file readback source;
- input artifact paths and hashes;
- Phase 2 run id;
- aggregation rule for the no-headroom decision;
- route-blocking decision rule;
- consumed-by-final-decision flags.

## Acceptance Gate

This card-opening checkpoint is accepted only if:

- the no-headroom negative-evidence artifact exists and parses;
- this reframing task card exists;
- `plan.md`, `OVERALL_PROGRESS.md`, `stage_scorecard.json`, and
  `experiment_log.jsonl` agree that Phase 2 is audited no-headroom and the next
  work is reframing;
- candidate search and Phase 3 remain false/not opened;
- no candidate, route tournament, Gate, runtime, EGO mainline, UI, LLM, AIRI,
  external service, deployment, push, tag, or remote anchor is run;
- local validation confirms JSON/JSONL parseability and expected dirty scope.

Future execution of this card may be considered successful only after a
read-only reviewer audit returns `success_reached` on a reframing artifact. That
future success still does not authorize candidate search until the next
candidate-free environment-spec task card is opened and validated.

## Claim Ceiling

This task card proves only that audited no-headroom negative evidence has been
preserved and that the next allowed step is bounded reframing.

It does not prove mechanism validity, self-awareness, subjective experience,
real emotion, autonomy, agency success, EGO readiness, companion readiness,
runtime readiness, user benefit, or mainline effect.

## Stop Conditions

Stop and record a blocker if:

- the Phase 2 no-headroom evidence artifact is missing or unparsable;
- any source hash in the no-headroom evidence record does not match the current
  source artifact;
- candidate search, Phase 3, route tournament, Gate, runtime, EGO mainline, UI,
  LLM, AIRI, external service, deployment, push, tag, or remote anchor is
  attempted;
- `MINIMAL-ENV-SPEC-001A` is mutated, normalized, amended, or refrozen;
- the reframing proposes baseline weakening, threshold tuning, or legal-query
  removal without a replacement auditable boundary;
- the no-headroom result is rewritten as a pass-shaped mechanism claim.

## Rollback Plan

Rollback this card-opening checkpoint only by reverting:

- `docs/research_campaign/phase2_no_headroom_reframing_001a.md`;
- `artifacts/research_campaign/phase2_no_headroom_negative_evidence_001a.json`;
- the matching plan/progress/scorecard/ledger entries;
- the matching validation artifact.

Do not delete or rewrite Phase 0, Phase 1, Phase 2 harness outputs, Phase 2
audit failures, Phase 2 repair records, or historical evidence.

## Expected Changed Files

- `docs/research_campaign/phase2_no_headroom_reframing_001a.md`
- `artifacts/research_campaign/phase2_no_headroom_negative_evidence_001a.json`
- `artifacts/research_campaign/phase2_no_headroom_reframing_task_card_validation_001a.json`
- `docs/research_campaign/plan.md`
- `docs/OVERALL_PROGRESS.md`
- `artifacts/research_campaign/stage_scorecard.json`
- `artifacts/research_campaign/experiment_log.jsonl`

## Forbidden Changes

- No candidate mechanism implementation.
- No Phase 3 mechanism search.
- No route tournament.
- No formal Gate execution.
- No runtime, EGO mainline, UI, LLM, AIRI, external service, deployment, push,
  tag, or remote anchor.
- No mutation, normalization, amendment, or refreeze of
  `docs/research/MINIMAL-ENV-SPEC-001A.md`.
- No rewrite of prior failed artifacts into passes.
- No threshold or budget tuning after seeing scores.

## Auto-Remote-Anchor

`forbidden`

## Immediate Next Action After This Card

Run focused start-state validation for this card-opening checkpoint. If it
passes, the next bounded task is to execute only the reframing artifact defined
by this card. Candidate search and Phase 3 remain blocked.
