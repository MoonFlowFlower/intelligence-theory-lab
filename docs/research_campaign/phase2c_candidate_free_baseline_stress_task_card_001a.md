# Phase2C Candidate-Free Baseline Stress Task Card 001A

Task ID:
`RESEARCH-CAMPAIGN-PHASE2C-CANDIDATE-FREE-BASELINE-STRESS-TASK-CARD-001A`

Status: opened for focused validation and read-only audit.

Standing governance contract:
`docs/research_campaign/goal_stage_audit_loop_001a.md`

Source route-check task:
`docs/research_campaign/phase2c_hidden_latent_post_result_route_check_task_card_001a.md`

Source route-check artifact:
`artifacts/research_campaign/phase2c_hidden_latent_post_result_route_check_001a.json`

Source route-check audit:
`artifacts/research_campaign/phase2c_hidden_latent_post_result_route_check_audit_001a.json`

## Problem Definition

The audited Phase2C post-result route check selected
`candidate_free_baseline_stress_task_card`. The reason is narrow: the repaired
candidate-free hidden-latent output has 12 trace rows from one fixed run, and
random plus lookup-family baselines tie the strongest fair macro accuracy
`0.3333333333333333`. That is not enough to authorize candidate mechanism work.

This task opens a bounded candidate-free baseline-stress contract. It defines
what a future stress checkpoint must test before any candidate mechanism,
Phase 3 search, route tournament, runtime/EGO mainline work, push, tag, remote
anchor, terminal verdict, or route-exhaustion claim.

Current task scope is card opening, validation, audit, and campaign-state
repair only. It does not authorize stress execution in this same checkpoint.

## Current Stage And Layer

Current stage:
`phase2c_candidate_free_baseline_stress_task_card_opening`

Layer:
`engineering_implementation + mechanism_hypothesis_governance`

Mainline integration status: none.

Enabled status: local candidate-free baseline-stress task-card governance only.

Real trigger evidence: audited post-result route-check evidence selecting
`candidate_free_baseline_stress_task_card`, route-check audit
`success_reached`, trace row count 12, single fixed run, strongest fair baseline
`fsm_planner` at macro accuracy `0.3333333333333333`, random and lookup-family
ties, candidate mechanism false, Phase 3 false, route tournament false, and
program terminal verdict not recorded.

Claim ceiling: candidate-free baseline-stress task-card readiness only. This
does not prove candidate validity, mechanism validity, learning/adaptation
success, subjectivity, consciousness, real emotion, autonomy, EGO readiness,
companion readiness, runtime/mainline effect, route exhaustion, terminal
verdict, or program completion.

Auto-Remote-Anchor: forbidden.

## Bounded Audit Before Execution

Real objective: freeze the next fair stress boundary for the hidden-latent
surface before any candidate mechanism work.

Problem-definition risk: the route could be misread as permission to start
candidate search. It is not. The current evidence is candidate-free and
under-stressed.

Strongest baseline explanation: the current apparent non-saturation may be a
low-row artifact. A random-like policy and several lookup/cache families tie
the strongest fair baseline under the current access boundary.

Strongest invalidating reason: if larger seed and family stress shows random,
lookup, graph-cache, transition-table, successor-map, FSM, episodic traversal,
trace-only replay, exhaustive legal query, or observation-only behavior reaches
the oracle within the equivalence band, the surface has no candidate headroom
and candidate work remains blocked.

Falsifier for current framing: future stress records baseline saturation,
random equivalence, leakage positive-control failure, replay recomputation
failure, unconsumed ablations, missing provenance, or hidden-label leakage.

Evidence still insufficient: this task card, validation artifact, audit
artifact, or any single future fixed-seed result is not candidate evidence.
Only a later callable stress run with preserved artifacts can support a bounded
route decision.

Mechanism-vs-behavior classification: this task tests no mechanism. It freezes
a candidate-free stress contract that will later test whether the environment
surface has baseline-resistant headroom.

Hard-coding / leakage / weak-baseline checks:

- no candidate mechanism implementation;
- no Phase 3 mechanism search;
- no route tournament;
- no source or test edits in this checkpoint;
- no stress execution or harness output generation in this checkpoint;
- no baseline removal, baseline budget weakening, or access-boundary asymmetry;
- no threshold tuning after seeing stress results;
- no mutation of repaired Phase2C output artifacts;
- no runtime, EGO mainline, UI, LLM, AIRI, deployment, push, tag, or remote
  anchor;
- no terminal verdict, route-exhaustion claim, or program-completion claim.

Rollback plan: if validation or audit fails, preserve the failed artifact,
restore the live frontier to route-check repair, and record the blocker in
`plan.md`, `docs/OVERALL_PROGRESS.md`,
`artifacts/research_campaign/stage_scorecard.json`, and
`artifacts/research_campaign/experiment_log.jsonl`.

Stop condition: any attempted source/test edit, stress execution, candidate
mechanism work, Phase 3 opening, route tournament, runtime/EGO mainline action,
push, tag, remote anchor, terminal verdict, or evidence-claim upgrade stops
this checkpoint.

## Collision Record

### Candidate A: Minimal repeat of the fixed repaired run

Evidence it would produce: another 12-row fixed-run candidate-free output.

Strongest cheap baseline that could match it: the same random and lookup-family
ties already recorded in the route-check artifact.

Leakage / hard-coding risk: low for mutation risk, high for progress theater
because it does not test robustness.

Smallest falsifying test: multi-seed stress changes the baseline ordering or
records saturation.

Expected failure mode: repeatability of an underpowered surface is mistaken for
headroom.

Decision: rejected as the next campaign boundary.

### Candidate B: Candidate-free multi-seed baseline stress contract

Evidence it would produce: a future callable stress run over multiple seeds,
larger train/heldout family counts, larger episode count, equal access and
budget for every baseline, positive-control leakage scans, replay
recomputation, ablations, provenance, and an aggregate route decision.

Strongest cheap baseline that could match it: random, majority,
observation-only, lookup, count-table, transition-table, graph-cache,
successor-map, nearest-neighbor, FSM planner, episodic traversal, trace-only
replay, exhaustive legal query, or any serialized-state/full-bundle decoder
explicitly admitted by the future access boundary.

Leakage / hard-coding risk: medium. The future stress checkpoint must preserve
candidate-visible isolation and include positive controls that would fail on
hidden labels, answer maps, task-family labels, or target-action fields.

Smallest falsifying test: strongest fair baseline reaches the oracle within the
equivalence band on any declared stress slice, random ties strongest after
aggregation, replay fails to consume required inputs, ablations are not rerun,
or provenance is missing.

Expected failure mode: apparent Phase2C headroom collapses under seed/family
stress.

Decision: selected.

### Candidate C: Narrow candidate task card immediately after route check

Evidence it would produce: a future candidate contract but no robustness
evidence.

Strongest cheap baseline that could match it: no direct score, but premature
authorization would make later candidate gains uninterpretable if the surface
collapses under stress.

Leakage / hard-coding risk: medium, because candidate contract design could
adapt to the single fixed repaired output.

Smallest falsifying test: baseline stress records no-headroom or random
equivalence.

Expected failure mode: candidate-card progress before the surface is worth
testing.

Decision: deferred until stress results preserve headroom.

### Candidate D: Surface redesign or closure now

Evidence it would produce: a governance downgrade or redesign task without
running the stress promised by the route check.

Strongest cheap baseline that could match it: not applicable as a score; the
risk is premature closure before the selected stress route is tried.

Leakage / hard-coding risk: low, but it may skip a cheap discriminative check.

Smallest falsifying test: baseline stress can be specified with clear stop
conditions and no candidate work.

Expected failure mode: over-conservative closure without measuring whether
headroom survives stronger stress.

Decision: deferred unless this card fails validation/audit.

## Mainline Target

None. This task has no runtime, EGO mainline, UI, LLM, AIRI, external service,
deployment, product, or companion target.

## Enabled-State Requirement

This checkpoint enables only:

- create this task card;
- validate this task card;
- create a read-only audit artifact for this task card;
- repair stale live-frontier readback so plan/progress/scorecard/ledger agree;
- update `docs/research_campaign/plan.md`;
- update `docs/OVERALL_PROGRESS.md`;
- update `artifacts/research_campaign/stage_scorecard.json`;
- append `artifacts/research_campaign/experiment_log.jsonl`;
- run focused parse/readback validation;
- create one local commit after validation and audit, using exact path staging
  limited to the expected changed files in this card.

No source, tests, stress execution, harness output, candidate mechanism, Phase
3, route tournament, runtime/EGO mainline, push, tag, or remote anchor is
enabled in this checkpoint.

## Future Stress Contract

A later checkpoint may execute candidate-free baseline stress only after this
card validates and audits successfully. That later checkpoint must preserve a
callable computation path and produce machine-readable artifacts.

Minimum future stress axes:

- at least five independent seeds;
- at least three train families and three heldout families per seed;
- at least four episodes per family;
- candidate-free execution only;
- equal access boundary and budget for all baselines;
- aggregate and per-seed strongest-fair-baseline reporting;
- explicit random-tie and lookup-family-tie reporting;
- no candidate-visible hidden labels, answer maps, target actions, or
  task-family labels;
- replay recomputation from serialized state, current observation,
  legal-action/query schema, budget state, and latent belief or memory state;
- positive-control leakage scans;
- rerun ablations under real interventions;
- code-path hashes and source/input artifact hashes;
- no threshold or equivalence-band changes after stress results are seen.

Minimum future baseline battery:

- random;
- majority;
- observation-only;
- lookup;
- count table;
- transition table;
- graph cache;
- successor map;
- nearest neighbor;
- FSM planner;
- episodic traversal;
- trace-only replay;
- exhaustive legal query.

If a future stress runner grants broader access to any candidate-like route,
it must also include access-parity full-bundle or serialized-state decoder
baselines and record the access expansion explicitly.

Minimum future ablation battery:

- memory deletion;
- source-memory deletion;
- latent-rule swap;
- heldout task-family transfer;
- cross-episode reset;
- exploration-budget ablation;
- partial-observation ablation;
- observation-field masking;
- spurious-token injection/removal.

Minimum future output artifacts:

- `artifacts/phase2c_candidate_free_baseline_stress_001a/result.json`;
- `artifacts/phase2c_candidate_free_baseline_stress_001a/trace.jsonl`;
- `artifacts/phase2c_candidate_free_baseline_stress_001a/baseline_comparison.json`;
- `artifacts/phase2c_candidate_free_baseline_stress_001a/ablation_report.json`;
- `artifacts/phase2c_candidate_free_baseline_stress_001a/replay_report.json`;
- `artifacts/phase2c_candidate_free_baseline_stress_001a/leakage_report.json`;
- `artifacts/phase2c_candidate_free_baseline_stress_001a/computed_evidence_provenance.json`;
- `artifacts/phase2c_candidate_free_baseline_stress_001a/failure_manifest.json`;
- `artifacts/research_campaign/phase2c_candidate_free_baseline_stress_001a.json`;
- `artifacts/research_campaign/phase2c_candidate_free_baseline_stress_audit_001a.json`.

Future isolated implementation paths, if a source wrapper is needed:

- `src/phase2c_candidate_free_baseline_stress_001a/`;
- `tests/phase2c_candidate_free_baseline_stress_001a/`.

The future stress checkpoint must not modify
`src/phase2c_hidden_latent_harness_001a/` unless a separate repair card
explicitly authorizes that modification.

## Real-Trigger Evidence Requirement

Execution must read and record:

- repo root, branch, HEAD, upstream/ahead-behind, and
  `git status --short --branch -uall`;
- this task card;
- `docs/research_campaign/goal_stage_audit_loop_001a.md`;
- `docs/research_campaign/plan.md`;
- `docs/OVERALL_PROGRESS.md`;
- `artifacts/research_campaign/stage_scorecard.json`;
- `artifacts/research_campaign/experiment_log.jsonl`;
- `docs/research_campaign/phase2c_hidden_latent_post_result_route_check_task_card_001a.md`;
- `artifacts/research_campaign/phase2c_hidden_latent_post_result_route_check_001a.json`;
- `artifacts/research_campaign/phase2c_hidden_latent_post_result_route_check_audit_001a.json`;
- `artifacts/research_campaign/phase2c_hidden_latent_harness_execution_output_repair_audit_001a.json`;
- `artifacts/research_campaign/phase2c_hidden_latent_harness_execution_output_repair_001a.json`;
- `artifacts/phase2c_hidden_latent_harness_001a_repaired_001a/result.json`;
- `artifacts/phase2c_hidden_latent_harness_001a_repaired_001a/baseline_comparison.json`;
- `artifacts/phase2c_hidden_latent_harness_001a_repaired_001a/trace.jsonl`.

## Hypothesis

If Phase2C evidence is candidate-free but only one fixed run with 12 trace rows
and random/lookup-family baseline ties, then the next valid campaign boundary
is a candidate-free multi-seed baseline-stress contract, not candidate
mechanism work.

## Strongest Baseline

The strongest false explanation remains cheap behavior under equal access:
random, majority, observation-only, lookup, count table, transition table,
graph cache, successor map, nearest neighbor, FSM planner, episodic traversal,
trace-only replay, exhaustive legal query, and any broader-access decoder
granted by the future stress boundary.

If any strongest fair baseline reaches the oracle within the equivalence band,
or if random/lookup-family baselines tie the strongest fair score after stress
aggregation, future candidate work remains blocked and the next route is
surface redesign, closure, or claim downgrade.

## Ablation Requirement

This checkpoint runs no ablation. It freezes that future stress must rerun the
ablation battery under real interventions and consume each ablation result in
the final verdict. Stored-score mutation or static verdict dictionaries are
invalid.

## Trace / Replay Requirement

This checkpoint produces no new trace. Future stress must recompute behavior
from:

```text
serialized_state + current_observation + legal_action_or_query_schema + budget_state + latent_belief_or_memory_state
```

Hash equality, stored-output comparison, or trace-only replay is insufficient
unless paired with the recomputation path and required negative controls.

## Computed-Evidence Provenance Gate

This card-opening checkpoint must record:

- producer command or structured readback source;
- input artifact paths and SHA-256 hashes;
- route-check selected route;
- selected stress design axes;
- expected future output artifacts;
- forbidden changes;
- source/test/stress-output absence in this checkpoint;
- candidate mechanism, Phase 3, route tournament, runtime/mainline, push, tag,
  and remote-anchor flags;
- program terminal status explicitly set to `active_not_complete` and
  `not_recorded`.

Future stress execution must record producer functions, input artifacts,
run IDs, seeds, context/episode IDs, aggregation rule, code path hash,
baseline invocation, ablation invocation, replay recomputation, leakage
positive controls, and source-pin integrity.

## Acceptance Gate

This checkpoint is accepted only if:

- this task card exists;
- focused validation passes;
- read-only audit returns `success_reached`;
- route-check artifact selected `candidate_free_baseline_stress_task_card`;
- route-check audit returned `success_reached`;
- current repaired evidence remains single-run, low-row, and random/cheap-tied;
- future stress axes and baseline battery are frozen;
- no source or test files are changed;
- no stress execution or harness output is generated;
- candidate mechanism, Phase 3, route tournament, runtime/EGO mainline, push,
  tag, and remote anchor remain false/blocked;
- program status remains `active_not_complete`;
- plan/progress/scorecard/ledger agree on the next frontier.

## Stop Conditions

Stop and preserve a failure if:

- source, test, or stress-output files are edited or generated in this
  checkpoint;
- route-check source artifacts are missing, fail to parse, or do not select
  `candidate_free_baseline_stress_task_card`;
- the task attempts to authorize candidate mechanism implementation, Phase 3,
  route tournament, runtime/EGO mainline, UI, LLM, AIRI, deployment, push, tag,
  remote anchor, terminal verdict, route exhaustion, or program completion;
- validation or audit cannot confirm plan/progress/scorecard/ledger agreement;
- the future stress contract weakens baseline access, removes random or
  graph-cache challengers, or omits positive-control leakage scans, replay
  recomputation, ablations, or provenance.

## Rollback Plan

If validation or audit fails, keep this task card and the failure artifact,
restore the live frontier to baseline-stress task-card repair, and record the
blocker in `plan.md`, `docs/OVERALL_PROGRESS.md`,
`artifacts/research_campaign/stage_scorecard.json`, and
`artifacts/research_campaign/experiment_log.jsonl`.

Do not delete prior route-check, repaired-output, invalid-output, audit,
baseline, or negative-evidence records.

## Expected Changed Files

- `docs/research_campaign/phase2c_candidate_free_baseline_stress_task_card_001a.md`
- `artifacts/research_campaign/phase2c_candidate_free_baseline_stress_task_card_validation_001a.json`
- `artifacts/research_campaign/phase2c_candidate_free_baseline_stress_task_card_audit_001a.json`
- `docs/research_campaign/plan.md`
- `docs/OVERALL_PROGRESS.md`
- `artifacts/research_campaign/stage_scorecard.json`
- `artifacts/research_campaign/experiment_log.jsonl`

## Forbidden Changes

- No source or test edits.
- No stress execution or stress-output artifact generation.
- No mutation of repaired or invalid Phase2C output artifacts.
- No candidate mechanism implementation.
- No Phase 3 mechanism search.
- No route tournament.
- No runtime, EGO mainline, UI, LLM, AIRI, external service, deployment,
  push, tag, or remote anchor.
- No program terminal verdict or route-exhaustion claim.
- No deletion, rewriting, or weakening of prior negative, repair, audit, or
  baseline evidence.
- No `git add -A`; local staging must use exact paths only.

## Auto-Remote-Anchor Decision

Auto-Remote-Anchor: forbidden.

Push, tag, and remote anchor are not authorized by this task card.

## Local Commit Decision

Local commit: authorized after focused validation and read-only audit pass, but
only for the exact expected changed files listed in this card.

## Next Minimal Closed-Loop Action

Run focused validation and read-only audit for this task card. If both pass,
the next task may only implement and run candidate-free Phase2C baseline stress
under this reviewed contract; it may not implement candidates, open Phase 3,
run a route tournament, touch runtime/EGO mainline, push, tag, remote-anchor,
or record a terminal verdict without a later bounded route decision.
