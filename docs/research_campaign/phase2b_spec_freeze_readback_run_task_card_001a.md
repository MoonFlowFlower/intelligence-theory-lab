# Phase2B Spec-Freeze / Readback Run Task Card 001A

Task id: `RESEARCH-CAMPAIGN-PHASE2B-SPEC-FREEZE-READBACK-RUN-001A`

Source task id:
`RESEARCH-CAMPAIGN-PHASE2B-SPEC-FREEZE-READBACK-EXECUTION-001A`

Source task card:
`docs/research_campaign/phase2b_spec_freeze_readback_execution_task_card_001a.md`

Source spec draft:
`docs/research/phase2b_minimal_env_reframe_spec_001a.md`

Status: read-only reviewer re-audit returned `success_reached` after preserved
validation-contract, stale Status, scorecard-field, next-action, and
source-hash repairs. The next bounded action may create only the deterministic
freeze manifest and readback result listed in this card.

Auto-Remote-Anchor: forbidden.

## Problem Definition

The Phase2B spec-freeze/readback execution task card was focused-validated and
read-only reviewer re-audited with `success_reached` after stale source-hash,
next-action, and claim-ceiling repairs. That checkpoint explicitly did not
authorize actual freeze/readback artifacts.

This checkpoint is the separate authorization checkpoint for the next bounded
run step. It must prove that authorizing a deterministic spec freeze and
start-state readback is still within the audited candidate-free, baseline-first
route before any freeze file or readback result is created.

This checkpoint does not create the freeze manifest. It does not create the readback result.
It does not execute the Phase2B harness, and does not authorize candidate
mechanisms, Phase 3, route tournaments, runtime/EGO mainline work, push, tag,
or remote anchor.

## Bounded Audit

Layer: engineering implementation + mechanism-hypothesis governance.

Real objective: authorize only the next deterministic Phase2B spec-freeze and
start-state readback run step after focused local validation and read-only
reviewer audit success, while preventing freeze/readback authorization from
becoming a headroom, mechanism, or candidate claim.

Strongest baseline explanation: a source freeze or readback can be mistaken for
measured headroom. The stronger explanation remains that Phase 2 measured
`no_headroom_baseline_saturated`; the next run can only preserve source and
boundary identity before any candidate-free harness work.

Strongest invalidating reason: if this checkpoint creates the freeze file,
creates the readback result, authorizes Phase2B harness execution, authorizes
candidate mechanisms, opens Phase 3, weakens prior baseline/ablation/replay
requirements, or treats authorization as evidence of headroom, the checkpoint
is invalid.

Falsifier for current framing: any current-state artifact shows a freeze file,
readback result, Phase2B harness output, candidate run, Phase 3, route
tournament, runtime/EGO mainline change, push, tag, or remote anchor before this
task card has focused validation and read-only reviewer audit success.

Insufficient evidence: this task card, JSON parse success, source hash
readback, reviewer text without recorded artifact, or any static clean report.
Even after audit success, this checkpoint proves only bounded authorization for
the next freeze/readback run step.

Mechanism-vs-resemblance classification: this checkpoint is governance and
execution-boundary authorization. It does not test behavior, measured headroom,
learning, agency, subjectivity, consciousness, real emotion, autonomy, EGO
readiness, companion readiness, or mainline effect.

Anti-hardcoding audit: the next freeze/readback run must preserve the existing
requirement that target labels, final-action scores, answer maps, full legal
response bundles, semantic action-label leaks, fixture/path target leaks,
unused frozen seeds, and hidden test-only logic paths remain blocked before any
harness implementation or measurement is enabled.

Minimal validation for this checkpoint: local readback must prove this card
exists, references the audited execution task card, preserves the Phase 2
no-headroom result and Phase2B spec hash, keeps freeze/readback disabled before
reviewer audit success, records no future freeze/readback artifacts, and
updates plan/progress/scorecard/ledger consistently.

Stop condition for this card-opening checkpoint: stop if any freeze file,
readback result, `src/`, `tests/`, harness output, oracle score, baseline score,
replay score, ablation score, leakage score, candidate mechanism, Phase 3,
route tournament, runtime/EGO mainline path, push, tag, or remote anchor is
created or authorized before focused validation and read-only reviewer audit
success.

Rollback plan: revert only this task card, its validation/audit artifacts, and
matching plan/progress/scorecard/ledger entries. Do not delete or rewrite Phase
0, Phase 1, Phase 2, no-headroom, reframing, Phase2B spec, validation, or audit
artifacts.

Acceptance signal for this authorization checkpoint: focused validation passes,
read-only reviewer audit reaches `success_reached`, and campaign state records
that the next bounded action may create only the deterministic freeze manifest
and readback result listed below.

## Current Stage

This is a run-authorization task-card checkpoint for a future Phase2B
spec-freeze and start-state readback. It does not create the freeze manifest or
the readback result.

## Mainline Target

None. No runtime, EGO mainline, UI, LLM, AIRI, external service, deployment, or
product path is targeted.

## Enabled-State Requirement

Only task-card drafting, campaign bookkeeping, focused local validation, and
read-only reviewer audit are enabled before reviewer success is recorded.

After focused validation and read-only reviewer audit both succeed, the next
bounded action may create only:

- `docs/research/phase2b_minimal_env_reframe_spec_001a.freeze.json`
- `artifacts/research_campaign/phase2b_spec_freeze_readback_execution_001a.json`

The next bounded action may also create its focused validation and read-only
audit artifacts:

- `artifacts/research_campaign/phase2b_spec_freeze_readback_execution_validation_001a.json`
- `artifacts/research_campaign/phase2b_spec_freeze_readback_execution_audit_001a.json`

Phase2B harness execution, oracle scoring, baseline scoring, replay scoring,
ablation scoring, leakage scoring, candidate mechanisms, Phase 3, route
tournament, Gate execution, runtime/EGO mainline, UI, LLM, AIRI, external
services, deployment, push, tag, and remote anchor remain disabled.

## Real-Trigger Evidence Requirement

The card-opening validation and reviewer audit must read back:

- current repo root, branch, HEAD, upstream/ahead-behind state, and worktree
  status;
- Phase 2 audited no-headroom result;
- Phase 2 no-headroom negative-evidence artifact;
- Phase2B spec draft;
- audited Phase2B spec-freeze/readback execution task card;
- current `plan.md`, `OVERALL_PROGRESS.md`, `stage_scorecard.json`, and
  `experiment_log.jsonl`;
- absence of every future freeze/readback execution artifact listed above;
- SHA256 for `docs/research/MINIMAL-ENV-SPEC-001A.md`;
- SHA256 for `docs/research/phase2b_minimal_env_reframe_spec_001a.md`;
- SHA256 for this task card.

## Hypothesis

A separately validated and reviewer-audited run-authorization card can prevent
the campaign from treating prior task-card audit success as implicit execution
permission, while still allowing the next minimal freeze/readback step without
opening candidate work.

## Strongest Baseline

The strongest false route is authorization theater: flipping a permission flag
is treated as progress toward mechanism evidence. This card therefore preserves:

- strongest fair baseline as `max(all applicable callable baselines)`;
- graph lookup, transition table, successor map, FSM planner, episodic
  traversal, count table, exhaustive legal query, full-bundle decoder,
  serialized-state decoder, passive attackers, and trace-only replay baselines;
- equal budget and input boundary for oracle and all fair baselines;
- no candidate mechanism before measured headroom exists.

## Ablation Requirement

This card-opening checkpoint runs no ablations. It preserves all ablation and
control requirements from `RESEARCH-CAMPAIGN-PHASE2B-CANDIDATE-FREE-HEADROOM-001A`.

## Trace / Replay Requirement

This card-opening checkpoint runs no replay. It preserves the future replay
requirement:

```text
serialized_state + current_observation + legal_action_or_query_schema + budget_state
```

Hash-only, stored-output, or self-reported replay remains invalid.

## Computed-Evidence Provenance Gate

This card-opening checkpoint must record source hashes in its validation
artifact. The next freeze/readback run must record:

- source spec path and SHA256;
- freeze manifest path and SHA256;
- readback result path and SHA256;
- producer function or command for freeze/readback;
- input artifacts;
- run id;
- aggregation rule for start-state validation;
- source file digests or code path hashes for validators;
- generated artifact paths;
- consumed-by-final-verdict flags for validation checks.

Static verdict dictionaries, handwritten clean reports, unconditional pass
reports, and tests that only assert pass are forbidden.

## Acceptance Gate For This Checkpoint

- this task card exists;
- it references the audited Phase2B spec-freeze/readback execution task card;
- it references the Phase2B spec draft;
- it preserves the Phase 2 `no_headroom_baseline_saturated` result;
- it states that no freeze file or readback result is created in this
  checkpoint;
- it authorizes only the next freeze/readback run step after focused validation
  and read-only reviewer audit success;
- it preserves candidate-free baseline-first headroom measurement before
  candidate work;
- it keeps Phase2B harness execution, candidates, Phase 3, route tournament,
  runtime/EGO mainline, push, tag, and remote anchor disabled;
- focused validation verifies source hashes, future path absence, authorization
  flags, JSON/JSONL parseability, and dirty-scope allowlist;
- read-only reviewer audit reaches `success_reached`;
- `phase2b_spec_freeze_authorized` and
  `phase2b_start_state_readback_authorized` may become true only after focused
  validation and read-only reviewer audit success;
- `phase2b_execution_authorized`, `candidate_mechanism_run`, and
  `phase3_opened` remain false.

## Claim Ceiling

Phase2B spec-freeze/readback run authorization only. No freeze manifest,
readback result, Phase2B harness execution, measured headroom, mechanism
validity, learning, agency, subjectivity, consciousness, real emotion,
autonomy, EGO readiness, companion readiness, or mainline-effect claim.

## Stop Conditions

Stop if:

- focused validation fails;
- read-only reviewer audit returns anything other than `success_reached`;
- source hashes are stale or missing;
- any future freeze/readback execution artifact already exists before
  authorization success;
- any harness source/test path is touched;
- any Phase2B execution score is produced;
- any candidate mechanism or Phase 3 path is opened;
- any route tournament, runtime/EGO mainline path, push, tag, or remote anchor
  is attempted;
- prior no-headroom evidence, baseline requirements, ablation requirements,
  replay requirements, or claim ceilings are weakened.

## Expected Changed Files

- `docs/research_campaign/phase2b_spec_freeze_readback_run_task_card_001a.md`
- `artifacts/research_campaign/phase2b_spec_freeze_readback_run_task_card_validation_001a.json`
- `artifacts/research_campaign/phase2b_spec_freeze_readback_run_task_card_audit_001a.json`
- `docs/research_campaign/plan.md`
- `docs/OVERALL_PROGRESS.md`
- `artifacts/research_campaign/stage_scorecard.json`
- `artifacts/research_campaign/goal_stage_audit_loop_validation_001a.json`
- `artifacts/research_campaign/experiment_log.jsonl`

## Forbidden Changes For This Checkpoint

- No `src/` or `tests/` changes.
- No spec freeze file creation.
- No readback result creation.
- No Phase2B harness execution.
- No oracle, baseline, replay, ablation, leakage, or headroom scores.
- No candidate mechanism, Phase 3, route tournament, runtime/EGO mainline,
  UI, LLM, AIRI, external service, deployment, push, tag, or remote anchor.
- No mutation of `docs/research/phase2b_minimal_env_reframe_spec_001a.md`.

## Auto-Remote-Anchor

`forbidden`

## Next Minimal Closed-Loop Action

Create only the deterministic Phase2B spec freeze manifest and start-state
readback result listed in this card, then run focused validation and read-only
audit for those artifacts. Do not execute the Phase2B harness, implement
candidates, open Phase 3, run a route tournament, touch runtime/EGO mainline,
push, tag, or remote-anchor.
