# Phase2B No-Headroom Preservation Reframing 001A

Task id: `RESEARCH-CAMPAIGN-PHASE2B-NO-HEADROOM-PRESERVATION-REFRAMING-001A`

Source audited result:
`artifacts/research_campaign/phase2b_candidate_free_headroom_001a.json`

Source reviewer audit:
`artifacts/research_campaign/phase2b_candidate_free_headroom_audit_001a.json`

Source validation:
`artifacts/research_campaign/phase2b_candidate_free_headroom_validation_001a.json`

## Purpose

Preserve the audited Phase2B candidate-free no-headroom result as
route-blocking negative evidence and open only a bounded preservation/reframing
checkpoint.

This task card does not implement a candidate, open Phase 3, run a route
tournament, mutate or refreeze the Phase2B frozen surface, rerun the Phase2B
harness, touch runtime/EGO mainline, or publish a remote anchor. It only
authorizes preservation of the audited negative evidence and a governance
reframing record that states what must change before any later candidate-free
environment attempt.

## Current Layer

`engineering_implementation + mechanism_hypothesis_governance`

This is not subjectivity validation and not philosophical consciousness work.

## Bounded Audit Before Execution

Real objective: preserve the audited Phase2B no-headroom result without
converting it into a pass-shaped mechanism claim, then define the smallest
future representation question that could justify a new candidate-free
environment-spec attempt.

Problem-definition risk: treating a second audited no-headroom result as a
reason to rescue candidates, weaken baselines, tune thresholds, or open a route
tournament would bypass the strongest evidence now available.

Strongest baseline explanation: even after the Phase2B reframe, the observable
surface remained reducible to a fair `count_table` baseline under the same
budget; the visible-channel oracle and strongest fair baseline both reached
macro F1 `1.0` inside the `0.03` equivalence band.

Strongest invalidating reason: if this checkpoint only writes more governance
language while preserving the same reducible surface, it does not increase
discriminative mechanism evidence.

Falsifier for the current framing: a future candidate-free harness on any
reframed surface again reaches the visible-channel oracle within the equivalence
band using count-table, graph-cache, lookup, transition-table, successor-map,
FSM-planner, episodic-traversal, trace-only replay, serialized-state decoder,
or exhaustive legal-query challengers.

Insufficient evidence: this task card, JSON parse success, progress-file
agreement, or reviewer success on the preservation checkpoint does not prove a
new environment has headroom and does not authorize candidate work.

Mechanism-vs-behavior classification: this task is negative-evidence
preservation and route governance. It does not test or validate a candidate
mechanism.

Hard-coding / leakage / weak-baseline checks:

- no hidden label relocation;
- no renaming answer-bearing fields to evade a scanner;
- no threshold or budget tuning after seeing Phase2B scores;
- no baseline weakening;
- no candidate-specific affordance;
- no second logic path used only by future tests;
- no mutation or refreeze of `docs/research/phase2b_minimal_env_reframe_spec_001a.md`;
- no mutation or refreeze of `docs/research/phase2b_minimal_env_reframe_spec_001a.freeze.json`;
- no candidate mechanism, Phase 3, route tournament, Gate, runtime, EGO
  mainline, UI, LLM, AIRI, deployment, push, tag, or remote anchor.

## Problem Definition

Phase2B produced audited negative evidence:

```text
verdict = no_headroom_baseline_saturated
visible_channel_oracle_macro_f1 = 1.0
strongest_fair_baseline_id = count_table
strongest_fair_baseline_macro_f1 = 1.0
equivalence_band = 0.03
candidate_mechanism_run = false
phase3_opened = false
runtime_or_mainline_touched = false
reviewer_verdict = success_reached
```

The campaign must record this as route-blocking no-headroom evidence for the
current frozen Phase2B surface and keep candidate search, Phase 3, route
tournament, and runtime/EGO mainline work blocked.

## Mainline Target

None. This task has no runtime, EGO mainline, UI, LLM, AIRI, external service,
deployment, product, or companion target.

## Enabled-State Requirement

Only local preservation and governance reframing are enabled:

- create a Phase2B no-headroom negative-evidence record;
- create a Phase2B preservation/reframing result artifact;
- update campaign plan/progress/scorecard/ledger fields to match the bounded
  checkpoint;
- run focused validation and read-only reviewer audit of this checkpoint.

No new harness, candidate mechanism, Phase 3 search, route tournament, Gate,
runtime, EGO mainline, push, tag, or remote anchor is enabled.

## Real-Trigger Evidence Requirement

Execution must read and record:

- repo root, branch, HEAD, upstream/ahead-behind, and `git status --short --branch`;
- this task card;
- `docs/research_campaign/plan.md`;
- `docs/OVERALL_PROGRESS.md`;
- `artifacts/research_campaign/stage_scorecard.json`;
- tail or append-only write target for `artifacts/research_campaign/experiment_log.jsonl`;
- `artifacts/research_campaign/phase2b_candidate_free_headroom_001a.json`;
- `artifacts/research_campaign/phase2b_candidate_free_headroom_validation_001a.json`;
- `artifacts/research_campaign/phase2b_candidate_free_headroom_audit_001a.json`;
- `artifacts/phase2b_candidate_free_headroom_001a/result.json`;
- `artifacts/phase2b_candidate_free_headroom_001a/baseline_comparison.json`;
- `artifacts/phase2b_candidate_free_headroom_001a/computed_evidence_provenance.json`;
- prior Phase 2 no-headroom negative-evidence and reframing records.

## Hypothesis

If a second candidate-free surface is saturated by the strongest fair baseline,
then the campaign should preserve the result as negative evidence and route to
problem-representation redesign only, not to candidate authoring or Phase 3.

## Strongest Baseline

The strongest false explanation for any future progress is representation
theater: another surface may look stricter but remain reducible to cheap
baselines under the same budget.

Future reframing must explicitly challenge:

- count-table / frequency table;
- graph lookup;
- transition table;
- successor map;
- FSM planner;
- episodic traversal;
- trace-only replay / n-gram trace lookup;
- full-bundle and serialized-state decoders;
- exhaustive legal query under the same budget;
- passive observation-only decoders.

## Ablation Requirement

This checkpoint runs no new mechanism ablation. It must preserve that Phase2B
ablation controls passed for the local harness but still produced no headroom.

Future reframing must define fail-able controls for:

- target-label or final-action-score removal;
- legal-query response corruption;
- hidden answer-map injection;
- source-provenance omission;
- replay tamper;
- strongest-baseline equality;
- observation-only shortcut removal.

## Trace / Replay Requirement

This checkpoint must preserve that Phase2B replay recomputation passed from:

```text
serialized_state + current_observation + legal_action_or_query_schema + budget_state
```

Future work must retain replay recomputation. Hash equality, stored-output
equality, and report self-consistency remain insufficient.

## Computed-Evidence Provenance Gate

The preservation and reframing artifacts must be derived from current
machine-readable Phase2B evidence, not narrative recollection. They must record:

- producer command or structured readback source;
- input artifact paths and hashes;
- Phase2B run id;
- aggregation rule for the no-headroom decision;
- route-blocking decision rule;
- consumed-by-final-decision flags.

## Acceptance Gate

This checkpoint is accepted only if:

- the Phase2B no-headroom negative-evidence artifact exists and parses;
- the Phase2B preservation/reframing result artifact exists and parses;
- this task card exists and contains the required task-card fields;
- `plan.md`, `OVERALL_PROGRESS.md`, `stage_scorecard.json`, and
  `experiment_log.jsonl` agree that Phase2B is audited no-headroom and the next
  work is preservation/reframing only;
- candidate search and Phase 3 remain false/not opened;
- no new candidate, route tournament, Gate, runtime, EGO mainline, UI, LLM,
  AIRI, external service, deployment, push, tag, or remote anchor is run;
- local validation confirms JSON/JSONL parseability and expected dirty scope;
- read-only reviewer audit returns `success_reached` or records a bounded
  failure without widening scope.

Reviewer success on this checkpoint still does not authorize candidate search
or Phase 3. It only authorizes opening a separate future problem-representation
redesign card if the campaign continues.

## Claim Ceiling

This task proves only that audited Phase2B no-headroom negative evidence has
been preserved and that the next allowed work is bounded reframing or
problem-representation redesign.

It does not prove mechanism validity, self-awareness, subjective experience,
real emotion, autonomy, agency success, EGO readiness, companion readiness,
runtime readiness, user benefit, or mainline effect.

## Stop Conditions

Stop and record a blocker if:

- the Phase2B result, validation, audit, raw result, baseline comparison, or
  provenance artifact is missing or unparsable;
- any source hash in the no-headroom evidence record does not match the current
  source artifact;
- candidate search, Phase 3, route tournament, Gate, runtime, EGO mainline, UI,
  LLM, AIRI, external service, deployment, push, tag, or remote anchor is
  attempted;
- the Phase2B source spec or freeze manifest is mutated, normalized, amended,
  or refrozen;
- the checkpoint proposes baseline weakening, threshold tuning, or legal-query
  removal without a replacement auditable boundary;
- the no-headroom result is rewritten as a pass-shaped mechanism claim.

## Rollback Plan

Rollback this checkpoint only by reverting:

- `docs/research_campaign/phase2b_no_headroom_preservation_reframing_001a.md`;
- `artifacts/research_campaign/phase2b_no_headroom_negative_evidence_001a.json`;
- `artifacts/research_campaign/phase2b_no_headroom_preservation_reframing_001a.json`;
- `artifacts/research_campaign/phase2b_no_headroom_preservation_reframing_task_card_validation_001a.json`;
- the matching plan/progress/scorecard/ledger entries;
- the matching reviewer audit artifact if one is generated.

Do not delete or rewrite Phase 0, Phase 1, Phase 2, Phase2B harness outputs,
reviewer failures, repair records, or historical evidence.

## Expected Changed Files

- `docs/research_campaign/phase2b_no_headroom_preservation_reframing_001a.md`
- `artifacts/research_campaign/phase2b_no_headroom_negative_evidence_001a.json`
- `artifacts/research_campaign/phase2b_no_headroom_preservation_reframing_001a.json`
- `artifacts/research_campaign/phase2b_no_headroom_preservation_reframing_task_card_validation_001a.json`
- `artifacts/research_campaign/phase2b_no_headroom_preservation_reframing_audit_001a.json`
- `docs/research_campaign/plan.md`
- `docs/OVERALL_PROGRESS.md`
- `artifacts/research_campaign/stage_scorecard.json`
- `artifacts/research_campaign/goal_stage_audit_loop_validation_001a.json`
- `artifacts/research_campaign/experiment_log.jsonl`

## Forbidden Changes

- No candidate mechanism implementation.
- No Phase 3 mechanism search.
- No route tournament.
- No formal Gate execution.
- No runtime, EGO mainline, UI, LLM, AIRI, external service, deployment, push,
  tag, or remote anchor.
- No mutation, normalization, amendment, or refreeze of the Phase2B source spec
  or freeze manifest.
- No rewrite of prior failed artifacts into passes.
- No threshold or budget tuning after seeing scores.

## Auto-Remote-Anchor

`forbidden`

## Immediate Next Action After This Card

Create the Phase2B no-headroom negative-evidence record and preservation
reframing artifact from current machine-readable evidence, then run focused
validation. If validation passes, run read-only reviewer audit. Candidate
mechanisms, Phase 3, route tournament, runtime/EGO mainline, push, tag, and
remote anchor remain blocked.
