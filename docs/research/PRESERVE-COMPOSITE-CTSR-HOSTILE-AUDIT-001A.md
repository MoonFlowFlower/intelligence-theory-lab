# PRESERVE-COMPOSITE-CTSR-HOSTILE-AUDIT-001A

Verdict: `closed_as_mechanism_evidence_frozen_as_negative_evidence`

Current layer: engineering governance / negative-evidence preservation / route-block record only.

Mainline integration status: not integrated; no EGO mainline, runtime, bridge, Gate5, candidate, tournament, UI, LLM, or deployment path was entered.

Enabled status: no new enabled mechanism path; this is documentation and artifact preservation only.

Real trigger evidence: user-provided Claude hostile route-level audit for the COMPOSITE-CROSS-TASK-STATE-REUSE pass-chain, plus read-only repo artifact/source readback listed below.

Claim ceiling: negative-evidence preservation and route-block governance only. This does not prove mechanism validity, Gate validity, agency, subjectivity, consciousness, emotion, autonomy, companion readiness, EGO readiness, runtime readiness, or stable user benefit.

Next minimal closed-loop action: do not cite the current COMPOSITE pass-chain as mechanism evidence. If the hypothesis is retained, open only a separately authorized surface-redesign solvability-inversion preflight; otherwise close or replace the route.

## Bounded Task Card Readback

- Task id: `PRESERVE-COMPOSITE-CTSR-HOSTILE-AUDIT-001A`
- Problem definition: preserve the Claude hostile route-level audit as canonical negative evidence against downstream use of the current COMPOSITE pass-chain as mechanism evidence.
- Current stage/layer: engineering governance / negative-evidence preservation / route-block record only.
- Mainline target: none.
- Enabled-state requirement: no new enabled mechanism path.
- Real-trigger evidence requirement: preserve the hostile audit conclusion that the current COMPOSITE pass-chain should close or be replaced as mechanism evidence.
- Hypothesis: preserving the audit will block downstream tasks from citing the pass-chain unless a future explicitly authorized surface-redesign preflight addresses the block.
- Strongest baseline: the legal-interface oracle collapses to majority, so the surface is legally unsolvable rather than mechanism-discriminative.
- Ablation: not applicable; no rerun or old generator modification is authorized.
- Trace/replay requirement: no replay claim; preserve the audit finding that the current replay path reads answer-bearing serialized state.
- Computed-evidence provenance gate: readback/provenance only; no new mechanism score.
- Acceptance gate: preserve the audit content, route verdict, blocking issues, downstream usage rule, source references, changed-file list, and no-old-artifact-modified flag.
- Claim ceiling: negative-evidence preservation and route-block governance only.
- Stop condition: stop if preservation requires editing old source/artifacts, rerunning candidate code, changing field definitions, or reframing the block as a pass.
- Rollback plan: revert the isolated preservation commit.
- Expected changed files: this report and `artifacts/preserve_composite_ctsr_hostile_audit_001a/{result.json,readback.json}`.
- Forbidden changes: old COMPOSITE source/artifacts/tests/results, ORACLE-BLOCK artifacts, Gate/tournament/bridge/runtime files, field-definition changes, and candidate implementation.
- Auto-Remote-Anchor decision: conditional; only after clean scoped commit and exact branch/tag remote readback.

## Bounded Audit Before Preservation

Real objective: prevent a pass-shaped COMPOSITE route narrative from being reused as mechanism evidence after the hostile audit identified schema split and answer-bearing alias access.

Strongest baseline explanation: the legal-interface oracle is at majority (`0.25` vs `0.25`), while answer-bearing fields exist outside the legal prediction interface. The apparent pass-chain is therefore compatible with an unsolvable legal surface rather than cross-task state reuse evidence.

Strongest reason this task may be invalid: preservation could accidentally upgrade the hostile audit into a new computed mechanism verdict. This report therefore records only route governance and readback; it does not rerun, rescore, repair, or reclassify old experiments as passes.

Falsification of the current framing would require a separate authorized surface where legal-interface prediction is possible above majority without forbidden alias access, where simple lookup/identity/static baselines remain below threshold, and where replay recomputes from non-answer-bearing state. That evidence is not produced here.

Evidence still insufficient after this task: any claim that composite cross-task state reuse is impossible in general, any Gate4 or candidate admissibility claim, any bridge or EGO-mainline readiness claim, or any consciousness/agency/subjectivity claim.

Mechanism-vs-behavior classification: this preservation task tests no mechanism. It preserves a route-level negative-evidence judgment that the current behavior surface is not discriminative mechanism evidence.

Audit hazards preserved: schema split, answer-bearing forbidden aliases, dictionary-lookup replay, handcrafted latent variables, masking fallback outside `ACTION_POOL`, missing latent-action-binding positive control in the pass-chain surface, weak baseline interpretation, and claim inflation risk.

Minimal validation: write isolated preservation files, confirm only allowed files changed, confirm old source/artifacts are untouched by git diff, and report clean status after commit if committed.

Acceptance signal: the route record says the current COMPOSITE pass-chain is closed as mechanism evidence, frozen as negative evidence, and not authorized for candidate, tournament, bridge, runtime, Gate5, or EGO mainline continuation.

## Preserved Hostile Audit Content

The user-provided Claude hostile audit states, in its one-sentence ruling:

```text
这条 route 的三连 pass 不是机制证据,是生成器构造出来的同义反复。"区分性"完全来自 schema split——答案只存在于禁字段,baseline 被禁止读取,所以信息论上必然 <= 多数票。11:04 的 oracle-block 判得对,应予确认。该 route 作为机制证据 CLOSE/REPLACE,产物 FREEZE 为负证据。
```

Faithful preservation of the audit's route judgment:

- The current COMPOSITE pass-chain is closed as mechanism evidence.
- Existing COMPOSITE artifacts are frozen as negative evidence and must not be rewritten into pass-shaped evidence.
- The `PRESERVE-CLAUDE-AUDIT-LEGAL-INTERFACE-ORACLE-BLOCK-001A` block is valid.
- The current surface is legal-interface unsolvable: the oracle is at majority and the legal fields do not contain enough signal to solve heldout Task B actions.
- The replay path is audited as reading `shared_state_update.serialized_state_after.latent_action_binding[probe]`, an answer-bearing / forbidden-alias channel.
- The masking effect is confounded because the masked fallback is `hold`, outside the `ACTION_POOL`.
- The pass-chain leakage positive control missed the `latent_action_binding` alias channel used by replay.
- Downstream candidate, tournament, bridge, runtime, Gate5, or EGO-mainline work is not authorized from this pass-chain.

## Blocking Issues

1. Legal oracle equals majority.

   The prior preserved legal-interface oracle reports `oracle_accuracy = 0.25`, `majority_baseline_accuracy = 0.25`, and `oracle_minus_majority_delta = 0.0`. This supports the hostile audit's conclusion that the current legal surface is not discriminative mechanism evidence.

2. Answers exist only in forbidden or answer-bearing alias fields.

   The hostile audit identifies `hidden.task_b_target_action`, `task_b.post_update_action`, and `shared_state_update.serialized_state_after.latent_action_binding[probe]` as answer-bearing locations. Current field-audit readback confirms `shared_state_update.serialized_state_after`, `task_b.post_update_action`, `hidden.task_b_target_action`, and `task_b.observation.latent_binding_probe` are in the forbidden checked field set.

3. Replay recomputation is dictionary lookup from `serialized_state_after`.

   The hostile audit treats `recompute_task_b_action_from_serialized_state()` as reading the stored binding and returning the target action, not as reconstructing behavior from non-answer-bearing serialized state plus observation. This preservation task makes no replay claim.

4. Handcrafted latent variables are not environment-forced.

   The audit preserves that `task_b_target_action`, `latent_action_binding`, and prediction-error fields are generator-written labels or pointers, not independently forced transition dynamics.

5. Masking fallback outside `ACTION_POOL` makes the masking effect non-discriminative.

   Readback from the current multi-seed artifact records `aggregate_masking_effect_rate = 1.0`, while source readback shows `ACTION_POOL = ["inspect_boundary", "consolidate_trace", "defer_action", "replan_memory"]` and masked/default behavior using `hold`. The hostile audit preserves this as an alphabet-mismatch confound, not mechanism ablation evidence.

6. Leakage positive-control misses the `latent_action_binding` alias channel in the pass-chain surface.

   The pass-chain positive controls block `hidden.task_b_target_action` and `task_b.post_update_action`, but the hostile audit identifies `serialized_state_after.latent_action_binding[probe]` as the replay alias channel that must be covered before any future surface can claim leakage resistance. The later ORACLE-BLOCK artifact checks `shared_state_update.serialized_state_after`, which supports the block rather than rescues the pass-chain.

## Non-Blocking Caveats

- An exact legal-interface lookup oracle is not a universal learner, but the prior oracle readback records heldout legal tuples as unique and disjoint from train (`heldout_tuple_overlap_with_train_count = 0`, `heldout_unique_tuple_count = 80`), with legal identifier-like fields unique in heldout. The hostile audit treats this as enough for route blocking at the current surface.
- Existing COMPOSITE reports may have disciplined claim ceilings; that is evidence-hygiene quality, not mechanism evidence.
- This preservation report does not independently rerun all generators, seeds, or oracle code.

## Downstream Usage Rule

The current COMPOSITE-CROSS-TASK-STATE-REUSE pass-chain must not be cited as mechanism evidence, Gate4 evidence, candidate-admissibility evidence, tournament evidence, bridge evidence, EGO readiness evidence, or mainline effect evidence.

Allowed downstream use:

- cite as negative evidence for the current COMPOSITE surface;
- cite as a route-block record;
- cite as rationale for closure, replacement, or a separately authorized surface-redesign solvability-inversion preflight.

Forbidden downstream use:

- candidate continuation on this surface;
- tournament continuation on this surface;
- Gate5, bridge, runtime, or EGO-mainline entry;
- changing forbidden/allowed field definitions to rescue the old result;
- rewriting old artifacts or result JSONs;
- using remote anchor or this preservation as proof of mechanism validity.

## Referenced Readback Sources

- `C:/Users/LEO/.codex/attachments/1005c002-9ed5-4b30-bcd3-a21859b76306/pasted-text.txt` - hostile audit source, SHA-256 `46648FAA7D749DF1B1292920B89424D88A2641357483B119987C344FE9D9D1F2`.
- `C:/Users/LEO/.codex/attachments/5877b10c-4d4c-4a44-918e-3f23cbf162bd/pasted-text.txt` - task card source, SHA-256 `F89A42FA7694BD3657E1C39E5643F2D64F87ED6CF725DF8DEB54D78ED3264EAD`.
- `docs/research/PRESERVE-CLAUDE-AUDIT-LEGAL-INTERFACE-ORACLE-BLOCK-001A.md` - prior route block report, SHA-256 `B52248F16374193499094CE147E468E9EA73A2ED290AA5F7278237C55150ACED`.
- `artifacts/preserve_claude_audit_legal_interface_oracle_block_001a/result.json` - prior oracle-block result, SHA-256 `19FAB167739CCC8A23B97630C7D8DBA6FC87AB4C2BB936DDB40486FA5B354FC8`.
- `artifacts/preserve_claude_audit_legal_interface_oracle_block_001a/legal_interface_oracle_report.json` - legal oracle readback, SHA-256 `713271B503AC8746B98A517256A97530D0F375D554EA86DEB58BE2D9F866AD28`.
- `artifacts/composite_cross_task_state_reuse_multi_seed_surface_robustness_preflight_001a/result.json` - pass-chain preflight result, SHA-256 `D4294E62E13E831E84CDDD8A4BBBC867BF98A50B4CDF7531DF1AED378368A68D`.
- `artifacts/composite_cross_task_state_reuse_multi_seed_surface_robustness_preflight_001a/field_access_audit.json` - pass-chain field access audit, SHA-256 `8DC5BCBF238E4F5C088A7BE0932DCF9CCE60BEDC681A8A5E86892F655BB0F485`.
- `artifacts/composite_cross_task_state_reuse_multi_seed_surface_robustness_preflight_001a/update_masking_results.json` - masking readback, SHA-256 `36593DD5661A34FACD54E9E0060B8B42F22714980BA7AAFFF2A83015E1808987`.
- `artifacts/composite_cross_task_state_reuse_multi_seed_surface_robustness_preflight_001a/positive_control_results.json` - pass-chain positive controls, SHA-256 `0804B5DC6B24FFD855D7F5827F80369CCABC976E038AA78E41E851D8D9433210`.
- `src/composite_cross_task_state_reuse_multi_seed_surface_robustness_preflight_001a/runner.py` - source readback only, SHA-256 `551B85F0AFE3082B8D97F86B72ADF491D58E9BB3DDCF3C91D197B49EEB31803C`.

## Changed Files

Allowed changed files for this task:

- `docs/research/PRESERVE-COMPOSITE-CTSR-HOSTILE-AUDIT-001A.md`
- `artifacts/preserve_composite_ctsr_hostile_audit_001a/result.json`
- `artifacts/preserve_composite_ctsr_hostile_audit_001a/readback.json`

Old COMPOSITE source/artifact/test/result files are not authorized to change.

## What This Does Not Prove

- consciousness
- subjective experience
- real emotion
- real autonomy
- self-awareness
- agency success
- functional subject success
- stable user benefit
- companion readiness
- EGO readiness
- runtime readiness
- bridge readiness
- Gate4 validity
- Gate5 readiness
- candidate validity
- tournament validity
- mechanism validity
- that all cross-task state reuse theories are impossible
