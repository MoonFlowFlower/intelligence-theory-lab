# SESSION-HANDOFF-FSP-20260703 — S3a/S3b banked; S3c governance saga closed at 001C; R3 sweep RUNNING; rung1 002A audited

- Session scope: operator-executed banks + Claude audits for S3a, S3b, S3c-R1/R2 (two honest STOPs), three governance objects (S3C-BATTERY-SPEC-001A/001B/001C), and the queue-#4 route audit of TLGP rung1 002A FULL_SCOUT. Roles unchanged: Codex implements (no git), Claude audits (numbers recomputed from artifacts; drafted governance objects at operator request, disclosed in-file), operator banks/pushes/signs forks.
- Claim ceiling: instrument code + tuning-procedure + sealing/leak instrumentation + rung1 capacity-diagnostic evidence only. Zero environment-validity, baseline-power, gap, mechanism, learning, agency, or EGO claims. S3d/S5 have NOT run.
- Next session reading order: this file → `FSP-STAGE-LEDGER.md` (L-004 still current; L-005 pending) → memory `fsp-route-program-001a` → memory `itl-tlgp-rung1-capacity-sweep-002a-full-scout-route-audit`.

## 1. State (end of 2026-07-03)

- Commit chain (all pushed, origin/codex/meta-theory-scaffold): `b812552` TLGP anchor → `f1cf1aa` S0 → `6bb6107` → `ac4fa20` L-004 → `9aa6680` S1 → `05a9db0` S2 slice → `2c9a4ec` S2b → `03845e5` S2c → `326bd51` S2d → `4afcceec` S2e (S2 complete) → `3bf24cc` S3a → `ed39677` S3b → `4c77116` S3c spec-gap STOP + 001A → `d8a3324` S3c-R1 STOP + 001B → `567af96` S3c-R2 STOP + 001C.
- Stage position: S0✓ S1✓ S2✓ S3a✓ S3b✓ → **S3c-R3 sweep RUNNING** (launched under 001C; projection 24.257 CPU-h vs 30.0 line; config-parallel threads=1 permitted) → S3d (should-win certs + NULL env) → S4 → S5 → S6.
- Frozen source stack for S3c (Codex verifies all SEVEN before work; python blob-sha, no git):
  card `0a5e38a5dbe9…` / constitution `882a68de2973…` / execution plan `c07eec2ac384…` / frozen_design `32d2cbd538f3…` / 001A `2da9e5693255…` / 001B `bfabd9ebfbd4…` / 001C `2940db67ad3b…`. Plus F2 vocabulary sha256 `3a08b3b5a387…` (do not rebuild).
- The checkpoint script FROZEN_FILES still guards only the original 4; 001A/B/C are guarded at prompt level + audit. Optional hardening open: add them to the script.
- Track T: rung1 002A FULL_SCOUT completed exit 0 and AUDITED (see §3); Track-T bank + L-005 ledger transition NOT yet done (operator sequence in §4).

## 2. Operational contract (carried forward + this session's additions)

1. All prior rules from SESSION-HANDOFF-FSP-20260702 §2 remain: private-index checkpoint script for every Track-F bank; Codex = implementer, NO git; ANTI-IDLE clause in every prompt; FUSE illusions (file-API reads for edited files; sandbox pytest zero evidential force).
2. **Git-view illusions (expanded)**: the shared index now shows large fake staged-D/M state; `git status`/`git diff` vs worktree are UNTRUSTWORTHY in both host and sandbox. Authoritative checks: raw `sha256sum` vs recorded values, `git hash-object` vs `ls-tree <commit>` blob (beware CRLF: banked blobs are LF-normalized; disk may be CRLF → false CHANGED), file-API reads for edited files. Three S3a artifacts flagged "CHANGED" this session were byte-identical by sha256 (EOL/stale-index illusion).
3. **STOP discipline precedent strengthened**: two honest S3c STOPs (spec-gap; projection>line) with preserved failure manifests, then repairs by governance supersede — never by executor discretion. Executor may not shrink grids, substitute members, or touch budget lines.
4. **Budget line vs evidence threshold (001C precedent)**: budget lines are operator-adjustable with a signed decision note + full score-exposure disclosure; evidence thresholds/gates are not. The S2/S2e 24 CPU-h TRACTABILITY line is claim-bearing and firewalled — never cite 001C as precedent for moving it.
5. Governance objects are drafted by Claude at operator request pre-registration-clean (before scores exist), approved by the operator's bank act, pinned by blob sha in the next prompt.

## 3. Audit trail of this session

- **S3a** (3bf24cc): ACCEPT. 10 recipe-only trajectory sets, member_view+adjudicator sha256 bound; PrefixOnlyPredictor + 4 degenerates + 2 LS w/ declared conventions; AST sealing import-graph test. Blocker B1: leak-scan production report VACUOUS (scanned_files=[]) → mandated at S3b head.
- **S3b** (ed39677): ACCEPT. B1 CLOSED (10 sets × 300,000 records scanned, per-set sha256_verified, 0 findings); graph-cache ×5 REAL fitted tables w/ alias_reports (429/195/13/24422/675415 keys; episodic heldout coverage 0.6936, fallback cascade declared) + RAG-k5 + NN-user; partition-order STOP preserved (bug in new S3b producer; S3a code blob-verified untouched); test counts reconciled 48→56 items (parametrize).
- **S3c-R1** (4c77116, d8a3324): executor STOP `blocked_by_missing_or_ambiguous_frozen_s3c_spec` — verified REAL (frozen_design has member names only; obs_decoder_gate is the T5.2 θ-gate, no-conflation) → 001A addendum (24 decoder configs = 8×3 anchored to gate cap; 640/160 deterministic split; seq specs w/ W15 session-reset; 24 CPU-h line; pooled-fitting risk registered w/ S3d per-set repair path). Then PART 0 STOP: projection 4748.76 CPU-h — diagnosed as F2 full-vocab (33,824-dim) dense-HGB memory-infeasible + per-example feature builds + unmeasured GRU heuristic weights → 001B delta-supersede (top-1024 thresholded n-gram vocab built fit-users-only; GBT half-data DISCLOSED handicap; incremental features + teacher-forced mandatory w/ equivalence tests; single-thread accounting; GPU prohibited; line unchanged).
- **S3c-R2** (567af96): efficiency delivered (4748→24.257 CPU-h, 196×; six measured per-class costs; arithmetic independently verified) but 24.257 > 24.0 → honest STOP. Operator signed fork A → **001C**: line raised once 24→30 CPU-h (budget decision; challenger strength untouched; S2 firewall written; 30h runtime guard; config-parallel threads=1 accounting; score exposure = all chance-level, disclosed in-file).
- **TLGP rung1 002A FULL_SCOUT** (queue-#4 audit per pre-registered protocol `TLGP-RUNG1-CAPACITY-SWEEP-002A-QUEUE4-ROUTE-AUDIT-PREREG-001A.md`): R0 ✓ (54/54 recomputed from trace at 1e-9), R1 ✓ (admissibility re-derived), R2 ✓ (485-600 epochs, plateaus, 001A-echo excluded), R3 ✓ (ablation collapse recomputed 0 fail; no fair saturation, fair_max≈0.33 vs ideal 1.0; leakage clean; no hairline — clears 3/3 @0.99+, bar 0.9), R5 ✓ (pins all_match, runner on-disk hash match, replay 37,800 rows max_abs_diff=0.0). **R4: verdict DOWNGRADED** — bp C0=8→C1=16→C2=16 fails the pre-registered strict-increase clause (grid top 172, not censored). Standing verdict: **H_cap (segment-bounded)** — capacity binds C0→C1; C1→C2 bar-flat unresolved at the 2× grid jump (16→32) while continuous scores show capacity gains at every k in all seeds (k=32: 0.79→0.83). Route `reopen_rung3_powered_after_powered_rung1_confirm` PRESERVED with condition: confirm card must add finer k grid between 16 and 32 (e.g., 20/24/28) or pre-register diminishing bar-crossing returns.

## 4. Immediate queue for next session

1. **S3c-R3 report arrives → audit checklist**: 7 freeze shas + vocab sha verified; cumulative CPU-h trace ≤ 30.0 (runtime guard honored); per-config wall-clocks fresh (ANTI-IDLE); selection = argmax internal-val macro-balanced accuracy, tie→lower index; NO heldout numbers anywhere; models/recipes persisted w/ sha256 + declared FP nondeterminism; sealing test extended to obs_decoders/seq_models and passing; tests/fsp_pum_env from 69 items; artifact sha256s spot-recomputed. Then bank (paths: src/fsp_pum_env, tests/fsp_pum_env, artifacts/FSP-PUM-ENV-IDPROBE-001A, + this handoff file) and draft S3d prompt.
2. **S3d prompt must carry** (accumulated obligations): should-win certificates for EVERY member per constitution §7 + NULL-env control; winnable cells needed for constant one-hot degenerates (predict_all=31 / predict_none=0); episodic_traversal 31% heldout fallback noted; pooled-fitting adequacy adjudicated here (cert failure = FAIL_BASELINE_UNDERPOWERED → pre-registered per-set refit path per 001A §2); GBT half-data handicap adjudicated here (001B Amendment 2).
3. **Track-T operator sequence** (order-sensitive, after R3 banked): confirm the rung1 Codex SESSION is fully closed (its harness fires `git add -u` per turn) → delete `.git/index.lock` → `git reset` (mixed) to rebuild the poisoned shared index → Track-T bank of FULL_SCOUT (explicit paths; trace/val_curves via LFS per .gitattributes; the Track-F script REFUSES TLGP paths — use manual plumbing or a Track-T script variant; ask Claude for exact commands at that point) → **L-005 ledger transition**, suggested wording: `L-005: TLGP rung1 002A FULL_SCOUT audited — H_cap segment-bounded (bp 8→16→16; C1→C2 unresolved at 2x grid; R0-R5 clean, replay exact); route reopen_rung3_powered_after_powered_rung1_confirm ACCEPTED w/ confirm-card grid-resolution requirement.`
4. Powered rung1 confirm card: design-only until operator authorizes; must include the finer-grid requirement.

## 5. Standing debts and queued objects

- **S4 obligations** (unchanged from 20260702 §4, all still open): planted-VALUE leak mutant; `stable_fact` RNG stream reconciliation; runtime sealing enforcement at harness level; per-gap N interpretation note; policy-class compute evaluation plan (naive 20× = 170 CPU-h).
- **Design-only queue (P1 gate)**: MPVL/EBPP probe-policy control baseline (+ its causal-version 6-step loop noted 2026-07-02); FSP-BORROWED-STACK-REGISTRY-001A; causal_identification_contract fields to be folded into MECHANISM-SIGNATURE-VERDICT-STANDARD as 001B at next causal-card authoring (NOT a fourth standard) — see memory `itl-causal-claim-ladder-and-hume-compression` (C0-C3 claim ladder now governs causal-claim wording).
- Optional hardening: add 001A/B/C blob shas to checkpoint script FROZEN_FILES.

## 6. Evidence-status marks

- S3a/S3b/S3c-R1/R2 artifacts + three governance objects: repo-committed, pushed, audited. Two S3c failure manifests + one projection-failure v2 preserved verbatim.
- rung1 FULL_SCOUT bundle: on disk, audited, NOT yet banked (LFS + lock sequence pending).
- Nothing in this session moves any claim: no environment-validity, no Gap certificates, no mechanism evidence, no baseline-power evidence (S3d adjudicates that). First claim-bearing event remains S5 adjudicated in S6.
- Score exposure to date (for pre-registration hygiene): chance-level internal-validation figures only (documented in 001C §"score exposure"); rung1 capacity diagnostics are Track-T scoped and do not touch PUM-ENV claims.
