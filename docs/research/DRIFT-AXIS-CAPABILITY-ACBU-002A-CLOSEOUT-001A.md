# DRIFT-AXIS-CAPABILITY-ACBU-002A — CLOSEOUT-001A (closed by bounded analysis; NOT run)

Status: ROUTE CLOSED_BY_BOUNDED_ANALYSIS. Endorsed by operator + independent Red-audit (R2). Phase-2
STEP-0 was NOT run. Docs-only. No mechanism-validity claim.

## layer / claim ceiling
learning-adaptation / mechanism-hypothesis benchmark design. This closeout supports at most a bounded
NEGATIVE route decision. It does NOT prove mechanism invalidity, and asserts nothing about equal-access
mechanism, OOD/extrapolation generalization, learning-in-general, agency, autonomy, subjectivity,
consciousness, or EGO/companion readiness.

## lineage
- ACBU-001A STEP-0 ideal-gap: REJECTED (definitional) — POMDP recurrent meta-policy ≡ belief-state
  optimal under unbounded resources; gap ≡ 0.
- ACBU-002A (PREREG-002A): reframed to a matched-coverage, resource-bounded structured-vs-amortized
  sample-efficiency kill-test after the HAZARD_COVERAGE_ASYMMETRY was caught in 001A.
- Independent Red-audit R1: fixed single-source (B7), cell-param oracle guard (B2), and the false
  R* "validated upper bound" (B4 → raw metric, R_ref logged reference).
- Independent Red-audit R2: fixed the baseline-fairness rig path (B4: max-feasible baseline rung +
  adequacy fail-closed at bar = MDE, no ε_adeq slack) and R_ref semantic drift (B7); added the
  `F_train` committed-row definition.

## closure basis (bounded analysis)
Once (i) hazard coverage is matched (candidate prior support = Broad training hazard distribution =
Uniform(H_grid)), (ii) the comparator is the max-feasible-trained Broad, and (iii) adequacy is
fail-closed at bar = MDE, the candidate's achievable advantage over Broad is structurally compressed to
the MDE band. Reason: R_ref is the known-params version of the SAME §4 candidate policy, so the
inferred-params candidate satisfies candidate ≲ R_ref, while an adequate max-feasible Broad is within MDE
of R_ref; hence sep_broad is pinned within ~MDE. The only residual PROCEED window is a thin
myopic-reference anomaly (candidate's parameter-uncertainty-driven exploration transiently exceeding the
myopic known-params one-step-KG R_ref, flagged by R_REF_EXCEEDED_FLAG). Expected run verdicts are
therefore near-certainly SATURATED_BASELINE_EQUIVALENCE or UNDERPOWERED_BASELINE.

## verdict
Drift-axis matched-coverage sample-efficiency (this design point) has no attributable positive headroom
for explicit structured online action-conditioned belief-update over a strongest-feasible amortized
recurrent baseline. This is a bounded NEGATIVE route decision, weaker than the 001A definitional kill
(here the anomaly window is thin, not provably empty), consistent with the lab's recurring finding that
a fair strongest-feasible baseline saturates the candidate's separation.

## what this does NOT prove
Not a mechanism-invalidity proof; not definitional (the myopic-reference anomaly is not excluded, only
bounded to ~MDE); nothing about a genuinely non-amortizable OOD/extrapolation environment; nothing about
consciousness/agency/subjectivity/autonomy/EGO readiness.

## reopen condition
Do NOT reopen by re-running ACBU-002A. A successor may reopen the drift axis only with a NEW card that
changes the coverage/adequacy/reference structure — e.g. a genuinely non-amortizable extrapolation
environment with SYMMETRIC access for candidate and baseline, or a non-myopic R_ref that removes the
anomaly window — and must cite this closeout as prior negative evidence.

## provenance
Frozen spec: PREREG-002A + ACBU-002A card, banked (docs-only) at the re-bank commit recorded in the
host-git readback accompanying this closeout. PREREG-001A retained (no-delete) as superseded record.
This closeout carries no scoring artifacts (route closed without a run).
