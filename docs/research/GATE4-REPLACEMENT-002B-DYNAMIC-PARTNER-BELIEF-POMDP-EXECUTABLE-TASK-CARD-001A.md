# GATE4-REPLACEMENT-002B Dynamic Partner-Belief POMDP Executable Task Card

Task ID: GATE4-REPLACEMENT-002B-DYNAMIC-PARTNER-BELIEF-POMDP-EXECUTABLE-TASK-CARD-001A

Canonical artifact directory:

```text
artifacts/gate4_replacement_002b_dynamic_partner_belief_pomdp_executable_task_card_001a/
```

Future executable task card:

```text
GATE4-REPLACEMENT-002C-DYNAMIC-PARTNER-BELIEF-POMDP-EXECUTION-001A
```

## Verdict

`gate4_replacement_002b_dynamic_partner_belief_pomdp_executable_task_card_001a_pass`

This is an engineering-governance drafting result only. It does not implement a POMDP harness, source scoring code, tests, datasets, experiment outputs, social-latent evidence, or Gate4 validity.

## Layer

Engineering-governance plus mechanism-hypothesis task-card drafting. No upgrade is authorized to subjectivity validation, philosophical consciousness, Gate5, admission, bridge, runtime, EGO-mainline, UI, companion behavior, relationship learning, emotion systems, proactive behavior, LLM integration, AIRI integration, deployment, API keys, or external services.

## Source Boundary

The 002A sealed boundary was read back before drafting:

```text
c2bfa9a337e1f10588d43c12e0c84c3ba14f493b
```

The local HEAD, local tag, remote branch, and remote tag all matched that boundary at source readback. The pinned tag was:

```text
remote-anchor-gate4-replacement-002a-mechanism-problem-definition-baseline-first-design-brief-001a-c2bfa9a
```

## Inherited Negative Evidence

001E is inherited as negative evidence:

- candidate score: `1.0`
- pair-count table score: `1.0`
- full-bundle decoder score: `1.0`
- serialized-state decoder score: `1.0`
- strongest faithful baseline: `pair_count_table`
- stop condition: `pair_count_table_tied_candidate`

001F closed 001D-derived repair attempts. Continuing to patch 001D, preserving target-bearing bundle or feedback recoverability, constructing candidate-specific puzzles, or denying baselines candidate-equivalent access remains closed.

002A reset the problem definition and required baseline-first design before any future candidate success metric.

## Anti-Sycophancy Audit

Strongest baseline explanation: apparent partner-belief behavior may be produced by pair counts, trace lookup, partner-ID lookup, preference tables, full-bundle decoding, serialized-state decoding, finite-state policy, query-capable imitation, belief tables, graph/cache controls, or a non-social world model.

Strongest reason this task may be invalid: a POMDP-style toy environment can still encode hidden partner type, policy shift, reward signal, target label, or hidden state in visible fields, serialized state, context IDs, paths, trace ordering, or finite scripts.

What would falsify the framing: any faithful callable non-mechanism baseline tying or beating the candidate on heldout or counterfactual partner dynamics; missed positive-control leakage; no-signal false success; social-latent ablation without degradation; replay that cannot recompute behavior from serialized state plus observation; or inability to define fair faithful baselines.

Evidence still insufficient: aggregate score, readable traces, post-hoc explanations, stored action replay, replay hashes, schema validity, or tests that assert pass.

Mechanism or behavioral resemblance: 002B tests neither. It creates a future contract for bounded mechanism-proxy testing and blocks behavioral-resemblance-only success.

## 002C Framing

The future 002C task must implement an isolated dynamic partner-belief POMDP-style experiment with partial observability, at least two partner latent types, partner policy shift after interaction history, heldout partner dynamics, counterfactual probes, interventions on candidate social-latent state and partner-observation stream, a positive-control leakage condition, a negative-control no-signal condition, and explicit train, heldout, counterfactual, and ablation context IDs.

The target must not be directly recoverable from partner ID, visible labels, pair counts, bundle fields, serialized state, trace ordering alone, menu preference tables, fixed finite-state scripts, answer labels, context IDs, fixture names, paths, action names, or reward labels.

## Baseline-First Contract

002C must implement independent callable baselines before candidate scoring:

- random/control baseline;
- pair-count baseline;
- n-gram / trace lookup baseline;
- partner-ID baseline;
- preference-table baseline;
- full-bundle decoder;
- serialized-state decoder;
- finite-state policy baseline;
- query-capable imitation baseline;
- belief-table baseline;
- oracle-access upper bound;
- ablated-candidate baseline with social-latent update disabled;
- non-social world-model baseline.

Each baseline must record producer function, input artifacts, run ID, seed, train context IDs, heldout context IDs, counterfactual context IDs, episode IDs, aggregation rule, code path hash, access-rights declaration, and failure mode ruled out.

If a faithful non-oracle baseline ties or beats the candidate, 002C must report baseline-equivalence negative evidence.

## Ablation, Replay, And Provenance

002C ablations must rerun episodes under real interventions. Required ablations disable the social-latent update path, freeze other-state after initial observation, shuffle partner-observation history, remove partner policy shift, swap partner latent dynamics, mask partner ID and explicit labels, inject target leakage as positive control, and remove social signal as negative control.

Replay must recompute candidate and baseline behavior from serialized state plus observation. Hash-only replay, stored action replay, and stored metric replay are invalid.

Every reported result, baseline, ablation, contrast, leakage, and replay metric must record producer function, input artifacts, run ID, seed, context IDs, episode IDs, aggregation rule, and code path hash.

## Acceptance Gate

002C may pass only if the candidate beats the strongest faithful non-mechanism baseline on heldout and counterfactual partner dynamics; full-bundle and serialized-state decoders fail below the candidate under fair access; pair-count, partner-ID, trace-lookup, finite-state, and belief-table baselines fail below the candidate; ablated candidate degrades meaningfully; positive-control leakage is detected; negative-control no-signal blocks false success; replay recomputes behavior; all baselines and ablations are callable and invoked; provenance is complete; failure-path tests are present; and forbidden scope files remain unchanged.

## Stop And Rollback

Stop and preserve a blocked report if the design depends on encryption, obfuscation, field hiding, partner-ID lookup, answer-label recovery, trace-only lookup, candidate-specific puzzle construction, unfair baseline access, non-rerun ablations, hash-only replay, or missing positive/negative controls.

If source modules, tests, harnesses, scoring code, datasets, or experiment artifacts are created outside the future 002C allowed paths, revert those changes and preserve a blocked report. If prior 001D/001E/001F/002A/002B artifacts are mutated, revert those mutations and preserve a blocked report.

## Claim Ceiling

Allowed claims:

- bounded executable-task-card drafting for a future Gate4 replacement experiment;
- baseline-first 002C execution constraints;
- governance readiness to review or later execute 002C.

Forbidden claims:

- valid Gate4;
- social-latent inference evidence;
- mechanism validity;
- agency;
- selfhood;
- consciousness;
- emotion;
- autonomy;
- EGO readiness;
- stable user benefit;
- Gate5 readiness;
- admission readiness;
- bridge readiness;
- runtime readiness;
- companion readiness;
- proof that Bio-CMBC, CVPSM, VCCO, CMBC, or R/G is correct.

## What This Does Not Prove

This does not prove that dynamic partner-belief POMDP is sufficient, that any social-latent mechanism works, that future baselines will be defeated, that Gate4 is valid, or that any system has agency, selfhood, consciousness, emotion, autonomy, EGO readiness, companion readiness, or stable user benefit.
