# CLAUDE-INDEPENDENT-DRAFT-ROUTE-C-CANDIDATE-CARD-001A-R1-REAUDIT-001A

R1 hostile re-audit preservation for
`DRAFT-ROUTE-C-CANDIDATE-CARD-001A-R1`.

Date preserved: 2026-06-16

## Preservation Status

- Re-audit source: operator-provided pasted task text in the current Codex
  session.
- Preservation mode: local faithful preservation of the reported independent
  hostile re-audit verdict, authorization boundary, and required bindings.
- Codex role in this file: preservation only.
- Codex did not perform an independent re-audit.
- Candidate implementation authorized: false.
- Source/test/runtime/Gate/mainline changes authorized: false.
- Push/tag/remote-anchor authorized: false.
- Auto-Remote-Anchor: forbidden.

## Verdict Preserved

`accepted_for_candidate_implementation_card_drafting_only`

The preserved verdict authorizes drafting a bounded future implementation card
only:

`ROUTE-C-CANDIDATE-IMPLEMENTATION-CARD-001A`

It does not authorize candidate implementation, source code changes, tests, Gate
execution, mainline wiring, runtime/live integration, push, tag, or remote
anchor.

## Current Layer

`engineering-governance / implementation-card drafting only`

## Mainline Integration Status

None. This preservation does not touch or authorize any EGO mainline, Gate,
runtime, bridge, scheduler, admission, product, deployment, UI, LLM, AIRI, or
live path.

## Enabled Status

None. No executable candidate, CLI implementation, pytest implementation, Gate
runner, runtime path, or live path is enabled.

## Real Trigger Evidence

Local docs/artifacts only: preservation of the reported R1 hostile re-audit and
drafting of a future implementation-card contract. No mechanism score,
candidate score, Gate score, or live trigger is produced.

## Preserved Hypothesis Boundary

A future separately authorized Route C candidate may test a bounded
self-boundary / hidden-self-set inference proxy only if it beats both:

1. the strongest passive baseline family; and
2. the strongest fair interventional baseline family,

while surviving provenance, replay, leakage, ablation, margin, and saturation
failability gates.

This preservation task does not test that hypothesis.

## RF-1: Forged-Provenance Option-2 Closure

The future implementation card must bind these requirements:

- Future implementation must extend forged-provenance positive controls to the
  option-2 path.
- It must include a co-forged-input positive control where value, basis, and
  recorded inputs are made self-consistent.
- The provenance gate must fail closed when recorded inputs are not pinned to
  independent source artifact hashes.
- Re-derived basis is admissible only when recorded inputs are source-pinned and
  independently hashed.
- Self-declared input/basis consistency is insufficient.
- Every material row must either:
  - re-execute the producer on recorded, source-pinned inputs and compare output
    to recorded value; or
  - rederive basis from source-pinned inputs and prove the source-pinned inputs
    were not co-forged.
- Candidate-run material rows are in scope; this cannot be deferred to Gate
  level.

## RF-2: Pre-Run Mechanism-Component Lock

The future implementation card must bind these requirements:

- The mechanism component under test must be specified before any candidate
  run.
- It must be minimal, independently specified outside candidate source, and
  hashed before run.
- Candidate advantage cannot be used after the fact to define the mechanism
  component.
- Feature-impoverishment control must use this pre-hashed component.
- If granting this component to the fair baseline does not allow it to approach
  candidate performance, the baseline is deemed unfair/impoverished and the run
  is invalid.

## RF-3: Independent Anchoring Of B1-B5 / N1-N4

The future implementation card must restate and bind all R1 B1-B5 and N1-N4
requirements. It must not rely only on the preserved
`CLAUDE-INDEPENDENT-DRAFT-ROUTE-C-CANDIDATE-CARD-001A-AUDIT-001A` artifact.

Required independent local readbacks:

- B1 F-forge closure.
- B2 Phase 0 passive-family superset.
- B3 fair-interventional baseline hardening.
- B4 margin/saturation failability.
- B5 six graph-cache challengers.
- N1 prior negatives.
- N2 frozen margin predeclaration/hash.
- N3 replay tautology exclusion.
- N4 truth-seed isolation.

## RF-4: Future Implementation Diff Hygiene

The future implementation card must require git-verifiable diff evidence. It
must prove exactly which files changed, and must separately prove whether any
`src/`, `tests/`, `scripts/`, Gate, mainline, runtime, scheduler, bridge,
product/admission, credential, or push-script path was touched.

Push, tag, and remote anchor remain forbidden while PAT hygiene is unresolved.

## Baseline And Gate Bindings Preserved

The future implementation card must include the passive family as a superset of
the accepted Phase 0 family:

- `positional_first_k`
- `mean`
- `variance`
- `correlation`
- `pca_subspace`
- `cross_episode`
- `supervised`
- `legal_field_membership`

Additional passive attackers may be added, but none of the Phase 0 attackers may
be removed or weakened. `obs_only_family_max` must be computed as max over the
union.

The future fair interventional baseline family must include:

- random legal intervention policy.
- greedy information-gain policy.
- exhaustive legal query policy within same budget.
- Bayesian / likelihood updater if applicable.
- lookup-capable interventional imitation baseline.
- direct-objective optimizer using all legal non-candidate mechanisms.
- all six graph-cache challengers:
  - `graph_lookup`
  - `transition_table`
  - `successor_map`
  - `count_table`
  - `fsm_planner`
  - `episodic_traversal`

All must enter the saturation judgment.

The future candidate must beat `obs_only_family_max` and the strongest fair
interventional baseline under predeclared, hashed margin values. If the
strongest fair interventional baseline equals, exceeds, or approaches the
candidate within frozen margin, Route C must close or downgrade as bounded
negative evidence. Both-scored-high is not success.

## Required Future Ablations

Future implementation must require real rerun ablations, not report edits:

- remove mechanism-specific state.
- remove interventional access.
- remove update path.
- remove replay state.
- remove candidate memory/cache.
- remove self-boundary-specific feature.
- shuffle target labels.
- swap twin pairs where passive distribution is held constant.
- remove candidate-only fields.
- replace candidate policy with strongest fair baseline policy.

## Required Future Replay, Leakage, And Computed Evidence

Replay must recompute candidate behavior from serialized state plus legal
observation/action history. Stored-hash comparison alone is insufficient.
Tautological replay controls do not count toward failability.

Future leakage positive controls must cover value-level passive leakage,
schema/name/key leakage, order leakage, action/API leakage, serialized-state
leakage, result-json leakage, candidate-only-field leakage, train/heldout
contamination, truth-seed contamination, and unused frozen seed.

No mechanism score may come from literals, static dictionaries, self-report,
unconditional clean reports, or hand-filled JSON.

Every material score must include:

- producer_function.
- source path.
- recorded inputs.
- source-pinned input artifact hash.
- run_id.
- seed.
- context_id.
- episode_id.
- aggregation.
- code_path_hash.
- recompute command.
- output artifact.

Material rows include candidate score, passive baseline scores,
`obs_only_family_max`, fair interventional baseline scores, graph-cache
challenger scores, margin decision, saturation decision, ablation scores, replay
recomputation, leakage scan, leakage positive controls, forged-provenance
controls, and final verdict selection.

## Acceptance Gate For This Preservation Task

Accepted only if:

- R1 re-audit is preserved faithfully.
- `ROUTE-C-CANDIDATE-IMPLEMENTATION-CARD-001A` is drafted.
- RF-1 through RF-4 are explicitly bound.
- implementation remains forbidden.
- no candidate code is created.
- no source/test/runtime/Gate/mainline path is changed.
- no push/tag/remote anchor is attempted.
- PAT or any secret is not printed, copied, exposed, or modified.
- claim ceiling remains implementation-card drafting only.

## Claim Ceiling

R1 hostile re-audit preservation and bounded candidate-implementation-card
drafting only. No candidate implementation. No mechanism evidence. No Gate pass.
No mainline effect. No runtime/live effect. No agency, autonomy, consciousness,
emotion, stable user benefit, or EGO readiness.

## Stop Condition

Stop immediately if candidate implementation is introduced; source or test code
is modified; Gate/mainline/runtime/bridge/scheduler/product path is touched;
push/tag/remote anchor is attempted; PAT or a secret is printed or modified; the
implementation card weakens R1 B1-B5 or RF-1 through RF-4; or any result claims
Route C works.

## Rollback Plan

If forbidden files are touched, revert immediately. If the implementation card
fails to bind RF-1 through RF-4, preserve a blocker and do not proceed. If any
candidate code appears, revert and record the violation. If push/tag/anchor is
attempted, stop and report a blocker.

## Next Minimal Closed-Loop Action

Use the preserved R1 hostile re-audit to draft
`ROUTE-C-CANDIDATE-IMPLEMENTATION-CARD-001A` only. Candidate implementation
remains forbidden.

## What This Does Not Prove

This preservation does not prove Route C mechanism validity, hidden-self-set
inference, self-boundary evidence, candidate success, Gate pass, mainline
effect, runtime readiness, live path, agency, autonomy, consciousness, emotion,
stable user benefit, or EGO readiness.
