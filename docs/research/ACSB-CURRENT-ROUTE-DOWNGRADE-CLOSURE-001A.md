# ACSB-CURRENT-ROUTE-DOWNGRADE-CLOSURE-001A

Preservation and route-closure record for the current
`action_conditioned_self_boundary` (ACSB) route.

This record preserves a route decision only. It does not implement, repair,
rerun, rescore, redesign, or create a replacement harness. It does not create
`001E`. It does not authorize a non-`001E` replacement preflight. It does not
enable or modify any Gate4, Gate5, bridge, tournament, runtime, companion,
EGO-mainline, product, production, or mainline config path.

## Verdict

`preserve_acsb_current_route_downgrade_closure_001a_pass`

Preserved route-decision verdict:

`acsb_route_decision_close_or_downgrade_current_route`

Preserved route consequence:

`acsb_current_route_downgraded_not_mechanism_falsified_001a`

## Layer And Scope

- Current layer: `route-governance / ACSB current-route downgrade preservation only`
- Mainline integration status: none.
- Enabled status: none.
- Real trigger evidence: current handoff containing Claude's route-decision
  review `acsb_route_decision_close_or_downgrade_current_route`, plus the latest
  preserved blocker boundary at `d5b4b9217e6354032f531345027a7d2f3bc11f1e`.
- Claim ceiling: `ACSB current-route downgrade preservation only`

## Start-State Readback

- Branch: `codex/meta-theory-scaffold`
- Local HEAD: `d5b4b9217e6354032f531345027a7d2f3bc11f1e`
- Remote branch `origin/codex/meta-theory-scaffold`:
  `d5b4b9217e6354032f531345027a7d2f3bc11f1e`
- Latest preserved blocker tag:
  `remote-anchor-claude-independent-acsb-001d-execution-001a-hostile-audit-001a-d5b4b92`
- Local blocker tag:
  `d5b4b9217e6354032f531345027a7d2f3bc11f1e`
- Remote blocker tag:
  `d5b4b9217e6354032f531345027a7d2f3bc11f1e`
- Start ahead/behind: `0 0`
- Start status: clean

## Preserved Route Decision

The current ACSB route is downgraded from active route to closed and sealed
harness-hygiene lesson set.

This downgrade is not a mechanism-falsification claim. ACSB remains untested by
a valid `001D` harness. ACSB is neither proven nor disproven. A clean ACSB
harness might or might not show a positive margin.

The following consequences are preserved:

1. `001B`, `001C`, and `001D` are preserved as invalid-harness and
   evidence-hygiene lessons, not as mechanism-negative evidence.
2. `fe5aa85` is not accepted as ACSB negative evidence.
3. `fe5aa85` may be cited only as invalid-harness blocker evidence.
4. No `001E` is created or authorized.
5. No non-`001E` replacement design preflight is authorized.
6. No fourth ACSB harness is authorized by this preservation.
7. Any future ACSB re-entry requires a separate route-decision card for a
   materially different surface, not repair of the current ACSB chain.

For mechanical readback: `001E` is not authorized, and a non-`001E`
replacement design preflight is not authorized.

## Governing Evidence Readback

The preserved blocker boundary is:

- Verdict: `preserve_claude_independent_acsb_001d_execution_001a_hostile_audit_001a_pass`
- Commit: `d5b4b9217e6354032f531345027a7d2f3bc11f1e`
- Tag:
  `remote-anchor-claude-independent-acsb-001d-execution-001a-hostile-audit-001a-d5b4b92`

The audited execution at `fe5aa85430850f4bd7607917b1e73e129a8a8a21` initially
reported:

`acsb_001d_execution_001a_bounded_negative_evidence`

The independent hostile audit preserved at `d5b4b9217e6354032f531345027a7d2f3bc11f1e`
blocked that result as:

`claude_independent_acsb_001d_execution_001a_audit_blocks_feature_impoverished_or_unfair_baseline`

Its route consequence was:

`acsb_001d_execution_001a_bounded_negative_evidence_not_accepted`

## Preserved Downgrade Rationale

The prior ACSB route decision authorized one final corrected challenge only
because it was supposed to be terminal and because the metric could be
structured as:

`candidate_or_reference - max(cheap_baseline_family)`

Under that intended structure, leakage or static decodability should have
self-defeated the evidence claim instead of depending only on scanners.

The `001D` execution failed to honor that structure in substance. The current
handoff and preserved hostile audit identify the same core collapse:

- `full_reference`
- `fair_capacity_disabled_reference`
- `fitted_pure_python_mlp_no_boundary_learner`
- observation-only probe

all reduced to the same target-generation path:

`_target_from_observation = phase_bit XOR action_bit`

where `phase_bit` and `action_bit` are present in `legal_observation`.

Therefore `1.0 / 1.0 / 0.0` is not valid negative evidence. It is an
oracle-minus-oracle constructive identity artifact.

## Anti-Zeno Assessment

The current route has repeatedly collapsed into oracle, lookup,
boundary-inert, or non-causal surfaces. Continuing immediate ACSB repairs would
likely produce more governance artifacts rather than discriminative mechanism
evidence.

This is preserved as an anti-Zeno route consequence.

The correct next action is preservation and stop, not repair. This route record
does not authorize `001E` and does not authorize a fourth ACSB harness.

## Future Re-Entry Boundary

This record does not authorize re-entry.

A future route-decision card for a materially different surface would need to
make hard preflight gates explicit before any implementation. Those conditions
are preserved here only as non-authorizing conditions:

- target cannot be computed from same-step legal observation alone;
- target-generation function must be unreachable from candidate, reference,
  baselines, and decoders by call-graph or AST scan;
- at least one no-boundary learner must genuinely consume train data;
- empty-train and label-shuffle positive controls must reduce learned baseline
  performance;
- no-boundary learner must not call target generator, oracle rule,
  evaluator-only fields, hidden target function, or answer-bearing alias;
- `reference_core.selected_output` must causally depend on boundary memory;
- boundary-memory mutation must change pre-registered reference behavior on
  some cases;
- disable-persistence or capacity-disabled intervention must cause
  pre-registered behavior difference or block;
- replay must recompute behavior depending on serialized boundary state, not
  only same-step legal observation;
- provenance must prove non-oracle call graph and train-consumption evidence;
- strongest fair baseline must be independent and not feature-impoverished;
- positive controls must prove scanners fire for oracle wiring, train
  non-consumption, boundary inertness, label leakage, and path splitting.

These are future re-entry conditions only. They do not authorize re-entry now.

## Claim Ceiling

`ACSB current-route downgrade preservation only`

This does not prove ACSB validity or invalidity in general, mechanism validity,
Gate validity, agency, autonomy, consciousness, emotion, subjectivity, runtime
readiness, EGO readiness, stable user benefit, companion/product readiness, or
mainline effect.

## Next Minimal Closed-Loop Action

Stop ACSB current-route work. Do not repair `001D`. Do not create `001E`. Do not
start a replacement preflight. Future work must move to broader route planning
or a materially different surface only through a separate route-decision card.

## What This Does Not Prove

This preservation does not prove ACSB mechanism validity or invalidity, valid
Gate evidence, agency, autonomy, consciousness, emotion, subjectivity, runtime
readiness, EGO readiness, stable user benefit, companion/product readiness, or
mainline effect. It also does not convert `fe5aa85` into valid bounded negative
evidence.
