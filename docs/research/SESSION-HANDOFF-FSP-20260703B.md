# SESSION-HANDOFF-FSP-20260703B — S3c-R3 audited + banked (incident repaired); S3D spec banked; S3d EXECUTION RUNNING

- Session scope: Claude R3 audit (all numbers recomputed from artifacts) → operator bank → poisoned-index incident + append-only repair → S3D-SHOULD-WIN-NULL-ENV-SPEC-001A drafted + banked → S3d prompt issued → Codex RUNNING S3d now. Roles intact: Codex implements (NO git — the one-time git exception in S3C-R3-BANK-OPS-001A was superseded same-day, never exercised), Claude audits + drafts governance at operator request (disclosed in-file), operator banks/pushes.
- Claim ceiling: instrument code + tuning bookkeeping + (pending) baseline-capability certificates only. Zero environment-validity, baseline-power-beyond-certificate, gap, headroom, mechanism, learning, agency, or EGO claims. First claim-bearing event remains S5 adjudicated in S6.
- Next session reading order: this file → `FSP-STAGE-LEDGER.md` (L-004 current; L-005 pending) → memory `fsp-route-program-001a` → `docs/codex/tasks/FSP-PUM-ENV-IDPROBE-001A-S3D-SHOULD-WIN-NULL-ENV-SPEC-001A.md`.

## 1. State (end of 2026-07-03, second session)

- Commit chain (origin/codex/meta-theory-scaffold): … `567af96` S3c-R2 STOP + 001C → `2575d1a` operator PARTIAL Track-T bank ("Bank TLGP rung1 capacity sweep 002A LFS artifacts": 4 LFS pointers only, DRY_RUN + FULL_SCOUT trace/val_curves; the 6 FULL_SCOUT small jsons are STILL UNTRACKED; ran out-of-order vs 20260703 §4.3, recorded, harmless) → `560d5cb` S3c-R3 BANK (all R3 evidence + defect note + FP declaration + ops card + handoff-20260703; INCIDENT: the poisoned shared index carried PRE-STAGED deletions of 15 governance/record docs + a CLAUDE.md −1 line into the commit; pushed before detection) → `0acb6e9` repair-1 (15 files re-added after per-file `git hash-object` == `ls-tree 567af96` blob verification; `git diff --name-status 567af96 HEAD` shows ZERO deletions; frozen blobs spot-verified restored: 2940db67 / 882a68de / 0a5e38a5) → ⟨S3D spec bank commit — operator executed after this handoff was drafted; verify with `git --no-pager log --oneline -3`⟩.
- S3c-R3 final audited numbers: 40 configs = 24 decoder + 16 seq (001C §3 conformant); 15.0295 / 30.0 CPU-h = EXACT Σ per-config wall-clock (001C accounting); runtime_within_30_cpu_hours; selection argmax verified for all 5 members; all scores chance-level (0.0301–0.0318 vs 1/32 = 0.03125); heldout_numbers_present=false; manifest sha 0f711c8b verified HOST-side (sandbox FUSE view was stale — known illusion class).
- Key audit ruling (precedent): Codex's mid-run CPU-h meter fix = conformance repair TO the frozen 001C accounting definition (per-config whole wall-clock incl. materialization), NOT governance self-modification; direction was against executor interest. But the defective first sweep's outputs were overwritten → `s3c_r3_first_sweep_defect_note.json` records the loss (v1 values = unknown, provenance = null). `s3c_r3_fp_nondeterminism_declaration.json`: recipes deterministic same-platform/same-versions/threads=1 only; cross-platform replay tolerance adjudicated at consumption (S3d/S5).
- Frozen source stack is now EIGHT blobs + vocab: card `0a5e38a5dbe9…` / constitution `882a68de2973…` / execution plan `c07eec2ac384…` / frozen_design `32d2cbd538f3…` / 001A `2da9e5693255…` / 001B `bfabd9ebfbd4…` / 001C `2940db67ad3b…` / S3D spec ⟨recompute: `git hash-object docs/codex/tasks/FSP-PUM-ENV-IDPROBE-001A-S3D-SHOULD-WIN-NULL-ENV-SPEC-001A.md`; must equal the value pasted into the running Codex prompt⟩. F2 vocab sha256 `3a08b3b5a387…` (do not rebuild).
- S3d design summary (all pre-registered in the spec BEFORE any S3d number existed): 18 members × assigned cert cells with ρ (headroom-capture) thresholds anchored to the S2 ideal per cell; 2 cert-only simulator variants added for the constant one-hot degenerates (constant_none / constant_saturated) with mandatory BASE-invariance regression vs banked S3a recipe hashes; cert sets = 7 fresh sets, users 0..799 only, pre-registered seeds 20260711–17; NULL MDE = chance + 0.005 incl. the ideal; budget 12.0 CPU-h with PART 0 projection gate + runtime guard; adjudicates pooled-fitting adequacy (001A §2) and GBT half-data handicap (001B Am.2); episodic_traversal cert vacuous if in-cell fallback ≥ 0.5.

## 2. Operational contract (additions this session — BINDING)

1. Every host bank: unconditional bare `git reset` BEFORE staging; then porcelain gate = staged set equals the expected explicit list with ZERO `D` lines (hard stop); HEAD-pin mismatch = hard stop, no exceptions. Root cause of the incident: `git commit` commits the WHOLE index; scoped `git add` does not protect against pre-staged junk.
2. PowerShell hazards: long single-line git commands get wrapped/mangled silently — use `$paths` arrays + `ForEach-Object`; prefix `git --no-pager` for log/show/diff.
3. `git prune` deliberately postponed (unreachable objects may hold harness-era evidence).
4. Defect-preservation rule now binding for all executors (in the S3d prompt verbatim): instrument defect discovered after outputs exist → preserve defective outputs as `*_v1` BEFORE any rerun.
5. All prior rules remain: ANTI-IDLE in every prompt; file-API reads for provenance on edited files; sandbox pytest = zero evidential force; STOP discipline with preserved manifests; budget lines operator-adjustable with signed note vs evidence thresholds immovable (001C firewall — never cite it against the S2 24 CPU-h tractability line).

## 3. Next session queue

1. **S3d report arrives → audit checklist** (adjudicate strictly per spec §7): 8 freeze blob pins + 6 input sha256 pins verified by executor and re-verified by auditor; BASE-invariance regression vs banked S3a recipe hashes (THE critical check — sealed-generator drift voids everything); new-variant guards (concentration ≥ 0.95, θ-independence 1e-9, ideal-filter sanity 1e-9 on micro-config); PART 0 projection ≤ 12.0 written BEFORE the run; runtime trace monotone, final ≤ 12.0; cell-headroom guard ideal−chance ≥ 0.10 in every should-win cell; per-member ρ recomputed by auditor from the report (assignment table = spec §3; class-present convention stated); NULL table: all 18 members AND the ideal ≤ 0.03625; episodic fallback_rate_in_cell < 0.5; main trajectory sets unread + heldout 800..999 untouched (guard tests present); pytest suite grown from 72 items; any rerun → `*_v1` preserved.
2. **Verdict routing**: `s3d_all_certificates_green` ⇒ pooled fitting + GBT half-data handicap adjudicated ADEQUATE; next = S4 (harness + adjudicator per execution plan; standing S4 obligations in §4). `FAIL_BASELINE_UNDERPOWERED` ⇒ successor spec per pre-registered repair only (S3c members: per-set refit, 001A §2; GBT: full-data refit, 001B Am.2) — no silent switches. `FAIL_NULL_FALSE_HEADROOM` ⇒ instrument invalid, ALL S3d results void, redesign budget untouched.
3. **Bank sequence** (operator, only after audit ACCEPT): §2.1 safe sequence; expected paths = `src/fsp_pum_env`, `tests/fsp_pum_env`, `artifacts/FSP-PUM-ENV-IDPROBE-001A`, this handoff file, plus any not-yet-committed ops/spec files.
4. **Track-T completion** (after S3d bank, order-sensitive): bank the 6 FULL_SCOUT small jsons (LFS large files already in `2575d1a`; explicit paths; Track-F script refuses TLGP paths — manual staging with the §2.1 sequence) → **L-005 ledger transition**, suggested wording: `L-005: TLGP rung1 002A FULL_SCOUT audited — H_cap segment-bounded (bp 8→16→16; C1→C2 unresolved at 2x grid; R0-R5 clean, replay exact); route reopen_rung3_powered_after_powered_rung1_confirm ACCEPTED w/ confirm-card grid-resolution requirement.`
5. **Loose ends**: AGENTS.md uncommitted anti-commentary lines (operator keep-or-revert, separate commit); 13 worktree ` M` files suspected EOL churn (diagnose before ever adding; do NOT bundle into banks); optional hardening: add 001A/B/C + S3D spec blob shas to the checkpoint script FROZEN_FILES (separate commit).

## 4. S4 standing obligations (unchanged, all still open)

Planted-VALUE leak mutant (scanner currently key/type-matching only); `stable_fact` RNG stream reconciliation into the declared vocabulary; runtime sealing enforcement at harness level (battery cannot reach controlled_theta / response_distribution); per-gap N interpretation note; policy-class compute evaluation plan (naive 20× ≈ 170 CPU-h — needs S4 power design).

## 5. Evidence-status marks

- S3a / S3b / S3c(R1,R2,R3): repo-committed, pushed, audited; failure manifests + defect note preserved verbatim; two S3c STOP precedents + the R3 defect-note precedent stand.
- S3d: RUNNING; no artifacts yet; nothing banked; certificate thresholds pre-registered clean (drafted before any S3d number existed).
- rung1 FULL_SCOUT: audited; LFS大件 banked in `2575d1a`; small jsons + L-005 pending.
- Score exposure to date: chance-level internal-validation figures only (001C disclosure + R3 report). Nothing selectable, nothing heldout, nothing gap-shaped.
