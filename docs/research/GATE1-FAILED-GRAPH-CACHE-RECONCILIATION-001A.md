# GATE1-FAILED-GRAPH-CACHE-RECONCILIATION-001A

## Bounded Task Card

Task id: `GATE1-FAILED-GRAPH-CACHE-RECONCILIATION-001A`

Problem definition: re-run `POST-FREEZE-GATE0-3-SEQUENTIAL-REPAIR-QUEUE-001A` after the anchored Gate2 ablation repair, with default `max_queue_items_per_run = 1`, selecting only `GATE1-FAILED-GRAPH-CACHE-RECONCILIATION`. The task must reconcile the Gate1 literal-pass surface against the prior `failed_graph_cache_collapse` lineage before any repair-to-pass attempt.

Current stage/layer: engineering implementation / evidence-governance only.

Mainline target: none. This task does not target EGO mainline, runtime, bridge, admission, Gate4, integrated testbed, or Gate3.

Enabled-state requirement: no enabled mechanism path, no live entrypoint, no runtime path, and no downstream authorization.

Real-trigger evidence requirement: current repo readback must show the anchored Gate2 ablation repair is at or behind HEAD; the queue must select only the next unresolved item; the Gate1 reconciliation must use machine-readable lineage artifacts and callable-run ledger evidence.

Hypothesis: the correct next queue action is not a Gate1 pass repair. If callable graph-cache, window/cache, count-table, successor-map, and replay controls from the prior Gate1 lineage match or beat the candidate, the queue must preserve downgrade/closure and block positive Gate1 inheritance.

Strongest baseline: the apparent Gate1 001C pass surface is a report-shaped artifact whose baseline and ablation reports can be emitted by literals rather than by candidate reruns or fair control execution.

Ablation requirement: real ablation rerun evidence must be verified from callable execution provenance. Static ablation dictionaries or unconditional `executed: true` flags are insufficient.

Trace/replay requirement: replay/lineage evidence must come from recomputation or verified callable ledger artifacts, not from natural-language summaries alone.

Computed-evidence provenance gate: the reconciliation runner must compute hashes for source artifacts, parse JSON/JSONL inputs, check required control family execution in the run ledger, check prior result verdicts, verify that graph/cache/count/successor controls matched candidate under the prior rule, and write machine-readable artifacts.

Acceptance gate: selected item count is exactly one; selected item is `GATE1-FAILED-GRAPH-CACHE-RECONCILIATION`; Gate1 001C literal-pass evidence is not upgraded; prior `failed_graph_cache_collapse` is preserved if fair controls match or beat candidate; output artifacts include `selected_item.json`, `lineage_reconciliation.json`, `baseline_comparison.json`, `ablation_report.json`, `result.json`, `readback.json`, and `claim_ceiling.txt`; changed files remain in this task card, isolated Gate1 reconciliation source/test files, and isolated queue artifacts only.

Claim ceiling: Gate1 evidence reconciliation and post-freeze queue selection only. This does not prove Gate1 pass, replay/consolidation mechanism validity, bridge readiness, runtime readiness, EGO readiness, agency, consciousness, subjectivity, real emotion, autonomy, or stable user benefit.

Stop condition: stop if the selected queue item is not exactly Gate1 reconciliation; if any Gate3, integrated testbed, Gate4, bridge, admission, runtime, or unrelated file must be modified; if source artifacts are missing; if the prior failed lineage cannot be verified; if callable controls or real ablation provenance cannot be verified; or if the result would need to rewrite old artifacts into a pass.

Rollback plan: remove only the new isolated task-card, source/test, and artifact files created for this reconciliation; do not alter old Gate1, Gate2, Gate3, integrated, Gate4, bridge, admission, runtime, or EGO artifacts.

Expected changed files:

- `docs/research/GATE1-FAILED-GRAPH-CACHE-RECONCILIATION-001A.md`
- `src/gate1_failed_graph_cache_reconciliation_001a/`
- `tests/test_gate1_failed_graph_cache_reconciliation_001a.py`
- `artifacts/post_freeze_gate0_3_sequential_repair_queue_001a_gate1_failed_graph_cache_reconciliation/`

Forbidden changes:

- Gate3 source, tests, or artifacts
- integrated Gate0-Gate3 testbed source, tests, or artifacts
- Gate4 source, tests, or artifacts
- bridge, admission, runtime, EGO mainline, UI, LLM, AIRI, deployment, or companion paths
- old Gate1 or Gate2 artifacts outside isolated read-only source inputs
- unrelated files

Auto-Remote-Anchor decision: forbidden.

Local commit decision: forbidden unless separately requested after review.

## Bounded Audit Before Execution

Real objective: convert the next unresolved post-freeze queue item into a verifiable Gate1 reconciliation artifact, without laundering a literal Gate1 pass into mechanism evidence.

Problem-definition risk: the wrong task would try to make Gate1 pass. The correct task first asks whether prior callable graph-cache/window/count/successor controls already explain the candidate.

Strongest baseline explanation: a trivial report producer can emit Gate1 001C baseline and ablation JSON with `executed: true`, low baseline match rates, and a pass-shaped result without invoking the candidate, controls, or ablations.

Strongest reason the task may be invalid: if current artifacts cannot verify callable controls and real ablation reruns, this reconciliation cannot decide anything beyond "still blocked". It must not invent new Gate1 evidence.

Falsification of current framing: a scoped, authorized callable rerun would need to show that fair graph-cache/window/count/successor controls no longer match or beat the candidate and that ablations rerun the candidate through the real path. This task does not assume that result.

Evidence still insufficient after this task: any Gate1 pass claim, mechanism-validity claim, bridge/readiness claim, runtime claim, or EGO-mainline claim.

Mechanism-vs-behavior classification: this is evidence-governance reconciliation. It tests admissibility of prior evidence and preserves closure if fair controls explain the behavior; it does not test a new mechanism.

Hard-coding, leakage, and weak-baseline checks: static verdict dictionaries, literal baselines, unconditional clean reports, missing ledger execution, omitted graph-cache family controls, missing window/cache controls, unused seeds, missing ablation reruns, schema split, second logic path, and claim inflation all block positive Gate1 inheritance.

Minimal validation: run the reconciliation unit test, run the Gate1 reconciliation module to generate artifacts, validate generated JSON, run `git diff --check`, and verify final git status/scope.

Acceptance signal: the queue artifact selects only Gate1 reconciliation and preserves `failed_graph_cache_collapse` or downgrade/closure when callable controls match or beat the candidate.

## Claim Ceiling

This card authorizes only post-freeze queue selection and Gate1 evidence reconciliation. It does not authorize Gate1 repair-to-pass, Gate3, integrated testbed, Gate4, bridge, admission, runtime, EGO mainline, mechanism tournament, model-class reset, or any readiness claim.
