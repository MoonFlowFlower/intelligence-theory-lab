# CMBC Companion Parametric Action Expansion 006 Contract

Status: contract-only. No implementation or execution is authorized.

## Decision

`CMBC-COMPANION-PARAMETRIC-ACTION-INTERFACE-005-SHADOW-IMPLEMENT` passed:

`parametric_shadow_n7_compatibility_pass`

That result only shows that the 005 `CandidateOption` interface can preserve the old N=7 replay path. It does not show expanded action-space advantage.

The next authorized object is only this contract:

`CMBC-COMPANION-PARAMETRIC-ACTION-EXPANSION-006-CONTRACT`

## Goal

Define a bounded N>=20 parametric action-space expansion test using the passed 005 N=7 shadow interface. The future test must ask whether CMBC's free-input causal-probe advantage survives expanded anonymous `CandidateOption` spaces without semantic label leaks, renderer control, threshold retuning, RAG weakening, or rewriting 003/005 evidence.

This contract does not implement a selector, does not execute N>=20, and does not authorize a real companion agent.

## Current Claim Ceiling

Current maximum claim:

`CMBC has bounded free-input causal-probe evidence under the current small 7-action anonymous action set, plus N=7 parametric shadow compatibility evidence. Expanded action-space advantage is not established.`

This contract cannot prove:

- expanded action-space causal-probe advantage
- scalable companion behavior control
- open-ended action generation
- real companion agent readiness
- real proactive messaging safety
- LLM renderer safety in production
- EGO readiness
- consciousness
- subjective experience
- true self-awareness
- AGI
- life
- real emotion
- real love

## Required Sources

Future execution must reuse:

- `artifacts/cmbc_companion_parametric_action_interface_005_contract/candidate_option.schema.json`
- `artifacts/cmbc_companion_parametric_action_interface_005_shadow/shadow_result.json`
- `artifacts/cmbc_companion_free_input_live_lab_003_reexecute/free_input_live_lab_003_reexecute_result.json`
- frozen 20-turn free-input transcript
- stable outcome coding ledger
- predeclared 003B causal probe pack

New data is forbidden unless a later human review explicitly authorizes it.

## Expanded Action-Space Contract

Future execution must use between 20 and 50 anonymous `CandidateOption` records.

The variable-N path must not contain branches such as:

- `if N == 7`
- `if N == 20`

The future test must fail with `fixed_twenty_action_table_detected` if it creates a fixed 20-action table, fixed semantic recipes, or `ACTION_HANDLES = 20`.

## Selector-Visible Fields

Allowed selector-visible fields are only those permitted by the 005 `CandidateOption` and `ParametricSelectorInput` contracts:

- public observation features
- anonymous option IDs
- own-intervention history
- observed outcomes
- learned predicted effect vectors
- uncertainty / confidence values derived from allowed experience
- non-semantic goal / constraint vectors
- public horizon / budget
- prior support references without semantic labels
- non-semantic cost / risk / budget features

Forbidden selector-visible fields:

- semantic action labels
- public action names
- action family names
- natural language action descriptions
- rendered text
- renderer prompts
- object / entity names as shortcuts
- task ID, scenario ID, contract ID, cycle ID
- hidden latent state
- hidden future state
- oracle effects
- oracle action-to-effect table
- oracle plans
- evaluator metrics
- expected outputs
- baseline outputs

## Required Future Probe Coverage

Future execution must include:

- label permutation invariance
- effect swap sensitivity
- same-text different-causal-history probes
- supporting-prior deletion
- outcome perturbation
- feedback admission single contradiction
- feedback admission repeated feedback
- later correction / context narrowing
- renderer adversarial isolation
- behavior-only replay over the full N-option distribution

## Required Expanded Baselines

Future baselines must receive the same anonymous `candidate_options` list as CMBC:

- `RAGSummaryMemoryBaseline`
- `StrongHumanLikeHeuristicBaseline`
- `ExpandedContextualHeuristicBaseline`
- `ExpandedActionFrequencyBaseline`
- `ExpandedActionNearestNeighborBaseline`

Baseline outputs are forbidden selector inputs.

## Minimum Future Execution Gates

```text
candidate_option_count >= 20
candidate_option_count <= 50
semantic_label_visible_to_selector = false
public_action_name_visible_to_selector = false
rendered_text_visible_to_selector = false
natural_language_description_visible_to_selector = false
label_permutation_change_rate = 0.0
effect_swap_change_rate >= 0.8
rag_causal_probe_match_rate < 0.5
strong_heuristic_causal_probe_match_rate < 0.5
expanded_contextual_heuristic_causal_probe_match_rate < 0.5
expanded_action_frequency_match_rate < 0.5
expanded_action_nearest_neighbor_match_rate < 0.5
behavior_only_replay_match_rate = 1.0
renderer_action_change_rate = 0.0
action_distribution_entropy_reported = true
dominant_action_rate_reported = true
```

## Evidence Preservation

003 remains small-action-set evidence.

005 shadow remains N=7 compatibility evidence.

006 must not rewrite 003 or 005 as expanded action-space evidence. If future N>=20 execution passes, it must be recorded as new bounded evidence, not retroactive proof of earlier stages.

## Stop Conditions

Stop with `boundary_violation` or a more specific allowed verdict if the contract or future execution requires any of:

- `ACTION_HANDLES = 20`
- fixed 20-action recipe table
- semantic action labels in selector input
- public action names in selector input
- natural language action descriptions in selector input
- action family names in selector input
- renderer text in selector input
- threshold retuning
- RAG baseline weakening
- oracle effects or oracle action-to-effect table
- evaluator metrics in selector input
- baseline outputs in selector input
- rewriting 003 as parametric evidence
- rewriting 005 shadow as expanded evidence
- EGO integration
- real companion implementation
- proactive messages
- LLM action selection

## Allowed Verdicts

- `parametric_action_expansion_006_contract_ready`
- `contract_incomplete`
- `fixed_twenty_action_table_detected`
- `semantic_leak_risk_unresolved`
- `expanded_baseline_contract_incomplete`
- `replay_contract_incomplete`
- `evidence_preservation_failed`
- `boundary_violation`

## Recommended Next Task

If ready, the recommended next task is:

`CMBC-COMPANION-PARAMETRIC-ACTION-EXPANSION-006-EXECUTE`

That task is not authorized by this contract.
