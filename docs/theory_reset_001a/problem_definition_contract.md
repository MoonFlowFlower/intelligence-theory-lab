# THEORY RESET 001A Problem Definition Contract

## Contract Status

```text
task_id = THEORY-RESET-NEW-PROBLEM-DEFINITION-001A
contract_type = successor problem definition
verdict = theory_reset_001a_problem_definition_bounded_pass
implementation_authorized = false
training_authorized = false
```

## Successor Problem

The successor problem is:

```text
Can a bounded mechanism show intervention-sensitive internal update,
counterfactual action-conditioned adaptation, or process-level state change
under intervention that remains distinguishable from fair full-history cheap
controls under trace/replay, ablation, deletion, and counterfactual probes?
```

## Non-Goals

```text
not_a_representational_gap_rescue = true
not_output_only = true
not_a_model_class_reset = true
not_Gate1_reopen = true
not_same_agent_bridge = true
not_EGO_integration = true
not_companion_or_emotion_or_relationship_work = true
```

## Minimum Future Acceptance Shape

A future preflight may proceed only if it freezes:

```text
intervention probes
state-delta metrics
trace/replay schema
cheap-control adversary set
oracle/leakage probes
ablation set
shuffle/random-representation controls
failure taxonomy
claim ceiling
```

It must fail if fair controls reproduce both outputs and declared process
traces.

## Maximum Claim

```text
bounded successor problem-definition evidence only
```
