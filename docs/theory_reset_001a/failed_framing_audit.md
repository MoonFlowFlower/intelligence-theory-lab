# THEORY RESET 001A Failed Framing Audit

## Rejected Old Problem

```text
old_problem = output-level representational gap against cheap controls
verdict = failed_after_REPRESENTATIONAL-GAP-PREFLIGHT-001B
```

001B showed that the K<=4 bounded-window residue collapses when fair
full-history controls are allowed.

## Rejected Framing Patterns

```text
obscure_XOR_or_parity_search = rejected
raising_K_until_controls_fail = rejected
lowering_challenger_capacity = rejected
banning_summary_count_statistic_controls = rejected
calling_cache_or_FSM_state_a_mechanism = rejected
output_prediction_success_as_mechanism_evidence = rejected
model_class_reset_leap = rejected
Gate1_reopen_leak = rejected
same_agent_bridge_leak = rejected
EGO_scope_leak = rejected
LLM_RAG_companion_emotion_relationship_user_model_leak = rejected
```

## Reason

These framings either recreate the 001A/001B failure mode, depend on unfair
control exclusion, or attempt to jump from bounded negative evidence to
downstream implementation authority.

## Required Replacement

The replacement must target intervention-sensitive process evidence with
trace/replay and ablation probes. It must carry forward fair cheap controls
instead of weakening them.
